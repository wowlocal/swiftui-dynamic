# Flash Cards

An interactive SwiftUI study session demonstrating local state, model arrays,
conditional views, progress, button actions, and a small scoring flow.

Run it from the repository root:

```sh
swift run DynamicSwiftUIDemo --project Examples/FlashCards
```
