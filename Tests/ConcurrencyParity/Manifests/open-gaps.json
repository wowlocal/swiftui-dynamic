[
  {
    "id": "remaining-generated-task-group-surface",
    "requirementRef": "M4/remaining-task-group-surface",
    "kind": "surface-gap",
    "expectedObservation": "The active _Concurrency.swiftinterface inventory is generated and every mechanically exposed group member and overload has an explicit implementation/verification disposition with positive evidence or an owned negative/deferral rationale.",
    "currentObservation": "The generated inventory retains every active task-group overload plus all six public TaskGroup.Iterator and ThrowingTaskGroup.Iterator next/cancel rows with effect/isolation metadata. All seventy-seven generated task-group and iterator rows now have authored dispositions: the four public scope rows; state, cancelAll, canonical addTask/addTaskUnlessCancelled, named add, executor-preference add/addUnless, immediate add/addUnless, deprecated aliases, makeAsyncIterator, next, and iterator cancel declarations are all accounted. Same-source native parity covers the default-isolation scope-function subsets, the supported nonisolated ordinary-add subsets, conditional acceptance before cancellation and rejection afterward, iterator value semantics and terminal states, waitForAll, nextResult, and Task.name preservation. Strict Swift 6 probes select each named, executor-preference, conditional, and immediate overload family. For all eight immediate declarations, the explicit-nil inherited-MainActor subset matches Swift's guaranteed causal behavior: an operation starts synchronously before its add call returns, resumes after forced suspension, preserves Task.name, and is joined at scope exit; an active conditional call returns true, while a call after cancelAll returns false without creating the child and before preference validation. No sibling finish order or physical-thread behavior is claimed. Shared runtime guards reject active non-nil TaskExecutor preferences instead of silently ignoring them, but native CountingTaskExecutor evidence proves preference-driven resumption is observable. The sixteen ordinary executor-preference rows and eight immediate rows therefore remain known divergences together with arbitrary @isolated(any) operation-executor behavior. Pinned swiftlang inputs independently anchor ordinary group behavior; start-immediately inputs provide semantic and compile provenance but their task-group executor block is disabled, so local same-source runtime evidence remains authoritative. No currently generated task-group overload is unreviewed. Repeated wait/new-work semantics, task-executor preference scheduling, and arbitrary actor executors stay open and are demand-deferred (2026-07-16): they have zero measured demand across the 669-project corpus and FoodTruck and reopen only when a real interpreted program fails on a cited declaration."
  },
  {
    "id": "async-sequence-continuation-runtime",
    "requirementRef": "M6/protocol-iteration-streams-and-continuations",
    "kind": "coverage-gap",
    "expectedObservation": "Demand-cited protocol AsyncSequence iteration, streams, checked continuations, cancellation, and cleanup have native parity; both unsafe continuation entry points fail closed with explicit diagnostics and native acceptance anchors; broader unsafe ownership, negative capacities, and upstream adapter work stay demand-deferred until cited.",
    "currentObservation": "Finite interpreted and typed-host AsyncSequence iteration has same-source parity for success, typed failure, cooperative cancellation, control-flow cleanup, default witnesses, mutating iterator state, and tracked host suspension. AsyncStream and AsyncThrowingStream share runtime-owned suspension, cancellation, finish/failure, nonnegative buffering, copied-iterator, final-owner termination, escaped producer-handle, and cleanup evidence; negative capacities remain fail closed. The public withCheckedContinuation slice routes explicit isolation: nil, MainActor.shared, and a source actor plus omitted caller-defaulted #isolation from nonisolated, MainActor, and source-actor callers, resume(returning:), zero-argument Void resume(), and nonthrowing Result<T, Never> resume(with:) through a bounded continuation registry. Detached source producers wake one owner; the contextual body owns the selected actor; a different caller executor is restored after body return; canonical waitingForContinuation releases an already-owned source actor, permits a gated actor message to reenter, and reacquires the actor before continuation; both normal completion and infrastructure cancellation drain registry/task/session ownership. withCheckedThrowingContinuation shares that record; direct resumes project an exact value and original source error, concrete-error and existential Result<T, any Error> success/failure through resume(with:) delegate to those same terminal transitions, and its MainActor plus explicit/caller-defaulted source-actor error paths have exact contextual-body, mailbox-reentry, caller-restoration, and cleanup evidence. Omitted isolation is materialized once from caller lexical isolation. Both checked forms now have process-isolated double-resume trap parity: the first terminal transition wins, a second resume is fatal, and the invariant bypasses source `do`/`catch`; focused evidence proves one-record cleanup for each immediate misuse. Both checked forms also have successful-process abandonment-warning parity on final token release; the source task remains suspended, source catch is not entered, and explicit host cancellation is used only to drain the isolated interpreter child afterward. Escaped resumed-token lifetime now has exact same-source parity for both checked forms: after owner completion the retained token does not retain the task-local sentinel, continuation record, runtime, session, or interpreter, and token release is inert. Both unsafe continuation entry points now route from the generated active-interface surface to one shared fail-closed ownership intrinsic, with form-specific named diagnostics before body invocation or continuation allocation. Native acceptance is anchored by twenty exact `value:33` and `value:44` runs. Broader unsafe ownership, negative stream capacities, and the pinned upstream stream adapter remain explicit demand-deferred divergences. The pinned swiftlang async_stream.swift file remains an independent needs-adapter anchor because it imports StdlibUnittest and covers a broader surface."
  },
  {
    "id": "generated-concurrency-signatures-and-preflight",
    "requirementRef": "M7/generated-signatures-and-preflight",
    "kind": "surface-gap",
    "expectedObservation": "The generated _Concurrency inventory has a complete denominator and every declaration has reviewed positive evidence or a precise owned negative or deferral disposition.",
    "currentObservation": "All 177 declarations in the current active-SDK denominator are reviewed, including selected nominal members declared directly in actor bodies as well as extensions, and immutable target-aware project manifests now bind original source membership, target/compiler identity, native preflight, runtime conditional compilation, and resource roots. The inventory scope itself remains explicitly incomplete, while known-divergent and deferred declarations still require this owned gap until their narrower M4, M5, M6, or M9 semantics are implemented or the denominator is declared complete."
  },
  {
    "id": "physical-parallel-runtime-and-sanitizers",
    "requirementRef": "M9/parallel-runtime-and-sanitizers",
    "kind": "coverage-gap",
    "expectedObservation": "An opt-in parallel runtime executes eligible detached/background source work on physical workers while preserving cooperative-mode semantics, executor isolation, deterministic ownership cleanup, and a green Thread Sanitizer stress board.",
    "currentObservation": "ParsedProgram is immutable and Sendable and owns folded syntax, source locations, and one composite metadata capability covering declarations, callable/accessor/subscript facts, normalized call-site callee plus argument shape, member plans, nominals, property storage, enum cases, extensions, type aliases, and deinitializers. Sessions retain one target-specific ResolvedProgramPlan and MainActor-confined RuntimeProgramState across callbacks, SwiftUI tasks, and descendant source tasks. The core target has no blanket MainActor default; mutable evaluator/runtime/value/heap capabilities declare their confinement explicitly. RuntimeEntry projects only checked identity, immutable plan/metadata, and recursively copied value snapshots; Box, Environment, Instance, heap, closure, symbol, and opaque host values fail closed. A bounded driver executes checked jobs with real Task.detached workers, one shared FIFO permit pool, ordered results, cancellation/failure drain, and capture release. Explicit parallel mode currently admits six demand-scoped signature-free source kernels: literals, immutable String.count, immutable Substring-array map-count reduction, exact Task.yield, immutable String/String.Index distance, and immutable-Bool conditional Task.sleep. Cooperative mode remains the default; unsupported bodies remain confined. FreeChat's exact signature-free two-item `try?` sleep prefix now executes the core Task.sleep command physically, releases its permit at confined re-entry, and retains the awaited suffix plus complete RuntimeValue/Error outcome behind a Sendable task token. Provenance's exact capture-only weak-self nanoseconds spelling reuses that continuation boundary and enters `MainActor.run(body:)` through a selected-nominal intrinsic generated from the active _Concurrency interface. Session-iOS's exact capture-only weak-self literal-Duration spelling reuses that continuation boundary before an actor-isolated optional-self call with one directly captured immutable String; the actor, weak box, String, suffix, and outcome remain confined. Planet's signature-free literal-nanoseconds spelling reuses the same continuation before `MainActor.run(body:)`; the implicit receiver, suffix, actor entry, and outcome remain confined. Planet's complete signature-free `MainActor.run(body:)` detached body now uses an empty physical wrapper and the same confined continuation/outcome token; imported nominal identity is required before launch. Planet's exact one-expression `@MainActor in` operation reuses that wrapper and token while the authored closure remains confined; additional attributes and source-shadowed MainActor declarations stay cooperative. Cooperative Task.detached operation entry now treats closure-declared actor isolation separately from API-selected lexical inheritance: explicit MainActor and lazily resolved user-global-actor annotations survive suspension, while a plain detached closure no longer inherits its MainActor formation site; Task.immediate and Task.immediateDetached retain their distinct operation-executor rules. A demand-cited characterization additionally proves closure-signature `@Sendable` is executor-inert: ordinary Task retains MainActor lexical inheritance and Task.detached suppresses it before and after suspension. The Provenance-cited weak-self, KeyboardCowboy-cited named-weak, Swiftfin-cited weak/strong, and apple-browsers-cited strong/weak/weak explicit-MainActor compositions now use one typed continuation-signature capability and the empty physical wrapper; each complete closure and capture box remains confined, actor isolation survives suspension, strong ownership remains strong, and causally released weak referents disappear. Swiftfin's complete cited form also composes that wrapper with confined `for await` iteration, a per-element weak-self guard, and an optional callback; a causal release branch exits without a callback. Damus's parenthesized `Task.detached(priority:operation:)` spelling is characterized through the same call-site metadata, generated Task gateway, signature-free ClosureValue, and typed Task.yield kernel; it records the same physical receipts without adding a worker capability. The captureless, one-exact-weak, exact weak/strong, and exact strong/weak/weak shapes are admitted; reordered/short/aliased lists, unowned ownership, extra captures, reversed attributes, and source-shadowed MainActor remain cooperative. Swiftfin's contextual .detached spelling now resolves through its Task return annotation and the existing generated imported-static gateway; source types and extensions keep precedence, unknown imported statics retain their marker fallback, and the cited multi-expression task remains cooperative. Planet's cited inherited async Void call now carries one labeled directly owned immutable String capture through a checked worker snapshot and confines source invocation to re-entry; String literals, mutable captures, and other executor routes stay cooperative. Amperfy's exact weak-self @concurrent call uses that copied String schema while deferring receiver lookup until confined re-entry. Provenance's exact weak-self inherited timeout call uses typed literal provenance for one labeled String while keeping receiver lookup deferred until confined re-entry. Session-iOS's repeated updateMentions call uses typed captured-immutable provenance for one labeled String on that same inherited route. KeyboardCowboy's reload call carries one unlabeled immutable `[String]` through a recursively verified string-array command kind. Meshtastic-Apple's exact direct-self argument-free `try? await` call contains ordinary throwing-Void outcomes during confined re-entry and returns only typed Optional<Void> snapshots. FreeChat's contained sleep cancellation continues into the second expression in the same cancelled logical task without sending the suffix or its outcome to a worker. Provenance's deprecated nanoseconds sleep has the same cancellation/continuation contract while its imported MainActor body executes under the logical MainActor and a same-named source type remains source-resolved. Physical Task.yield and Task.sleep admission now requires both immutable explicit-member callee shape and the exact registered core Task host-function identity in the originating closure's lexical environment; a same-named source value remains cooperative. String.count admission asks the originating RuntimeProgramState for a typed property-target identity, so a same-module source extension shadows the imported member and stays cooperative. String.distance admission requires a typed standard-library method proof; any same-base source overload keeps execution cooperative until exact session-owned overload resolution exists. The compound count-reduction kernel now requires Array.map, Array.reduce, and statically inferred Substring.count proofs; core Array source extensions dispatch before imported members, and Array key-path evaluation preserves source String-versus-Substring root identity. All six snapshot kernels, all four sleep-prefix routes, and all five exact MainActor continuation wrappers plus the direct-self MainActor argument-free/Boolean-literal, @concurrent direct-self integer-argument or weak-self single-String, and default-actor synchronous Void, async required single-Int Void, or async explicit single-defaulted-Bool Void, and actor-declared custom-global-actor argument-free async Void plus inherited-caller argument-free or single-String async Void source-call wrapper subsets, including the exact capture-only `[self] in` and `[weak self] in await self?.processQueue()` spellings, have exact physical receipts, and the scoped TSan board covers twenty native-overlap repetitions plus all 100 driver/kernel/source-call tests. FoodTruck's Task.detached { await self.updatesLoop() } remains exact at target:member. Beyond immutable callee shape, a uniquely shape-resolved own reference method now publishes an origin-bound Sendable descriptor containing declaration identity, lexical placement, isolation/effects, and return type. Source instances retain their originating RuntimeProgramState so later facade runs cannot rebind the method; same-shape overloads fail closed. The FoodTruck direct-self MainActor subset now launches a real detached wrapper with a Sendable entry/task/descriptor/result command, restores the confined EvaluationTaskContext through a MainActor relay, copies only Void/String results, preserves logical cancellation and trap containment, and releases its worker permit at executor entry. TaskObservatory's three direct-self @concurrent nonisolated calls with labeled Int/Int64 arguments now launch real detached wrappers from copied integer literals or directly owned immutable captures, then re-enter the confined source evaluator under the logical cooperative-default task context. The unchanged project preserves its exact structured-concurrency result, records three physical receipts at maximum parallelism one, and drains every task/group/scope registry. iTorrent's cited true/false MainActor calls now carry an explicit Boolean command kind validated against both the selected Bool parameter and copied snapshot, recording two physical receipts; captured Bool remains outside the cited depth cap. Session-iOS's cited detached wrapper now re-enters the exact default-actor mailbox for its argument-free synchronous Void method and records one physical receipt. Planet's cited async actor wrappers now cover one required immutable labeled Int and one explicitly supplied Boolean literal for a defaulted Bool parameter; both release/reacquire the mailbox around suspension, and each records one physical receipt. Provenance's cited source-class wrapper now performs one physical launch for an argument-free async Void method on an actor-declared custom global actor; declaration admission is side-effect free, and confined invocation resolves and preserves the canonical actor across suspension. iTorrent's cited plain async source-class wrapper now inherits the detached caller through one physical launch and preserves nil actor isolation across suspension. Provenance's cited capture-only `[self] in` wrapper now takes that same physical route while the runtime task record retains the confined strong receiver. Its exact capture-only `[weak self] in await self?.processQueue()` wrapper also launches physically, but registration retains only the confined weak box and the MainActor relay reads it after worker entry before returning Optional.none or invoking the re-resolved target. The Amperfy-cited weak captured-String route carries no receiver across the worker boundary, and an occupied-permit regression proves release before re-entry. The Provenance-cited weak inherited String-literal route reuses that non-owning relay. Session-iOS extends the same inherited route to one directly owned immutable String capture without widening the weak @concurrent route. KeyboardCowboy adds one directly owned immutable `[String]` capture on that inherited weak route. Meshtastic-Apple adds a typed suppress-to-optional error disposition only for its direct inherited argument-free throwing Void route; fatal/session-abort control flow and uncited throwing routes remain outside it. Argument-bearing methods beyond the direct inherited captured-String, weak-self inherited literal/captured-String or captured-`[String]`, or weak-self @concurrent captured-String Void routes, String-returning methods, struct/enum-backed global actors, and custom executors remain cooperative. Capture ownership modifiers other than the exact weak-self route, aliases, multiple captures, explicit parameter/effect/return clauses, and non-call capture-list bodies remain cooperative. Weak/optional-self explicit-member calls outside the exact inherited argument-free, literal/captured-String, or captured-`[String]` Void routes and the @concurrent captured-String route remain cooperative and suspension-aware: a live source class/actor receiver suspends, while nil skips argument evaluation. Optional async closures likewise remain cooperative and suspension-aware: a present callable suspends, while nil skips argument evaluation and invocation. A causally gated detached weak capture releases its receiver while suspended, and runtime task ownership does not strengthen that capture. Optional source-value async mutation now preserves write-back through direct Optional storage and enclosing source stored properties across normal, source-thrown, and cancellation exits, plus ordinary synchronous nonthrowing get/set Optional properties on directly named source values; native coroutine accessors, async/throwing computed getters, computed reference or nested owners, source subscripts, and general physical weak-call routing remain unimplemented; every unadmitted Optional mutation owner now fails closed with one named storage-transaction diagnostic pending a demand citation, and native read/modify coroutine declarations fail at collection with a named coroutine-ownership diagnostic instead of degrading into an ordinary getter. Mutable or expression arguments, captured Bool, omitted defaults, defaulted integers, nondefaulted Boolean parameters, other noninteger arguments beyond the admitted immutable String capture, cited weak String literal, or cited weak `[String]` capture, explicit nonisolated methods, inherited methods outside the exact argument-free, single-String async Void, or argument-free `try?`-contained throwing Void subsets, String/multiple-argument/throwing/custom-executor actor calls, ambiguous calls, and richer results remain cooperative. Remaining work is semantic call-target resolution beyond the unique own reference-method subset, including same-shape type overloads, inherited/protocol witnesses, actor routing beyond the exact default-actor synchronous Void, async required single-Int Void, and async explicit single-defaulted-Bool Void subsets, custom-global-actor routing beyond the exact actor-declared argument-free async Void subset, inherited-isolation routing beyond the exact inherited-caller argument-free, single-String async Void, or argument-free `try?`-contained throwing Void subsets, host routing, and standard-library target families beyond the current core Task, String.count, conservative String.distance, Array.map, Array.reduce, and Substring.count proofs; the exact compound count-reduction target chain is complete. Remaining work also includes completion of session-owned mutable evaluation, safe lowering for further demand-cited source calls/expressions/suspensions, and expansion of differential/TSan evidence without exposing confined runtime objects; the public testing-only ExecutorJob hook remains preflight-only/deferred."
  }
]
