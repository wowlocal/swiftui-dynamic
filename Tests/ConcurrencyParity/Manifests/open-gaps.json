[
  {
    "id": "remaining-generated-task-group-surface",
    "requirementRef": "M4/remaining-task-group-surface",
    "kind": "surface-gap",
    "expectedObservation": "The active _Concurrency.swiftinterface inventory is generated and every mechanically exposed group member and overload has an explicit implementation/verification disposition with positive evidence or an owned negative/deferral rationale.",
    "currentObservation": "The generated inventory retains every active task-group overload plus all six public TaskGroup.Iterator and ThrowingTaskGroup.Iterator next/cancel rows with effect/isolation metadata. All seventy-seven generated task-group and iterator rows now have authored dispositions: the four public scope rows; state, cancelAll, canonical addTask/addTaskUnlessCancelled, named add, executor-preference add/addUnless, immediate add/addUnless, deprecated aliases, makeAsyncIterator, next, and iterator cancel declarations are all accounted. Same-source native parity covers the default-isolation scope-function subsets, the supported nonisolated ordinary-add subsets, conditional acceptance before cancellation and rejection afterward, iterator value semantics and terminal states, waitForAll, nextResult, and Task.name preservation. Strict Swift 6 probes select each named, executor-preference, conditional, and immediate overload family. For all eight immediate declarations, the explicit-nil inherited-MainActor subset matches Swift's guaranteed causal behavior: an operation starts synchronously before its add call returns, resumes after forced suspension, preserves Task.name, and is joined at scope exit; an active conditional call returns true, while a call after cancelAll returns false without creating the child and before preference validation. No sibling finish order or physical-thread behavior is claimed. Shared runtime guards reject active non-nil TaskExecutor preferences instead of silently ignoring them, but native CountingTaskExecutor evidence proves preference-driven resumption is observable. The sixteen ordinary executor-preference rows and eight immediate rows therefore remain known divergences together with arbitrary @isolated(any) operation-executor behavior. Pinned swiftlang inputs independently anchor ordinary group behavior; start-immediately inputs provide semantic and compile provenance but their task-group executor block is disabled, so local same-source runtime evidence remains authoritative. No currently generated task-group overload is unreviewed. Repeated wait/new-work semantics, task-executor preference scheduling, arbitrary actor executors, and the escaped-capability boundary stay open."
  },
  {
    "id": "group-escape-project-preflight-boundary",
    "requirementRef": "M4/group-escape-legality",
    "kind": "surface-gap",
    "expectedObservation": "Escaping structured TaskGroup capabilities are rejected through the same generated, target-aware host and project declarations used by real interpreter entry points.",
    "currentObservation": "The pinned upstream TaskGroup capture diagnostic is enforced by production required preflight. Receiver-qualified synthetic callables/properties and fail-closed nominal struct/class/enum declarations preserve member effects plus enclosing attributes; unsupported nominal forms are rejected before module compilation. Target-aware project manifests are not complete."
  },
  {
    "id": "actor-declaration-safety-boundary",
    "requirementRef": "M5/actor-declaration-safety-boundary",
    "kind": "coverage-gap",
    "expectedObservation": "Unsupported actor execution fails closed before valid actor declarations can run with class-like identity, storage, or dispatch semantics.",
    "currentObservation": "Native isolation diagnostics are available through preflight, but valid interpreted actor declarations can still enter the class-like declaration path without actor-owned storage or executor semantics."
  },
  {
    "id": "actor-identity-storage-serial-executor",
    "requirementRef": "M5/actor-identity-storage-and-serial-executor",
    "kind": "coverage-gap",
    "expectedObservation": "Actors have distinct identity, actor-owned storage, serial mailbox/executor queues, isolated entry hops, and bounded lifetime cleanup.",
    "currentObservation": "Only logical MainActor/cooperative-default executor identity and hop restoration are covered; there is no actor-owned storage or serial mailbox."
  },
  {
    "id": "actor-reentrancy-isolated-dispatch",
    "requirementRef": "M5/actor-reentrancy-and-isolated-dispatch",
    "kind": "coverage-gap",
    "expectedObservation": "Actor suspension/resume is reentrant without concurrent isolated execution; isolated parameters, arbitrary global actors, failure paths, and replayable stress are covered.",
    "currentObservation": "There is no actor resume-ownership model, isolated actor dispatch, arbitrary global-actor runtime, or mailbox/reentrancy stress board."
  },
  {
    "id": "async-sequence-continuation-runtime",
    "requirementRef": "M6/protocol-iteration-streams-and-continuations",
    "kind": "coverage-gap",
    "expectedObservation": "Protocol AsyncSequence iteration, streams, checked and unsafe continuations, cancellation, and cleanup have native parity.",
    "currentObservation": "TaskGroup-specific iteration exists, but protocol iteration and continuation ownership are not implemented."
  },
  {
    "id": "generated-concurrency-signatures-and-preflight",
    "requirementRef": "M7/generated-signatures-and-preflight",
    "kind": "surface-gap",
    "expectedObservation": "Every declaration in the generated _Concurrency inventory scope has a reviewed implementation/verification disposition; synthetic host signatures preserve effects and isolation in compiled preflight, and target-aware manifests describe real projects.",
    "currentObservation": "Active-SDK top-level function, Task static/instance, selected-nominal, task-group, and nested group-iterator declaration slices are generated, SDK modules are re-exported canonically, and typed synthetic declarations enter compiled multi-file preflight. The generic selected-nominal walker inventories all nine UnsafeCurrentTask member rows and fails closed if the family or required routes disappear. Both withUnsafeCurrentTask overloads and six core member routes have same-source Swift 6 parity for synchronous/asynchronous detached-task identity, hash consistency, base/effective priority, cancellation, exact rethrow, suspension, and cleanup. Both macOS 26 task-priority-escalation handler declarations now share one generated semantic route; same-source parity covers no registration replay, exact old/new delivery to all active nested handlers, operation-visible donation, exact failure/cancellation, repeated increases, and cleanup. The isolated wrapper accepts only explicit nil and fails closed for omitted or non-nil isolation; arbitrary actor/executor behavior of the public nonisolated(nonsending) spelling remains an explicit known divergence. The generated withTaskExecutorPreference declaration now routes to a dynamic same-task scope for the exact subset with no ambient custom TaskExecutor, explicit nil executor and isolation arguments, and a bare unqualified direct global async operation function carrying a plain explicit nonisolated modifier and no declaration-level executor preference. Same-source Swift 6 parity covers task identity through causal cancellation, native high-priority value, name, task-local and cancellation preservation across yield and scope exit, exact value/error propagation, and cleanup. Native nil inherits an ambient custom preference rather than clearing it; non-nil executors, ambient preference inheritance, omitted or non-nil isolation, nonisolated(nonsending), qualified or parenthesized references, local aliases or function-value conversions, closure-expression operations, and @concurrent or actor-isolated operations fail closed. The generated extractIsolation declaration now routes to synchronous metadata inspection for bare unqualified direct global async function declarations with plain explicit nonisolated provenance, including @concurrent. Same-source Swift 6 parity proves nil without operation invocation; the adapter path is synchronous and non-mutating, while focused tests prove no retained task, scope, group, or scheduler ownership and fail closed for actor identity, implicit nonisolation, synchronous functions, qualified or parenthesized references, local aliases, closure expressions, function-value conversions, member references, and nonisolated(nonsending). Focused runtime tests cover task capability lifetime, priority-handler ownership, task-executor-preference rejection boundaries, and extractIsolation provenance boundaries; hash(into:), escalatePriority(to:), and unownedTaskExecutor retain explicit generated diagnostics. Task aliases, cancellation handlers, immediate tasks, task groups, iterators, and compiler ABI rows retain their authored dispositions. Current accounting is 167 of 171 generated rows reviewed: 51 runtime-supported, 20 diagnosed-unsupported, 69 known divergences, 26 excluded compiler ABI, one deferred, and 4 unreviewed. The remaining four generated source-facing top-level rows and target-aware build manifests remain open."
  },
  {
    "id": "view-owned-async-lifecycle-runtime",
    "requirementRef": "M8/view-owned-async-lifecycle",
    "kind": "coverage-gap",
    "expectedObservation": "Generated .task/.task(id:)/.refreshable exposure uses a reusable lifecycle primitive with view identity, cancellation, and teardown evidence.",
    "currentObservation": "Synchronous host callbacks enter runtime-owned tasks, but view-owned async lifecycle identity and cancellation remain open."
  }
]
