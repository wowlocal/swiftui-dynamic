[
  {
    "id": "remaining-generated-task-group-surface",
    "requirementRef": "M4/remaining-task-group-surface",
    "kind": "surface-gap",
    "expectedObservation": "The active _Concurrency.swiftinterface inventory is generated and every mechanically exposed group member and overload has an explicit implementation/verification disposition with positive evidence or an owned negative/deferral rationale.",
    "currentObservation": "The generated inventory retains every active task-group overload plus all six public TaskGroup.Iterator and ThrowingTaskGroup.Iterator next/cancel rows with effect/isolation metadata. All eight isEmpty/isCancelled rows, all four cancelAll rows, all four canonical addTask(priority:operation:) rows, all four canonical addTaskUnlessCancelled(priority:operation:) rows, both deprecated spawn rows, both deprecated async rows, both deprecated async add rows, both deprecated asyncUnlessCancelled rows, both deprecated spawnUnlessCancelled rows, both makeAsyncIterator rows, and all six iterator rows have authored dispositions. Same-source native parity covers the supported nonisolated canonical add and conditional-add subsets across all four group kinds, conditional aliases returning true with value/error before cancellation and false with an empty tail afterward, plus the shared exactly-once iterator stream, value-semantic copies, terminal state after nil/error/cancel, default next, explicit MainActor next, and cancellation. Unchanged swiftlang coverage independently anchors ordinary TaskGroup.isEmpty/cancelAll/async, addTaskUnlessCancelled rejection after owner cancellation, ordinary spawnUnlessCancelled before/after cancelAll contrasted with unconditional spawn, and ordinary/throwing direct async iteration. Default and explicit-MainActor nonthrowing/throwing waitForAll behavior has native parity plus a pinned swiftlang positive preflight oracle. ThrowingTaskGroup.nextResult has local parity for empty/success/failure/cancellation and unchanged swiftlang recovery coverage. The complete any-Actor rows remain known divergences until arbitrary actor executors exist. Repeated wait/new-work semantics and reviewed dispositions for the remaining overloads stay open."
  },
  {
    "id": "group-escape-project-preflight-boundary",
    "requirementRef": "M4/group-escape-legality",
    "kind": "surface-gap",
    "expectedObservation": "Escaping structured TaskGroup capabilities are rejected through the same generated, target-aware host and project declarations used by real interpreter entry points.",
    "currentObservation": "The pinned upstream TaskGroup capture diagnostic is enforced by production required preflight. Receiver-qualified synthetic callables/properties and fail-closed nominal struct/class/enum declarations preserve member effects plus enclosing attributes; unsupported nominal forms are rejected before module compilation. Target-aware project manifests are not complete."
  },
  {
    "id": "actor-declaration-safety-boundary",
    "requirementRef": "M5/actor-declaration-safety-boundary",
    "kind": "coverage-gap",
    "expectedObservation": "Unsupported actor execution fails closed before valid actor declarations can run with class-like identity, storage, or dispatch semantics.",
    "currentObservation": "Native isolation diagnostics are available through preflight, but valid interpreted actor declarations can still enter the class-like declaration path without actor-owned storage or executor semantics."
  },
  {
    "id": "actor-identity-storage-serial-executor",
    "requirementRef": "M5/actor-identity-storage-and-serial-executor",
    "kind": "coverage-gap",
    "expectedObservation": "Actors have distinct identity, actor-owned storage, serial mailbox/executor queues, isolated entry hops, and bounded lifetime cleanup.",
    "currentObservation": "Only logical MainActor/cooperative-default executor identity and hop restoration are covered; there is no actor-owned storage or serial mailbox."
  },
  {
    "id": "actor-reentrancy-isolated-dispatch",
    "requirementRef": "M5/actor-reentrancy-and-isolated-dispatch",
    "kind": "coverage-gap",
    "expectedObservation": "Actor suspension/resume is reentrant without concurrent isolated execution; isolated parameters, arbitrary global actors, failure paths, and replayable stress are covered.",
    "currentObservation": "There is no actor resume-ownership model, isolated actor dispatch, arbitrary global-actor runtime, or mailbox/reentrancy stress board."
  },
  {
    "id": "async-sequence-continuation-runtime",
    "requirementRef": "M6/protocol-iteration-streams-and-continuations",
    "kind": "coverage-gap",
    "expectedObservation": "Protocol AsyncSequence iteration, streams, checked and unsafe continuations, cancellation, and cleanup have native parity.",
    "currentObservation": "TaskGroup-specific iteration exists, but protocol iteration and continuation ownership are not implemented."
  },
  {
    "id": "generated-concurrency-signatures-and-preflight",
    "requirementRef": "M7/generated-signatures-and-preflight",
    "kind": "surface-gap",
    "expectedObservation": "Every declaration in the generated _Concurrency inventory scope has a reviewed implementation/verification disposition; synthetic host signatures preserve effects and isolation in compiled preflight, and target-aware manifests describe real projects.",
    "currentObservation": "Active-SDK top-level function, Task static/instance, task-group, and nested ordinary/throwing group-iterator declaration slices are generated, SDK modules are re-exported canonically, and typed synthetic top-level functions plus receiver-qualified callables/properties enter compiled multi-file preflight. Async HostProperty reads use a task-bound host suspension and preserve optional/nested/static member access. Empty non-generic synthetic struct/class/enum declarations retain enclosing attributes, embed typed members in their nominal bodies, and reject conflicting or unsupported contracts. All 32 Task instance/static rows and forty-five task-group plus iterator state/cancellation/wait/nextResult/spawn/async/add/canonical-addTask/canonical-addTaskUnlessCancelled/asyncUnlessCancelled/spawnUnlessCancelled/makeAsyncIterator/next/cancel rows have authored dispositions: forty-one runtime-supported rows carry native-parity evidence, eighteen unsupported rows carry executable diagnostics, and eighteen partial overloads are recorded as known divergences. The remaining 79 of 156 generated rows and target-aware build manifests remain open."
  },
  {
    "id": "view-owned-async-lifecycle-runtime",
    "requirementRef": "M8/view-owned-async-lifecycle",
    "kind": "coverage-gap",
    "expectedObservation": "Generated .task/.task(id:)/.refreshable exposure uses a reusable lifecycle primitive with view identity, cancellation, and teardown evidence.",
    "currentObservation": "Synchronous host callbacks enter runtime-owned tasks, but view-owned async lifecycle identity and cancellation remain open."
  }
]
