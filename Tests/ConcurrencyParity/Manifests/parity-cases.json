[
  {
    "id": "async-function-exact",
    "fixture": "Fixtures/async-function-exact.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 1,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parityValue()",
    "interpreterProjection": "string",
    "nativeFact": "Awaiting this async function produces the exact value 'ready'.",
    "notes": "Smoke test for the same-fixture native/interpreter pipeline."
  },
  {
    "id": "async-try-await-conditional",
    "fixture": "Fixtures/async-try-await-conditional.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncTryAwaitConditionalProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A throwing async call remains awaited when its optional result is compared with nil as the condition of a ternary expression.",
    "notes": "The async function yields once and then returns nil. Exact output is nil in all 20 Swift 6.3.3 runs; there is no scheduler-order assertion."
  },
  {
    "id": "host-gateway-suspension",
    "fixture": "Fixtures/host-gateway-suspension.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await hostGatewaySuspensionProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Awaiting the controlled async wrapper suspends the caller until its gate opens, allowing a ready MainActor controller to make progress before the wrapper returns.",
    "notes": "The controller cannot run inline with Task construction. The host wrapper records entry and yields behind an explicit gate; only the barrier-forced trace is asserted."
  },
  {
    "id": "main-actor-task-partial-order",
    "fixture": "Fixtures/main-actor-task-partial-order.swift",
    "mode": "runtime",
    "assertion": "partial-order",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "startTasks()",
    "interpreterProjection": "instance-string-array:events",
    "requiredEvents": ["sync", "first", "second"],
    "precedes": [["sync", "first"], ["sync", "second"]],
    "nativeFact": "A newly created MainActor task does not run inline with its constructor; both tasks complete after the creator reaches a suspension point.",
    "notes": "The relative order of 'first' and 'second' is deliberately not asserted."
  },
  {
    "id": "concurrent-executor-hop",
    "fixture": "Fixtures/concurrent-executor-hop.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await concurrentExecutorHopProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A @concurrent nonisolated async method leaves MainActor for its entry and post-yield continuation, an awaited MainActor method runs on the main executor, and the caller resumes on MainActor afterward.",
    "notes": "Exact output is main|worker:worker:main|worker:worker:main|main in all 20 Swift 6.3.3 runs. The second work call starts in Task.detached; no relative scheduling order is asserted."
  },
  {
    "id": "actor-isolated-entry",
    "fixture": "Fixtures/actor-isolated-entry.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorIsolatedEntryProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A function whose isolated parameter defaults to #isolation receives the current actor when called from an actor-isolated method and nil when called from an explicitly nonisolated method.",
    "notes": "Exact output is actor:none in all 20 Swift 6.3.3 runs. The wrapper observes isolation identity only; no scheduler order or physical thread is asserted."
  },
  {
    "id": "custom-global-actor-isolation",
    "fixture": "Fixtures/custom-global-actor-isolation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await customGlobalActorIsolationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A function annotated with a user-declared global actor executes isolated to that declaration's canonical static shared actor, while an explicitly nonisolated function has no current actor isolation.",
    "notes": "Exact output is same:none in all 20 Swift 6.3.3 runs. Actor identity is compared directly; no physical thread or independent scheduling order is asserted."
  },
  {
    "id": "actor-arbitrary-global-actor-isolation",
    "fixture": "Fixtures/actor-arbitrary-global-actor-isolation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await arbitraryGlobalActorIsolationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Struct- and enum-backed global-actor declarations whose static shared values have separate source-actor types both expose that exact canonical actor through #isolation; a defaulted isolated existential selects and owns the same actor executor.",
    "notes": "Exact output is same:owned:same|same:owned:same in all 20 Swift 6.3.3 runs. The fixture covers both nominal-symbol representations and actor capability identity without asserting physical threads, independent scheduling order, or arbitrary @isolated(any) task-operation executors."
  },
  {
    "id": "actor-initialization",
    "fixture": "Fixtures/actor-initialization.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorInitializationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A synchronous actor initializer is lexically nonisolated, initializes actor-owned stored state without await, and the first externally awaited isolated method executes while owning that actor's executor.",
    "notes": "Exact output is none:owned:5 in all 20 Swift 6.3.3 runs. The fixture proves initialization isolation, retained state, and post-initialization mailbox ownership without asserting physical threads or unrelated message order."
  },
  {
    "id": "enum-case-metadata",
    "fixture": "Fixtures/enum-case-metadata.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await enumCaseMetadataProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Backticked enum case names, labeled associated values, and explicit Double raw values retain their source meaning when an enum value crosses an actor boundary.",
    "notes": "Exact output is authenticated:foodtruck|default|1.0:0.5 in all repetitions. FoodTruck cites these spellings in User, Panel, and BrandHeader.HeaderSize; no scheduler order or physical thread is asserted."
  },
  {
    "id": "extension-metadata",
    "fixture": "Fixtures/extension-metadata.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await extensionMetadataProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A dotted extension can add a retroactive conformance and method, while a matching constrained extension contributes its method when the value crosses an actor boundary.",
    "notes": "Exact output is true:42:21 in all repetitions. FoodTruck cites dotted, conforming, and generic-where extension headers; no nonmatching-constraint, scheduler-order, or physical-thread claim is made."
  },
  {
    "id": "typealias-metadata",
    "fixture": "Fixtures/typealias-metadata.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await typeAliasMetadataProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Active conditional top-level and member type aliases denote the selected concrete type when a value crosses an actor boundary.",
    "notes": "Exact output is mac:42 in all repetitions. Native runs use the pinned macOS target and both executions select the non-watchOS branch. FoodTruck cites conditional private top-level and member aliases in DetailedMapView; no inaccessible inactive-branch, positive-macOS-predicate, scheduler-order, or physical-thread claim is made."
  },
  {
    "id": "deinitializer-metadata",
    "fixture": "Fixtures/deinitializer-metadata.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "deinitializerMetadataProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An ordinary unannotated deinitializer on a MainActor-isolated class remains nonisolated and completes synchronously before execution continues after the final release.",
    "notes": "Exact output is body|none:foodtruck|after in all repetitions. FoodTruck cites the ordinary deinitializer on MainActor-isolated StoreMessagesManager; no physical-thread, scheduler-order, isolated-deinitializer, or custom-global-actor teardown claim is made."
  },
  {
    "id": "conditional-member-metadata",
    "fixture": "Fixtures/conditional-member-metadata.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await conditionalMemberMetadataProbe()",
    "interpreterProjection": "string",
    "nativeFact": "The first active conditional-compilation clause contributes its stored property, method, nested type, extension method, and enum case while declarations from the inactive member clauses do not participate.",
    "notes": "Native macOS and the interpreter's legacy iOS build identity both select the non-watchOS branch and produce foodtruck:7:type:extension:regular:nested. The first probe used String(describing:) for the enum and exposed an unrelated formatting divergence, so the oracle was narrowed before production to a String raw value. The corrected case was green before and after member-plan migration in twenty bounded repetitions; each native shard reported SHA-256 8c1f91f180b8517d68ab0a40b3f0cc2ab73a32db163c2d296b1bc25d9561a937. The same unchanged case remained exact before and after immutable target-resolved program ownership: declaration selection survives the actor hop and is not a new runtime-semantic claim. FoodTruck cites five conditional member regions in StoreSupportView, DonutGalleryGrid, OrdersView, and DetailedMapView; positive-watchOS selection, new member semantics, scheduler order, and physical threads are outside this case."
  },
  {
    "id": "callable-placement-metadata",
    "fixture": "Fixtures/callable-placement-metadata.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await callablePlacementMetadataProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Static and instance methods of a MainActor-isolated nominal inherit that actor while remaining callable through the type and an instance respectively.",
    "notes": "Exact output is type:same:foodtruck|instance:same in all repetitions and remains unchanged after an awaited actor hop and the immutable function-body migration. FoodTruck cites 133 functions including async account/StoreKit bodies, local layout helpers, mutating model operations, modifiers, and static callables; no bodyless invocation, class-func, physical-thread, scheduler-order, or unrelated-overload claim is made."
  },
  {
    "id": "initializer-declaration-metadata",
    "fixture": "Fixtures/initializer-declaration-metadata.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await initializerDeclarationMetadataProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An ordinary initializer of a MainActor-isolated class inherits MainActor, an explicit nonisolated initializer has nil lexical isolation, and failable initializer success or failure survives an awaited actor hop.",
    "notes": "Exact output is isolated:same:foodtruck|nonisolated:none:store|accepted|rejected in all repetitions. FoodTruck cites ordinary actor initializers, nonisolated initializers on MainActor classes, and init?; no async-initializer, custom-global-actor, physical-thread, or scheduler-order claim is made."
  },
  {
    "id": "actor-isolated-deinitializer",
    "fixture": "Fixtures/actor-isolated-deinitializer.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorIsolatedDeinitializerProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An explicit isolated deinitializer on a MainActor-isolated class inherits that executor and runs before the next MainActor statement when final release already occurs there.",
    "notes": "Exact output is deinit in all repetitions. The interpreter's host Instance lifetime is MainActor-owned, so this covers the same already-owned synchronous fast path without claiming source-actor or user-global-actor teardown scheduling."
  },
  {
    "id": "actor-global-actor-deinitializer",
    "fixture": "Fixtures/actor-global-actor-deinitializer.swift",
    "mode": "runtime",
    "assertion": "interpreter-diagnostic",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorGlobalActorDeinitializerProbe()",
    "interpreterProjection": "string",
    "interpreterDiagnosticContains": [
      "global-actor deinitializer",
      "executor-owned teardown"
    ],
    "nativeFact": "A deinitializer explicitly isolated to a user-declared global actor runs before the next statement when final release already owns that actor's executor.",
    "notes": "Native exact output is deinit in all repetitions. This proves only the already-owned synchronous fast path. The interpreter must fail closed because its general source ARC path cannot schedule executor-owned teardown; an enclosing global-actor annotation does not by itself isolate an ordinary deinitializer."
  },
  {
    "id": "actor-task-local-propagation",
    "fixture": "Fixtures/actor-task-local-propagation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorTaskLocalPropagationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A task-local dynamic binding follows the calling task across an actor hop and remains visible after the actor-isolated method suspends and reacquires its executor.",
    "notes": "Exact output is default:owned|bound:owned|bound:owned>bound:owned|default:owned in all 20 Swift 6.3.3 runs. The fixture proves task-local restoration and mailbox ownership without asserting FIFO, physical threads, or independent-message order."
  },
  {
    "id": "actor-queued-message-cancellation",
    "fixture": "Fixtures/actor-queued-message-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorQueuedMessageCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Cancelling a task whose cross-actor call is already waiting behind an occupied actor does not discard that call; after the actor becomes available, the method enters with actor ownership and observes the task's cancellation request.",
    "notes": "Exact output is requested|released|owned:cancelled in all 20 Swift 6.3.3 runs. MainActor run-to-suspension and a condition gate establish cancellation while the call is queued; no FIFO among multiple waiters, physical thread, or automatic throwing behavior is asserted."
  },
  {
    "id": "actor-serial-segment",
    "fixture": "Fixtures/actor-serial-segment.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorSerialSegmentProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A synchronous actor-isolated method executes as one mutually exclusive actor-executor segment, so two concurrent calls each own the actor and both mutations are retained.",
    "notes": "Exact output is owned:owned:2 in all 20 Swift 6.3.3 runs. Both ownership results are identical, so the assertion makes no FIFO, task-start-order, physical-thread, or cross-actor parallelism claim."
  },
  {
    "id": "actor-computed-property",
    "fixture": "Fixtures/actor-computed-property.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorComputedPropertyProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An externally awaited synchronous actor computed-property getter executes under that actor's isolation for the complete accessor segment, while an explicit nonisolated computed getter does not enter the actor.",
    "notes": "Exact output is owned:1|owned:2|unowned|2 in all 20 Swift 6.3.3 runs. Sequential awaits establish both isolated mutations; no FIFO between independent messages, physical-thread, or parallelism claim is made."
  },
  {
    "id": "actor-computed-setter",
    "fixture": "Fixtures/actor-computed-setter.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorComputedSetterProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A synchronous computed setter invoked from an actor-isolated method executes inside that method's actor-owned executor segment and may mutate actor-isolated storage.",
    "notes": "Exact output is owned:7|owned:11 in all 20 Swift 6.3.3 runs. Sequential awaited method calls establish both assignments; no external setter hop, physical-thread behavior, or independent-message order is asserted."
  },
  {
    "id": "actor-subscript-getter",
    "fixture": "Fixtures/actor-subscript-getter.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorSubscriptGetterProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Each externally awaited synchronous actor subscript getter executes inside the receiver actor's executor segment, while an explicitly nonisolated subscript remains on the caller executor.",
    "notes": "Exact output is owned:1|owned:3|unowned|3 in all 20 Swift 6.3.3 runs. Sequential awaits establish both isolated mutations; no FIFO, physical-thread, independent-message ordering, or setter behavior is asserted."
  },
  {
    "id": "actor-subscript-setter",
    "fixture": "Fixtures/actor-subscript-setter.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorSubscriptSetterProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A synchronous actor subscript setter invoked from an actor-isolated method executes inside that method's actor-owned executor segment and may mutate actor-isolated storage.",
    "notes": "Exact output is owned:7|owned:11 in all 20 Swift 6.3.3 runs. Sequential awaited method calls establish both assignments; no external setter hop, physical-thread behavior, or independent-message order is asserted."
  },
  {
    "id": "actor-reentrancy",
    "fixture": "Fixtures/actor-reentrancy.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorReentrancyProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An async actor message owns the actor before suspension, releases it so another message can mutate isolated state, and reacquires the same actor before resuming.",
    "notes": "Exact output is owned:owned:interleaved:owned:3 in all 20 Swift 6.3.3 runs. A controlled gate establishes every asserted edge; no FIFO, task-start order, physical thread, or unrelated scheduling order is asserted."
  },
  {
    "id": "actor-mailbox-stress",
    "fixture": "Fixtures/actor-mailbox-stress.swift",
    "mode": "runtime",
    "assertion": "stress",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorMailboxStressProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Round barriers force multiple actor messages to suspend and re-enter one actor; every resumed segment retains actor ownership, every message mutates exactly once, and all rounds and children drain.",
    "notes": "Eight children execute four barrier rounds with two additional in-actor yields per message. Exact terminal output is 32:4:0:0:8 in all 20 Swift 6.3.3 runs; only commutative counts and ownership invariants are asserted, not child order, FIFO, or physical threads."
  },
  {
    "id": "actor-cross-actor-failure",
    "fixture": "Fixtures/actor-cross-actor-failure.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorCrossActorFailureProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A cross-actor awaited throwing call runs inside the callee actor, releases that actor on failure, and restores the caller actor before catch handling and subsequent cross-actor work.",
    "notes": "Exact output is owned|caught|owned|owned:owned:2|owned in all 20 Swift 6.3.3 runs. The observation proves caller/callee actor-segment ownership and retained target mutations without asserting FIFO, physical threads, or unrelated scheduling order."
  },
  {
    "id": "actor-cross-actor-cancellation",
    "fixture": "Fixtures/actor-cross-actor-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorCrossActorCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A cancelled task reacquires the callee actor after controlled suspension before observing cancellation, then releases that actor and restores its caller actor before catch handling and subsequent cross-actor work.",
    "notes": "Exact output is owned|cancelled|owned|owned:owned:owned:2|owned in all 20 Swift 6.3.3 runs. Explicit gates establish cancellation after suspension and before resume; no FIFO, physical-thread, or unrelated scheduling order is asserted."
  },
  {
    "id": "actor-isolated-parameter",
    "fixture": "Fixtures/actor-isolated-parameter.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorIsolatedParameterProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A synchronous function with an isolated actor parameter executes on that argument's actor executor and may synchronously access and mutate the actor's isolated state.",
    "notes": "Exact output is owned:2|owned:5|5 in all 20 Swift 6.3.3 runs. Sequential external awaits establish both isolated mutations without asserting physical threads or independent-message ordering."
  },
  {
    "id": "actor-isolated-parameter-defaults",
    "fixture": "Fixtures/actor-isolated-parameter-defaults.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorIsolatedParameterDefaultsProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An optional isolated parameter inherits the caller's actor through a default #isolation value, accepts an explicit actor executor, and becomes nonisolated for either explicit nil or a nil default from a nonisolated caller.",
    "notes": "Exact output is owned:actor|owned:actor|unowned:none|unowned:none in all 20 Swift 6.3.3 runs. The four calls establish source-actor inheritance, explicit actor selection, explicit nil, and nonisolated default behavior without asserting physical threads or independent-message ordering."
  },
  {
    "id": "actor-isolated-parameter-mainactor-default",
    "fixture": "Fixtures/actor-isolated-parameter-mainactor-default.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "actorIsolatedParameterMainActorDefaultProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A default #isolation value in a MainActor caller is a non-nil actor isolation and selects MainActor, while an explicit nil value is observably nil without forcing a hop away from the synchronous caller lane.",
    "notes": "Exact output is actor:main|nil:main in all 20 Swift 6.3.3 runs. The result separates isolation-value presence from executor lane and makes no claim about arbitrary global actors, physical worker execution, or unrelated scheduling order."
  },
  {
    "id": "actor-computed-property-failure",
    "fixture": "Fixtures/actor-computed-property-failure.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorComputedPropertyFailureProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A cross-actor awaited throwing computed getter owns the target actor until failure, releases it on throw, and restores the caller actor before catch handling and subsequent target work.",
    "notes": "Exact output is owned|caught|owned|owned:owned:2|owned in all 20 Swift 6.3.3 runs. The observation proves caller/target ownership and retained target mutations without asserting FIFO, physical threads, or unrelated scheduling order."
  },
  {
    "id": "actor-computed-property-cancellation",
    "fixture": "Fixtures/actor-computed-property-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorComputedPropertyCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A cancelled task reacquires a throwing async computed getter's target actor before observing cancellation, then releases the target and restores the caller actor before catch handling and later target work.",
    "notes": "Exact output is owned|cancelled|owned|owned:owned:owned:2|owned in all 20 Swift 6.3.3 runs. An explicit gate establishes cancellation after getter suspension and before resume; no FIFO, physical-thread, or unrelated scheduling order is asserted."
  },
  {
    "id": "actor-computed-property-async-exits",
    "fixture": "Fixtures/actor-computed-property-async-exits.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorComputedPropertyAsyncExitsProbe()",
    "interpreterProjection": "string",
    "nativeFact": "After a controlled suspension, an async-throwing computed getter reacquires its target actor before either returning a value or throwing a source error; each exit releases the target and restores the caller actor before subsequent source work.",
    "notes": "Exact output is owned|value:1|owned|owned:owned:owned:2|owned||owned|caught|owned|owned:owned:owned:2|owned in all 20 Swift 6.3.3 runs. Separate source-owned gates force each suspension and the two runs are awaited sequentially; no FIFO, physical-thread, or unrelated scheduling order is asserted."
  },
  {
    "id": "actor-subscript-cancellation",
    "fixture": "Fixtures/actor-subscript-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorSubscriptCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A cancelled task reacquires a throwing async subscript getter's target actor before observing cancellation, then releases the target and restores the caller actor before catch handling and later target work.",
    "notes": "Exact output is owned|cancelled|owned|owned:owned:owned:2|owned in all 20 Swift 6.3.3 runs. An explicit gate establishes cancellation after getter suspension and before resume; no FIFO, physical-thread, or unrelated scheduling order is asserted."
  },
  {
    "id": "actor-subscript-async-exits",
    "fixture": "Fixtures/actor-subscript-async-exits.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await actorSubscriptAsyncExitsProbe()",
    "interpreterProjection": "string",
    "nativeFact": "After a controlled suspension, an async-throwing subscript getter reacquires its target actor before either returning a value or throwing a source error; each exit releases the target and restores the caller actor before subsequent source work.",
    "notes": "Exact output is owned|value:1|owned|owned:owned:owned:2|owned||owned|caught|owned|owned:owned:owned:2|owned in all 20 Swift 6.3.3 runs. Separate source-owned gates force each suspension and the two runs are awaited sequentially; no FIFO, physical-thread, or unrelated scheduling order is asserted."
  },
  {
    "id": "task-owned-evaluator-context",
    "fixture": "Fixtures/task-owned-evaluator-context.swift",
    "mode": "runtime",
    "assertion": "predicate",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "startTaskContextProbe()",
    "interpreterProjection": "instance-string-array:values",
    "predicate": "same-event-multiset",
    "nativeFact": "One hundred sibling MainActor tasks retain the lexical nested type and local index belonging to their own invocation across a forced yield; only completion order is unspecified.",
    "notes": "Native support and the interpreter harness both provide parityYield(_:) as an identity gateway with one real Task.yield suspension. Every interpreted async context identity is allocated by CooperativeConcurrencyRuntime; completion order and ordered shard digests remain deliberately unspecified."
  },
  {
    "id": "task-context-cancellation",
    "fixture": "Fixtures/task-context-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "startCancellationContextProbe()",
    "interpreterProjection": "instance-task-state-and-string-array:first:events",
    "nativeFact": "Cancelling a task only after it has entered a cancellable suspension completes it with CancellationError, while an interleaved sibling retains its extension's lexical nested type and completes with 'beta'.",
    "notes": "The started barrier forces cancellation-during-suspension. No task-start or sibling-completion order is asserted. This exact fixture also guards task-owned evaluator cleanup after the native-stack cache became pthread-scoped; it makes no physical-thread placement claim."
  },
  {
    "id": "detached-host-context-reentry",
    "fixture": "Fixtures/detached-host-context-reentry.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await detachedHostContextProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Task.detached does not inherit a TaskLocal value, but an explicitly captured value can be rebound around an async callback and remains visible inside that callback.",
    "notes": "The interpreter twin uses a real native detached task and verifies that TaskBoundEvalContext rebinds the exact originating EvaluationTaskContext ID. EvaluationTaskContext identifies its owning CooperativeConcurrencyRuntime without retaining or identifying an Interpreter facade; TaskBoundEvalContext remains the explicit host re-entry owner."
  },
  {
    "id": "async-initializer-context",
    "fixture": "Fixtures/async-initializer-context.swift",
    "mode": "runtime",
    "assertion": "predicate",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "startAsyncInitializerProbe()",
    "interpreterProjection": "instance-string-array:values",
    "predicate": "same-event-multiset",
    "nativeFact": "One hundred extension-declared async initializers retain their own argument and extension lexical nested type across suspension; sibling completion order is unspecified.",
    "notes": "The fixture alternates two nominal types so active initializer and lexical owner state cannot be shared accidentally."
  },
  {
    "id": "async-initializer-outcomes",
    "fixture": "Fixtures/async-initializer-outcomes.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncInitializerOutcomeProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Async initializers preserve success, thrown-error through try?, failable success, and failable nil outcomes across suspension.",
    "notes": "Every branch crosses the same real parityYield suspension before deciding its outcome."
  },
  {
    "id": "task-value-success",
    "fixture": "Fixtures/task-value-success.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskValueSuccessProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Reading await task.value suspends the caller until the task completes and then returns its successful value.",
    "notes": "The child is held behind an explicit gate. The opener is created immediately before the value read and cannot run inline on MainActor, so the value read is the suspension that lets it run."
  },
  {
    "id": "task-value-failure",
    "fixture": "Fixtures/task-value-failure.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskValueFailureProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A throwing task preserves its failure across suspension and try await task.value throws it to the awaiting caller.",
    "notes": "The operation crosses parityYield before throwing a source enum; the probe asserts catchability, not version-specific error text."
  },
  {
    "id": "task-value-multiple-waiters",
    "fixture": "Fixtures/task-value-multiple-waiters.swift",
    "mode": "runtime",
    "assertion": "predicate",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "startMultipleTaskWaiterProbe()",
    "interpreterProjection": "instance-string-array:events",
    "predicate": "same-event-multiset",
    "nativeFact": "Multiple tasks may await the same task value concurrently and every waiter receives the one completed success value.",
    "notes": "Both waiter tasks mark their arrival and then suspend on MainActor before a controller opens the child gate. Relative waiter resume order is unspecified."
  },
  {
    "id": "task-value-waiter-cancellation",
    "fixture": "Fixtures/task-value-waiter-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskValueWaiterCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Cancelling a task while it awaits another unstructured task's value does not end the value wait or cancel the target; the waiter receives the value and remains marked cancelled.",
    "notes": "MainActor isolation guarantees the waiter has entered target.value before the controller can cancel it. The target remains behind a separate gate until after cancellation. Exact output is target-active,waiter-cancelled,handle-cancelled on Swift 6.3.3."
  },
  {
    "id": "task-cancellation-before-start",
    "fixture": "Fixtures/task-cancellation-before-start.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskCancellationBeforeStartProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An unstructured task cancelled before it can start still enters its operation, observes cancellation, may return a normal value, and remains marked cancelled.",
    "notes": "MainActor isolation prevents the inherited task from starting before the creator cancels it and suspends on value. Exact output is body-ran,body-cancelled,handle-cancelled on Swift 6.3.3."
  },
  {
    "id": "task-result",
    "fixture": "Fixtures/task-result.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskResultProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Reading await task.result suspends until completion and returns success or failure as a Result value without throwing the task failure; get() rethrows the stored failure.",
    "notes": "The success is held behind an explicit gate; the failure crosses parityYield, is first inspected by case without try or catch, and is then observed through get()."
  },
  {
    "id": "task-completed-handle-reads",
    "fixture": "Fixtures/task-completed-handle-reads.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskCompletedHandleReadProbe()",
    "interpreterProjection": "string",
    "nativeFact": "After a task completes, repeated value and result reads reproduce its stored success or failure; Result.get() continues to rethrow the stored failure.",
    "notes": "The first value/result read establishes completion and all later reads target the completed handle. Exact output is value,value,success:value,failure,failure,get-caught on Swift 6.3.3."
  },
  {
    "id": "task-result-cancellation",
    "fixture": "Fixtures/task-result-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskResultCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A throwing task cancelled after entering a cancellable suspension completes await task.result as a failure Result instead of throwing from the property read.",
    "notes": "An explicit started barrier distinguishes cancellation during suspension from cancellation before task start; this fixture asserts only the failure case shape, not error text."
  },
  {
    "id": "task-detached-value",
    "fixture": "Fixtures/task-detached-value.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskDetachedValueProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A Task.detached operation may suspend and its handle value awaits completion and returns the operation result.",
    "notes": "This first detached-source fixture proves construction, suspension, and value flow. Task-local and executor inheritance require separate probes; priority is covered independently."
  },
  {
    "id": "task-priority-inheritance",
    "fixture": "Fixtures/task-priority-inheritance.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskPriorityInheritanceProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An explicit utility priority is visible in its task and inherited by an unstructured Task created inside it; a detached task without an explicit priority starts at medium priority.",
    "notes": "No handle is awaited by higher-priority code before the three raw values are captured, so priority escalation cannot contaminate the inheritance observation. Exact output is 17,17,21 on the active Swift 6.3.3 toolchain."
  },
  {
    "id": "task-priority-escalation",
    "fixture": "Fixtures/task-priority-escalation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 15,
    "interpreterEntry": "await taskPriorityEscalationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A running background-priority task is escalated to high priority when a high-priority task awaits its value, and a subsequently created unstructured child inherits that effective priority.",
    "notes": "The low task records 9 before the high waiter exists. On MainActor, the high task opens the low gate and immediately awaits the low handle with no intervening suspension, so the second read and child creation occur only after waiter registration. Exact output is 9,25,25 on Swift 6.3.3. The process remains hard-bounded at 15 seconds: two source-bound closing receipts showed the earlier 5-second native-binary deadline was unstable under the intended concurrent compiler/parity load, while two simultaneous four-worker focused boards completed all 20 native/interpreter repetitions with the same digest. The semantic assertion and repetition count are unchanged."
  },
  {
    "id": "task-priority-transitive-escalation",
    "fixture": "Fixtures/task-priority-transitive-escalation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 15,
    "interpreterEntry": "await taskPriorityTransitiveProbe()",
    "interpreterProjection": "string",
    "nativeFact": "High-priority donation propagates through a chain when the awaited task is itself awaiting another task.",
    "notes": "MainActor barriers ensure the utility middle task is already awaiting the background bottom task before the high task opens the bottom gate and immediately awaits the middle task. Exact output is 9,25,25 on Swift 6.3.3. The process remains hard-bounded at 15 seconds: two source-bound closing receipts showed the earlier 5-second native-binary deadline was unstable under the intended concurrent compiler/parity load, while two simultaneous four-worker focused boards completed all 20 native/interpreter repetitions with the same digest. The semantic assertion and repetition count are unchanged."
  },
  {
    "id": "task-local-declaration",
    "fixture": "Fixtures/task-local-declaration.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskLocalDeclarationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Distinct source @TaskLocal declarations with the same member name retain separate identities; projected withValue is dynamically scoped across suspension, inherited by Task, absent from Task.detached, and restored afterward.",
    "notes": "Every task is awaited, so the exact assertion depends only on task-local scope rather than scheduling order. The two defaults and bindings make textual-name aliasing observable, and both synchronous and suspending withValue overloads are exercised. Exact output is primary-default|secondary-default;primary-bound|secondary-default;primary-bound|secondary-sync;primary-bound|secondary-default;primary-default|secondary-default;primary-bound|secondary-bound;primary-bound|secondary-default;primary-default|secondary-default on Swift 6.3.3."
  },
  {
    "id": "task-local-inheritance",
    "fixture": "Fixtures/task-local-inheritance.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskLocalInheritanceProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A scoped task-local binding is inherited by an unstructured Task, not inherited by Task.detached, and restored after a nested binding exits.",
    "notes": "The same parityWithTaskLocalValue/parityReadTaskLocal declarations are supplied by native and interpreter support. The nested binding crosses a real parityYield suspension. Exact output is parent,parent:child:parent,default."
  },
  {
    "id": "task-local-unwind",
    "fixture": "Fixtures/task-local-unwind.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskLocalUnwindProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A task-local scoped binding is removed when its operation throws or is cancelled, so a catch outside the scope observes the inherited parent value.",
    "notes": "Both paths use the same source fixture and task-local support declarations. The throwing scope crosses parityYield; the cancellation path uses an explicit wait-started barrier before cancel(). Exact output is parent,parent."
  },
  {
    "id": "task-local-implicit-optional-default",
    "fixture": "Fixtures/task-local-implicit-optional-default.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskLocalImplicitOptionalDefaultProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An optional @TaskLocal static variable may omit its initializer; its implicit default is nil and scoped bindings restore that nil value.",
    "notes": "The same source declaration and withValue scope run on both sides and cross parityYield. Exact output is nil,bound,nil."
  },
  {
    "id": "unstructured-top-level-lifetime",
    "fixture": "Fixtures/unstructured-top-level-lifetime.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "startUnstructuredLifetimeProbe()",
    "interpreterProjection": "instance-string-array:events",
    "nativeFact": "An unstructured Task created by a function may continue after that function returns; it is not a structured child joined at lexical scope exit.",
    "notes": "The child waits behind a gate, the creator records returned and exits, and a separate controller opens the gate. The interpreter parity runner uses its explicit drainOwnedTasks host policy."
  },
  {
    "id": "task-handle-deallocation",
    "fixture": "Fixtures/task-handle-deallocation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskHandleDeallocationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Discarding the last source-level handle to an unstructured task neither cancels the task nor ends its operation; the task remains alive independently until completion.",
    "notes": "The Task result is assigned directly to _, so no source handle survives creation. MainActor gates prove that the task starts, remains active after the drop, and finishes only after explicit release. Exact output is completed,active on Swift 6.3.3. The interpreter's cooperative runtime strongly owns scheduled handles until autonomous completion or session-policy release."
  },
  {
    "id": "task-cancellation-caught",
    "fixture": "Fixtures/task-cancellation-caught.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskCancellationCaughtProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Cancellation is a cooperative request: a task may catch CancellationError from a cancellable suspension and complete successfully while its handle remains marked cancelled.",
    "notes": "The explicit started barrier forces cancellation during suspension. The exact result is success:caught,cancelled; cancellation request state and terminal outcome are asserted separately."
  },
  {
    "id": "unstructured-cancellation-isolation",
    "fixture": "Fixtures/unstructured-cancellation-isolation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await unstructuredCancellationIsolationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Cancelling an unstructured task does not propagate cancellation to another unstructured Task created inside it.",
    "notes": "The parent reaches a cancellable suspension only after storing the child handle. The child remains behind a separate gate until after parent cancellation; exact output is cancelled,child."
  },
  {
    "id": "task-sleep-cancellation",
    "fixture": "Fixtures/task-sleep-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskSleepCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Task.sleep suspends without blocking MainActor, and cancelling the sleeping task resumes it by throwing CancellationError.",
    "notes": "A separate async helper waits for the explicit started marker before cancellation. The 30-second deadline cannot expire within the five-second process bound; exact output is started,cancelled."
  },
  {
    "id": "task-yield-progress",
    "fixture": "Fixtures/task-yield-progress.swift",
    "mode": "runtime",
    "assertion": "stress",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskYieldProgressProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A MainActor task can repeatedly yield its executor until a ready sibling task makes observable progress.",
    "notes": "Task creation cannot run the worker inline, so progress requires a suspension. This completion-only liveness probe asserts neither a yield count nor relative scheduler order; every bounded native run returns completed."
  },
  {
    "id": "task-cancellation-handler-active",
    "fixture": "Fixtures/task-cancellation-handler-active.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskCancellationHandlerActiveProbe()",
    "interpreterProjection": "string",
    "nativeFact": "The modern operation:onCancel:isolation: and deprecated handler:operation: overloads both run an active cancellation handler before cancel() returns, only once for repeated cancellation requests, and in the cancelling task's dynamic context.",
    "notes": "A controlled suspension proves that the deprecated handler is registered before cancellation; MainActor serialization provides the equivalent modern boundary. The uncancelled controller makes Task.isCancelled false inside each handler. Strict Swift 6 SIL selects both exact overloads. Exact output is deprecated[0,1,1,false,done]|modern[0,1,1,false,true,done] in all 20 Apple Swift 6.3.3 runs; no ready-task scheduler order is asserted."
  },
  {
    "id": "task-cancellation-handler-pre-cancelled",
    "fixture": "Fixtures/task-cancellation-handler-pre-cancelled.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await preCancelledHandlerProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Registering a cancellation handler in an already-cancelled task invokes it immediately and before the operation begins.",
    "notes": "MainActor prevents the task from starting before two cancellation requests. Registration then occurs on that cancelled task, so Task.isCancelled is true in both onCancel and operation; exact output is 1,true,true,operation-cancelled on Swift 6.3.3."
  },
  {
    "id": "task-cancellation-handler-nested",
    "fixture": "Fixtures/task-cancellation-handler-nested.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await nestedCancellationHandlerProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Two nested active cancellation handlers run once in inner-to-outer order before the cancelled operation resumes.",
    "notes": "A MainActor started barrier makes both registrations active before cancellation and keeps the worker from resuming during cancel(). Exact output is inner,outer,operation on Swift 6.3.3; no ordering between independent ready tasks is asserted."
  },
  {
    "id": "task-cancellation-handler-scope-exit",
    "fixture": "Fixtures/task-cancellation-handler-scope-exit.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskCancellationHandlerScopeExitProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A cancellation handler is no longer active after its operation returns or throws, so later cancellation does not invoke it.",
    "notes": "MainActor exit barriers place each cancel() strictly after the corresponding handler scope unwinds. Exact output is normal-operation,normal-exit,normal-cancelled,throwing-operation,throwing-exit,throwing-cancelled on Swift 6.3.3; handler event names are deliberately absent."
  },
  {
    "id": "async-let-value",
    "fixture": "Fixtures/async-let-value.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetValueProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An async-let initializer starts as a structured child before the parent reads the binding, and awaiting that binding suspends until the child produces its value.",
    "notes": "The child marks entry and waits behind an explicit gate. The parent cannot open that gate until the child has started, and it reads the binding only after opening it. Exact output is child-start,parent-open,child-end,value; no unconstrained ready-task order is asserted."
  },
  {
    "id": "async-let-top-level",
    "fixture": "Fixtures/async-let-top-level.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "nativeTopLevel": true,
    "interpreterEntry": "",
    "interpreterProjection": "string",
    "nativeFact": "Top-level async code supplies an implicit structured scope, so two async-let bindings are legal and awaiting both produces their exact values.",
    "notes": "The native side compiles this same source as a real top-level executable instead of adding the ordinary @main wrapper. Exact output is 42; the result dependency is asserted, while child start/finish order, simultaneous execution, and physical threads are deliberately unspecified."
  },
  {
    "id": "async-let-tuple-pattern",
    "fixture": "Fixtures/async-let-tuple-pattern.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetTuplePatternProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A tuple-pattern async let creates one initializer child whose stored tuple outcome supplies each awaited element binding.",
    "notes": "The single initializer waits behind an explicit gate and returns (left, right). Exact output is child-start,parent-open,child-end,left,right in 20 Swift 6.3.3 runs; no unconstrained ready-task order is asserted."
  },
  {
    "id": "async-let-multiple-bindings",
    "fixture": "Fixtures/async-let-multiple-bindings.swift",
    "mode": "runtime",
    "assertion": "partial-order",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetMultipleBindingsProbe()",
    "interpreterProjection": "string",
    "requiredEvents": ["first-start", "second-start", "parent-open", "first-end", "second-end", "first-value:one", "second-value:two"],
    "precedes": [["first-start", "parent-open"], ["second-start", "parent-open"], ["parent-open", "first-end"], ["parent-open", "second-end"], ["first-end", "first-value:one"], ["second-end", "second-value:two"], ["first-value:one", "second-value:two"]],
    "nativeFact": "Every initializer in one multi-binding async-let declaration starts as a distinct child before either binding is read; each awaited name receives its own child result.",
    "notes": "Both children increment a MainActor start count before waiting on one shared gate, so parent-open cannot occur until both are active. Swift 6.3.3 happened to produce one total order in 20 runs, but only the barrier, result-dependency, and parent program-order edges are asserted."
  },
  {
    "id": "async-let-separate-declaration-cleanup-order",
    "fixture": "Fixtures/async-let-separate-declaration-cleanup-order.swift",
    "mode": "runtime",
    "assertion": "partial-order",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetSeparateDeclarationCleanupOrderProbe()",
    "interpreterProjection": "string",
    "requiredEvents": ["first-start", "second-start", "scope-exit", "second-cancelled", "first-cancelled", "after-scope"],
    "precedes": [["first-start", "scope-exit"], ["second-start", "scope-exit"], ["scope-exit", "second-cancelled"], ["second-cancelled", "first-cancelled"], ["first-cancelled", "after-scope"]],
    "nativeFact": "Separate async-let declarations unwind in reverse registration order: the later child's cancellation and join complete before teardown of the earlier child begins.",
    "notes": "Both children enter 30-second cancellable suspensions before scope-exit. Swift 6.3.3 produced one total trace in 20 runs, but the fixture asserts only entry before exit, later-declaration cleanup before earlier-declaration cleanup, and full cleanup before after-scope."
  },
  {
    "id": "async-let-parent-cancellation-scope-exit",
    "fixture": "Fixtures/async-let-parent-cancellation-scope-exit.swift",
    "mode": "runtime",
    "assertion": "partial-order",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetParentCancellationScopeExitProbe()",
    "interpreterProjection": "string",
    "requiredEvents": ["child-start", "parent-wait", "cancel-issued", "child-cancel-requested", "child-observed-cancellation", "child-finished", "parent-observed-cancellation", "owner-exit", "parent-finished"],
    "precedes": [["child-start", "parent-wait"], ["parent-wait", "cancel-issued"], ["cancel-issued", "child-cancel-requested"], ["child-cancel-requested", "parent-observed-cancellation"], ["child-cancel-requested", "child-observed-cancellation"], ["child-observed-cancellation", "child-finished"], ["child-finished", "parent-finished"], ["parent-observed-cancellation", "owner-exit"], ["owner-exit", "parent-finished"]],
    "nativeFact": "Cancelling an owner propagates the request to its unread async-let child; after source catches cancellation, leaving the owner scope still joins that child before the parent task value completes.",
    "notes": "The child and parent are both inside 30-second cancellable suspensions before cancel-issued. Across 100 Swift 6.3.3 runs, child and parent cancellation observation had two relative orders, so only propagation, per-task program order, and the final structured join are asserted."
  },
  {
    "id": "async-let-created-after-owner-cancellation",
    "fixture": "Fixtures/async-let-created-after-owner-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetCreatedAfterOwnerCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An async-let child created after its owner is already cancelled starts with the owner's structured cancellation request.",
    "notes": "The MainActor owner publishes entry, then yields until its handle is cancelled before declaring the child. Exact output is owner-cancelled:child-cancelled in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "async-let-scope-exit",
    "fixture": "Fixtures/async-let-scope-exit.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetScopeExitProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Leaving a lexical scope without reading an async-let binding cancels the child and waits for that child to finish before execution continues after the scope.",
    "notes": "The child first enters a 30-second cancellable suspension, so natural completion is impossible within the process bound. The parent records scope-exit before returning; exact output child-start,scope-exit,child-cancelled,after-scope proves both implicit cancellation and the scope-exit join."
  },
  {
    "id": "async-let-early-return",
    "fixture": "Fixtures/async-let-early-return.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetEarlyReturnProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An early return cancels an unconsumed async-let child and joins it before the caller receives the returned value.",
    "notes": "The child is already inside a 30-second cancellable suspension when the function records early-return. Exact output is child-start,early-return,child-cancelled,returned,after-return in 20 Swift 6.3.3 runs; the caller-side returned event therefore proves cleanup completed first."
  },
  {
    "id": "async-let-throwing-scope-exit",
    "fixture": "Fixtures/async-let-throwing-scope-exit.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetThrowingScopeExitProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Throwing out of a scope cancels and joins an unconsumed async-let child before the owner error reaches the caller's catch.",
    "notes": "The nonthrowing child catches cancellation inside a 30-second suspension, so it cannot replace the owner's error. Exact output is child-start,scope-throw,child-cancelled,caught,after-catch in 20 Swift 6.3.3 runs."
  },
  {
    "id": "async-let-defer-before-declaration",
    "fixture": "Fixtures/async-let-defer-before-declaration.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetDeferBeforeDeclarationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Lexical cleanups unwind in reverse registration order: when defer is registered before an async-let child, child cancellation and join complete before that defer executes.",
    "notes": "The child is already inside a 30-second cancellable suspension at scope exit. Exact output is child-start,scope-exit,child-cancelled,defer,after-scope in 20 Swift 6.3.3 runs, proving the relative cleanup order rather than a scheduler accident."
  },
  {
    "id": "async-let-defer-after-declaration",
    "fixture": "Fixtures/async-let-defer-after-declaration.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetDeferAfterDeclarationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Lexical cleanups unwind in reverse registration order: when defer is registered after an async-let child, that defer executes before child cancellation and join.",
    "notes": "The child is already inside a 30-second cancellable suspension at scope exit. Exact output is child-start,scope-exit,defer,child-cancelled,after-scope in 20 Swift 6.3.3 runs, proving the reverse relative order without constraining unrelated scheduling."
  },
  {
    "id": "async-let-throwing-defer-order",
    "fixture": "Fixtures/async-let-throwing-defer-order.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetThrowingDeferOrderProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A throwing scope unwinds defer and async-let cleanup in reverse lexical registration order, while joining the cancelled child before propagating the owner error.",
    "notes": "Both registration orders are exercised after the child enters a 30-second cancellable suspension. Exact output is child-start,scope-throw,child-cancelled,defer,caught|child-start,scope-throw,defer,child-cancelled,caught in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "async-let-early-return-defer-order",
    "fixture": "Fixtures/async-let-early-return-defer-order.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetEarlyReturnDeferOrderProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An early return unwinds defer and async-let cleanup in reverse lexical registration order, joining the cancelled child before the caller receives the returned value.",
    "notes": "Both registration orders are exercised after the child enters a 30-second cancellable suspension. Exact output is child-start,early-return,child-cancelled,defer,returned|child-start,early-return,defer,child-cancelled,returned in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "async-let-cancellation-defer-order",
    "fixture": "Fixtures/async-let-cancellation-defer-order.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetCancellationDeferOrderProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Cancelling an owner preserves reverse lexical registration order between defer and unread async-let cleanup, joins the child before returning the owner task value, and leaves cancellation observable on both owner and child.",
    "notes": "A MainActor controller releases the cancelled child only after scope unwind begins, so the asserted cleanup order cannot come from cancellation wake-up order. Exact output is scope-exit,child-complete,defer,returned:cancelled|scope-exit,defer,child-complete,returned:cancelled in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "async-let-value-failure",
    "fixture": "Fixtures/async-let-value-failure.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetValueFailureProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Awaiting a throwing async-let binding waits for the child outcome and propagates its failure into the parent's do/catch.",
    "notes": "The child crosses an explicit yield before throwing the fixture's only error. Exact output is caught in 20 Swift 6.3.3 runs; no ready-task ordering is asserted."
  },
  {
    "id": "async-let-value-cancellation",
    "fixture": "Fixtures/async-let-value-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncLetValueCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Cancelling a parent suspended on an async-let value propagates cancellation to the structured child; the child's cancellation completes before the parent receives CancellationError, which the parent may catch and then return successfully.",
    "notes": "A MainActor flag is set immediately before the parent reaches the incomplete value wait, while the child is held in a cancellable 30-second suspension. Exact output is child-start,parent-await,child-cancelled,parent-caught,parent-finished in 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-group-wait-for-all",
    "fixture": "Fixtures/task-group-wait-for-all.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupWaitForAllProbe()",
    "interpreterProjection": "string",
    "nativeFact": "withTaskGroup creates structured group children, and waitForAll does not return until every added child has completed.",
    "notes": "The child marks entry and waits behind an explicit gate. Exact output is child-start,parent-open,child-end,group-finished,after-scope in 20 Swift 6.3.3 runs; every edge is forced by the gate, waitForAll, or parent program order."
  },
  {
    "id": "task-group-implicit-scope-exit",
    "fixture": "Fixtures/task-group-implicit-scope-exit.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupImplicitScopeExitProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Returning normally from a withTaskGroup body implicitly waits for outstanding children without cancelling them before the outer scope continues.",
    "notes": "The child starts behind an explicit gate; the body records body-return and opens the gate immediately before returning without waitForAll. Exact output is child-start,body-return,child-end,after-scope in 20 Swift 6.3.3 runs, proving normal completion and the implicit join."
  },
  {
    "id": "task-group-defer-cancel-cleanup-order",
    "fixture": "Fixtures/task-group-defer-cancel-cleanup-order.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupDeferCancelCleanupOrderProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A defer registered inside a withTaskGroup body runs before the group's implicit join; cancelAll from that defer requests cancellation of an already-started child, and the outer scope resumes only after the child completes.",
    "notes": "A MainActor flag proves the child entered its cancellable suspension before the body returns. Exact output is child-start,body-return,defer-cancel,child-cancelled,after-scope in all 20 Swift 6.3.3 runs; every edge follows from program order, defer execution, cancelAll, or structured join."
  },
  {
    "id": "task-group-throwing-defer-cleanup-order",
    "fixture": "Fixtures/task-group-throwing-defer-cleanup-order.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingDeferCleanupOrderProbe()",
    "interpreterProjection": "string",
    "nativeFact": "When a withThrowingTaskGroup body throws, its defer runs before exceptional group cleanup cancels and joins an outstanding child; the outer catch runs only after both cleanup phases and receives the original body error.",
    "notes": "A MainActor flag proves the child entered its cancellable suspension before body-throw. Exact output is child-start,body-throw,defer,child-cancelled,caught-body in all 20 Swift 6.3.3 runs; the test does not assert any unrelated ready-task order."
  },
  {
    "id": "task-group-owner-cancellation-defer-cleanup-order",
    "fixture": "Fixtures/task-group-owner-cancellation-defer-cleanup-order.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupOwnerCancellationDeferCleanupOrderProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An owner-cancelled task still runs the withTaskGroup body's defer before implicit join, waits for its cancelled structured child to complete, and only then publishes its successful value while its handle remains cancelled.",
    "notes": "MainActor flags prove owner and child entry before cancellation; the child cannot record completion until the body defer releases it. Exact output is child-start,scope-exit,defer,child-cancelled,returned:cancelled in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-group-child-nested-group",
    "fixture": "Fixtures/task-group-child-nested-group.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupChildNestedGroupProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A task-group child may create and own a nested task group; the inner grandchild completes and is joined before the child publishes its result, which the outer group then joins before returning.",
    "notes": "One child exists at each level. A shared gate places the inner grandchild before inner-open, and both awaited next() calls force the remaining edges. Exact output is grandchild-start,inner-open,grandchild-end,inner-finished,outer-finished:value in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-group-child-unstructured-task",
    "fixture": "Fixtures/task-group-child-unstructured-task.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupChildUnstructuredTaskProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An unstructured Task created by a task-group child remains outside the group's structured ownership: the child and group scope may complete while that task is still active, and group cleanup neither waits for nor cancels it.",
    "notes": "A MainActor gate holds the unstructured task after it starts. The group child returns its handle, both group joins complete, and only then does the owner open the gate and await the handle. Exact output is group-child-start,task-start,group-child-return,group-finished,after-scope,task-active:active:value in all 20 Swift 6.3.3 runs; every edge is barrier-, join-, or program-order-forced."
  },
  {
    "id": "task-group-bounded-tree",
    "fixture": "Fixtures/task-group-bounded-tree.swift",
    "mode": "runtime",
    "assertion": "stress",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupBoundedTreeProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Every recursively nested withTaskGroup scope joins all of its children before returning, so a bounded tree has a scheduler-independent node count.",
    "notes": "Depth/fanout shapes (0,4), (1,4), and (3,3) produce 1, 5, and 40 nodes. The deepest shape creates 39 structured children across 13 nested groups per execution; summation consumes every result and makes no child-completion-order claim. Exact terminal output is 1,5,40 in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-group-cancellation-storm",
    "fixture": "Fixtures/task-group-cancellation-storm.swift",
    "mode": "runtime",
    "assertion": "stress",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupCancellationStormProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Repeated TaskGroup.cancelAll requests cancel every already-started child, group scope exit drains all child outcomes, and group cancellation does not cancel the owner task.",
    "notes": "All 32 children enter a 30-second cancellable sleep before four cancelAll requests. A commutative completion count and cancellation-observation sum avoid sibling-order claims. Exact terminal output is 32:32:cancelled:owner-active in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-executor-composition",
    "fixture": "Fixtures/task-executor-composition.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskExecutorCompositionProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An ordinary Task created on MainActor inherits MainActor, while its async-let and nonisolated task-group children start on the cooperative executor; an ordinary Task created by that group child inherits the cooperative executor.",
    "notes": "The owner awaits its Task, async-let value, one group result, and the nested Task value, so no independent ready-task order is asserted. Exact output is main|main:worker:worker:worker in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-group-cancel-all",
    "fixture": "Fixtures/task-group-cancel-all.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupCancelAllProbe()",
    "interpreterProjection": "string",
    "nativeFact": "TaskGroup.cancelAll requests cancellation for outstanding children, while waitForAll and group scope exit still join each child before outer execution continues.",
    "notes": "The child has entered a 30-second cancellable suspension before cancel-all. Exact output is child-start,cancel-all,child-cancelled,child-finished,group-finished,after-scope in 20 Swift 6.3.3 runs, separating the cancellation request from completion and join."
  },
  {
    "id": "task-group-next-completion-order",
    "fixture": "Fixtures/task-group-next-completion-order.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupNextCompletionOrderProbe()",
    "interpreterProjection": "string",
    "nativeFact": "TaskGroup.next returns child results in completion order rather than task insertion order, consumes each result once, and returns nil after the group is drained.",
    "notes": "The first-added child is held in a confirmed cancellable suspension before the second child is added. Exact output is fast:slow:empty in 20 Swift 6.3.3 runs; the first next consumes the later-added fast result, cancelAll releases slow for the second next, and a third next observes no remaining result."
  },
  {
    "id": "task-group-next-overloads",
    "fixture": "Fixtures/task-group-next-overloads.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupNextOverloadProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Protocol witnesses select both @_disfavoredOverload zero-argument TaskGroup.next ABI entries, while direct zero-argument source selects the modern defaulted-isolation overload; all four preserve the shared completion, failure, cancellation, and drain semantics.",
    "notes": "Strict Swift 6 SIL identifies the legacy protocol witnesses separately from direct next(isolation:) calls. Nonisolated calls cover explicit nil, while explicit MainActor calls include a cancellation-driven suspension and resumption. Exact output is deterministic in 20 native and interpreted process repetitions; arbitrary source-defined actor executors remain outside this claim."
  },
  {
    "id": "task-group-iteration",
    "fixture": "Fixtures/task-group-iteration.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupIterationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "for await iteration over a nonthrowing TaskGroup yields every child result exactly once and consumes the group, so next() returns nil afterward.",
    "notes": "Three children return 1, 2, and 3; exact output is 3:6:empty in all 20 Swift 6.3.3 runs. Count and a commutative sum avoid asserting completion order."
  },
  {
    "id": "task-group-throwing-iteration",
    "fixture": "Fixtures/task-group-throwing-iteration.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingIterationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "for try await iteration over a ThrowingTaskGroup yields every successful child result exactly once and consumes the group, so a later next() returns nil.",
    "notes": "Three successful children return 1, 2, and 3; exact output is 3:6:empty in all 20 Swift 6.3.3 runs. Count and a commutative sum avoid asserting completion order; failure, cancellation, and early exit have dedicated committed cases."
  },
  {
    "id": "task-group-throwing-iteration-failure",
    "fixture": "Fixtures/task-group-throwing-iteration-failure.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await throwingTaskGroupIterationFailureProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A failed child read by for try await rethrows its source error, then exceptional throwing-group exit cancels and joins an outstanding sibling before the outer catch runs.",
    "notes": "The failing child waits until its sibling is inside a 30-second cancellable sleep. Exact output is sibling-start,child-failed,sibling-cancelled,caught-child in all 20 Swift 6.3.3 runs; the barrier, error propagation, and structured join establish every edge."
  },
  {
    "id": "task-group-throwing-iteration-cancellation",
    "fixture": "Fixtures/task-group-throwing-iteration-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await throwingTaskGroupIterationCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A cancelled child consumed by for try await throws CancellationError without cancelling the owner, and throwing-group scope exit joins every sibling before the outer catch runs.",
    "notes": "An explicit started barrier places one sibling inside a cancellable sleep before cancelAll and the cancelled child are introduced. Exact output is cancellation:owner-active:joined in all 20 Swift 6.3.3 runs; completion order between the two children is deliberately not asserted."
  },
  {
    "id": "task-group-throwing-iteration-early-exit",
    "fixture": "Fixtures/task-group-throwing-iteration-early-exit.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await throwingTaskGroupIterationEarlyExitProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Breaking out of for try await does not cancel the remaining throwing-group child; normal group scope exit joins it and leaves the owner task active.",
    "notes": "A started barrier makes the first yielded value deterministic while a sibling waits on an explicit release. Exact output is first:active:owner-active:joined in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-group-wait-for-all-consumes-results",
    "fixture": "Fixtures/task-group-wait-for-all-consumes-results.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupWaitForAllConsumesResultsProbe()",
    "interpreterProjection": "string",
    "nativeFact": "TaskGroup.waitForAll consumes every remaining child result, including after next has already consumed an earlier completion, so a subsequent next returns nil.",
    "notes": "A slow first-added child is suspended while next consumes a later-added fast child. cancelAll releases slow, waitForAll drains that remaining result, and the final next produces empty. Exact output is fast:empty in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-group-add-after-cancel-all",
    "fixture": "Fixtures/task-group-add-after-cancel-all.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupAddAfterCancelAllProbe()",
    "interpreterProjection": "string",
    "nativeFact": "TaskGroup.addTask remains allowed after cancelAll, but the newly created child starts with cancellation already requested.",
    "notes": "The group is cancelled before addTask. The child checks Task.isCancelled as its first source operation and returns cancelled exactly in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-group-is-cancelled",
    "fixture": "Fixtures/task-group-is-cancelled.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupIsCancelledProbe()",
    "interpreterProjection": "string",
    "nativeFact": "TaskGroup.isCancelled is false before cancelAll and true immediately after cancelAll returns.",
    "notes": "The same group is read on both sides of cancelAll with no child work. Exact output is active:cancelled in all 20 Swift 6.3.3 runs, so no scheduler order is involved."
  },
  {
    "id": "task-group-add-unless-cancelled",
    "fixture": "Fixtures/task-group-add-unless-cancelled.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupAddUnlessCancelledProbe()",
    "interpreterProjection": "string",
    "nativeFact": "The canonical addTaskUnlessCancelled overload returns true and adds work while active, then returns false without adding work after cancelAll on TaskGroup, ThrowingTaskGroup, DiscardingTaskGroup, and ThrowingDiscardingTaskGroup.",
    "notes": "Every group kind passes .high without asserting effective priority, accepts and drains active work, then rejects work after cancelAll and remains empty. The throwing result group also projects an exact child error. All 20 strict Swift 6.3.3 native and interpreted runs produce the same four-part exact output."
  },
  {
    "id": "task-group-owner-cancellation",
    "fixture": "Fixtures/task-group-owner-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupOwnerCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Cancelling a task that owns an active TaskGroup makes both the owner and group cancelled, causes addTaskUnlessCancelled to skip, and makes a later ordinary addTask child start cancelled.",
    "notes": "The owner publishes readiness inside the group, an outer task cancels its handle, and the owner proceeds only after Task.isCancelled becomes true. Exact output is cancelled:cancelled:skipped:child-cancelled in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-group-created-after-owner-cancellation",
    "fixture": "Fixtures/task-group-created-after-owner-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupCreatedAfterOwnerCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A TaskGroup created only after its owner has observed cancellation starts cancelled, causes addTaskUnlessCancelled to skip, and makes an ordinary child start cancelled.",
    "notes": "The owner publishes readiness and waits until Task.isCancelled before entering withTaskGroup. Exact output is cancelled:skipped:child-cancelled in all 20 Swift 6.3.3 runs."
  },
  {
    "id": "task-group-throwing-success",
    "fixture": "Fixtures/task-group-throwing-success.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "try await taskGroupThrowingSuccessProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A successful withThrowingTaskGroup child is consumable through try await group.next().",
    "notes": "The owner adds one immediately successful child and awaits its result. Exact output is value in all 20 Swift 6.3.3 runs; child failure and first-error behavior are not asserted."
  },
  {
    "id": "task-group-throwing-next-failure",
    "fixture": "Fixtures/task-group-throwing-next-failure.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingNextFailureProbe()",
    "interpreterProjection": "string",
    "nativeFact": "try await ThrowingTaskGroup.next() rethrows the source error stored by its failed child.",
    "notes": "One child throws a nominal enum case and the owner immediately awaits next; a generic catch switches over the delivered error and returns caught-child in all 20 Swift 6.3.3 runs. Multiple-failure selection is not asserted."
  },
  {
    "id": "task-group-throwing-next-cancellation",
    "fixture": "Fixtures/task-group-throwing-next-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingNextCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A cancelled throwing-group child that observes cancellation makes try await next() throw CancellationError without marking the owner task cancelled.",
    "notes": "The group is cancelled before its sole child is added; that child immediately calls Task.checkCancellation(). All 20 Swift 6.3.3 runs returned cancellation:owner-active."
  },
  {
    "id": "task-group-throwing-implicit-failure",
    "fixture": "Fixtures/task-group-throwing-implicit-failure.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingImplicitFailureProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Normal return from a withThrowingTaskGroup body implicitly joins an unconsumed failing child but discards that child failure and preserves the body's return value.",
    "notes": "The child starts behind an explicit gate, the body opens it immediately before returning, and child-failed must precede scope-return. Exact output is child-start,body-return,child-failed,scope-return-body-value in all 20 Swift 6.3.3 runs. Because the body itself is nonthrowing, the rethrows call needs no try; no child-error or scheduler-order choice is asserted."
  },
  {
    "id": "task-group-throwing-body-throw",
    "fixture": "Fixtures/task-group-throwing-body-throw.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await throwingTaskGroupBodyThrowProbe()",
    "interpreterProjection": "string",
    "nativeFact": "When a withThrowingTaskGroup body throws, scope exit cancels and joins its outstanding child before rethrowing the body's source error.",
    "notes": "A started barrier holds the body until its child is inside a 30-second cancellable sleep. Exact output is child-start,body-throw,child-cancelled,caught-body in all 20 Swift 6.3.3 runs; every edge follows from the barrier, body throw, or structured-scope join."
  },
  {
    "id": "task-group-throwing-wait-for-all-failure",
    "fixture": "Fixtures/task-group-throwing-wait-for-all-failure.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingWaitForAllFailureProbe()",
    "interpreterProjection": "string",
    "nativeFact": "try await ThrowingTaskGroup.waitForAll() completes after successful children and rethrows the source error from a single failed child.",
    "notes": "Sequential groups first await one successful child, then one child that throws a nominal enum case. Exact output is success:caught-child in all 20 Swift 6.3.3 runs. Multiple-failure selection and implicit scope exit are not asserted."
  },
  {
    "id": "task-group-throwing-wait-for-all-empty",
    "fixture": "Fixtures/task-group-throwing-wait-for-all-empty.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingWaitForAllEmptyProbe()",
    "interpreterProjection": "string",
    "nativeFact": "try await ThrowingTaskGroup.waitForAll() returns normally when the group has no children.",
    "notes": "The body explicitly awaits an empty throwing group and returns empty in all 20 Swift 6.3.3 runs. There is no child or scheduler ordering claim."
  },
  {
    "id": "task-group-throwing-wait-for-all-multiple-success",
    "fixture": "Fixtures/task-group-throwing-wait-for-all-multiple-success.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingWaitForAllMultipleSuccessProbe()",
    "interpreterProjection": "string",
    "nativeFact": "try await ThrowingTaskGroup.waitForAll() returns normally after multiple successful children.",
    "notes": "Two children return values that are not observed individually; explicit waitForAll returns all-success in all 20 Swift 6.3.3 runs. Child completion order and failure selection are not asserted."
  },
  {
    "id": "task-group-throwing-wait-for-all-single-failure-among-multiple",
    "fixture": "Fixtures/task-group-throwing-wait-for-all-single-failure-among-multiple.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingWaitForAllSingleFailureProbe()",
    "interpreterProjection": "string",
    "nativeFact": "try await ThrowingTaskGroup.waitForAll() rethrows the sole source error when another child succeeds.",
    "notes": "One child succeeds and one throws a nominal enum case; a generic catch switches over the delivered error and returns caught-child in all 20 Swift 6.3.3 runs. There is only one failure identity, so first-error and completion-order selection are not asserted."
  },
  {
    "id": "task-group-throwing-wait-for-all-multiple-failures",
    "fixture": "Fixtures/task-group-throwing-wait-for-all-multiple-failures.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingWaitForAllMultipleFailuresProbe()",
    "interpreterProjection": "string",
    "nativeFact": "try await ThrowingTaskGroup.waitForAll() retains the first error observed through completion-ordered next(), drains all remaining children, then rethrows that source error.",
    "notes": "The first child waits behind a gate. Its opener does not release that gate until the second child has recorded completion immediately before throwing, so completion order is forced rather than inferred from scheduling. Exact output is caught-second:drained and proves both first-observed-error selection and full draining."
  },
  {
    "id": "task-group-throwing-wait-for-all-cancellation",
    "fixture": "Fixtures/task-group-throwing-wait-for-all-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupThrowingWaitForAllCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A cancelled throwing-group child that observes cancellation contributes CancellationError to waitForAll without cancelling the owner; waitForAll drains every remaining child before rethrowing.",
    "notes": "The group is cancelled before either child is added. One child records completion and calls Task.checkCancellation(); the other records completion and succeeds while ignoring cancellation. All 20 Swift 6.3.3 runs returned cancellation:owner-active:drained."
  },
  {
    "id": "task-group-state-properties",
    "fixture": "Fixtures/task-group-state-properties.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "try await taskGroupStatePropertiesProbe()",
    "interpreterProjection": "string",
    "nativeFact": "TaskGroup, ThrowingTaskGroup, DiscardingTaskGroup, and ThrowingDiscardingTaskGroup begin empty and active; an outstanding child makes each nonempty, cancelAll marks each cancelled, and consuming or automatically discarding the child makes each empty again.",
    "notes": "Each child enters a 30-second cancellable sleep, so the post-add nonempty read does not depend on scheduling. The owner then cancels and deterministically drains the child. All four summaries were true:false:false:false:true:true in 20 strict Swift 6.3.3 runs."
  },
  {
    "id": "task-group-wait-for-all-explicit-main-actor-isolation",
    "fixture": "Fixtures/task-group-wait-for-all-explicit-isolation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupWaitForAllExplicitIsolationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Strict Swift 6 accepts MainActor.shared as the explicit isolated-actor argument for ordinary and throwing waitForAll; each call suspends for a gated child and resumes its caller on MainActor.",
    "notes": "The gates force both waits to suspend. Exact output is ordinary-supported:main:main|throwing-supported:main:main in all repetitions; arbitrary source-defined actor executors remain an explicit M5 gap."
  },
  {
    "id": "task-group-next-result",
    "fixture": "Fixtures/task-group-next-result.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupNextResultProbe()",
    "interpreterProjection": "string",
    "nativeFact": "ThrowingTaskGroup.nextResult is nonthrowing: an empty group returns nil, success and source failure become Result cases, cancellation becomes Result.failure(CancellationError), each outcome is consumed exactly once, and the owner is not cancelled.",
    "notes": "Every nextResult call passes MainActor.shared explicitly. Exact output is main|empty,success-value,success-consumed,failure-child,failure-consumed-active,cancellation:owner-active,cancellation-consumed|main in all 20 strict Swift 6.3.3 runs; arbitrary source-defined actor executors remain an M5 gap."
  },
  {
    "id": "task-group-deprecated-spawn",
    "fixture": "Fixtures/task-group-deprecated-spawn.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupDeprecatedSpawnProbe()",
    "interpreterProjection": "string",
    "nativeFact": "The deprecated spawn(priority:operation:) aliases remain unconditional child creation for ordinary and throwing task groups, return successful values, and let a throwing child project its source error through next().",
    "notes": "The source passes .high through both aliases but does not claim an exact effective child priority. Exact output must be ordinary-value|throwing-value:failure-child in every strict Swift 6.3.3 native and interpreter repetition; no child scheduling order is observed."
  },
  {
    "id": "task-group-deprecated-async",
    "fixture": "Fixtures/task-group-deprecated-async.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupDeprecatedAsyncProbe()",
    "interpreterProjection": "string",
    "nativeFact": "The deprecated async(priority:operation:) group aliases remain unconditional child creation for ordinary and throwing task groups, return successful values, and let a throwing child project its source error through next().",
    "notes": "The source passes .high through both aliases but does not claim an exact effective child priority. Exact output must be ordinary-value|throwing-value:failure-child in every strict Swift 6.3.3 native and interpreter repetition; no child scheduling order is observed."
  },
  {
    "id": "task-group-deprecated-add",
    "fixture": "Fixtures/task-group-deprecated-add.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupDeprecatedAddProbe()",
    "interpreterProjection": "string",
    "nativeFact": "The deprecated async add(priority:operation:) aliases use conditional child creation for ordinary and throwing groups: they return true and deliver the child outcome before cancellation, then return false and create no child after cancelAll; throwing children retain their source error.",
    "notes": "The active swiftinterface explicitly forwards priority to addTaskUnlessCancelled, but runtime child-priority equality is not asserted. The probe must be nonisolated under strict Swift 6. Exact output must be ordinary-accepted:ordinary-value:ordinary-rejected:ordinary-empty|throwing-accepted:throwing-value:failure-accepted:failure-child:throwing-rejected:throwing-empty."
  },
  {
    "id": "task-group-deprecated-async-unless-cancelled",
    "fixture": "Fixtures/task-group-deprecated-async-unless-cancelled.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupDeprecatedAsyncUnlessCancelledProbe()",
    "interpreterProjection": "string",
    "nativeFact": "The deprecated asyncUnlessCancelled aliases are synchronous conditional child creation for ordinary and throwing groups: they return true and deliver the child outcome before cancellation, then return false and create no child after cancelAll; throwing children retain their source error.",
    "notes": "The active swiftinterface forwards priority and operation directly to addTaskUnlessCancelled. The source passes .high but does not assert exact effective child priority. Exact output must be ordinary-accepted:ordinary-value:ordinary-rejected:ordinary-empty|throwing-accepted:throwing-value:failure-accepted:failure-child:throwing-rejected:throwing-empty."
  },
  {
    "id": "task-group-deprecated-throwing-spawn-unless-cancelled",
    "fixture": "Fixtures/task-group-deprecated-throwing-spawn-unless-cancelled.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupDeprecatedThrowingSpawnUnlessCancelledProbe()",
    "interpreterProjection": "string",
    "nativeFact": "The deprecated ThrowingTaskGroup.spawnUnlessCancelled alias returns true and delivers successful or source-error child outcomes while active, then returns false and creates no child after cancelAll.",
    "notes": "The source passes .high but does not assert exact effective child priority. Exact output must be throwing-accepted:throwing-value:failure-accepted:failure-child:throwing-rejected:throwing-empty. The ordinary TaskGroup row is covered directly by the unchanged pinned swiftlang cancel-then-spawn fixture."
  },
  {
    "id": "task-group-make-async-iterator",
    "fixture": "Fixtures/task-group-make-async-iterator.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskGroupMakeAsyncIteratorProbe()",
    "interpreterProjection": "string",
    "nativeFact": "TaskGroup and ThrowingTaskGroup iterators consume the group's exactly-once completion stream, have value-semantic per-copy terminal state, become permanently terminal after nil, cancel(), or a thrown next(), and iterator cancellation requests group cancellation.",
    "notes": "The shared-stream sum is order-independent. Exact output must be default:5:6|shared:3|terminal:nil:nil:42|copy:nil:7:nil|throwing:failure-child:nil:9|cancel:cancelled:nil:rejected:cancelled:nil:rejected in every strict Swift 6.3.3 native and interpreter repetition."
  },
  {
    "id": "host-callback-task-runtime",
    "fixture": "Fixtures/host-callback-task-runtime.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "parityInterpreterHostCallback()",
    "interpreterProjection": "host-callback-global-string:parityHostCallbackProbe:phase",
    "nativeFact": "A synchronous MainActor callback publishes its inline mutation before returning, while an unstructured task created by that callback continues through Swift concurrency and may use structured task groups.",
    "notes": "The interpreter entry deliberately returns before the callback fires, matching a retained SwiftUI Button action. Exact output started,done-3 proves both synchronous callback semantics and independently scheduled concurrency-runtime work; no physical worker-thread identity is asserted."
  },
  {
    "id": "host-callback-structured-composition",
    "fixture": "Fixtures/host-callback-structured-composition.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "parityInterpreterHostCallbackStructuredComposition()",
    "interpreterProjection": "host-callback-global-string:parityHostCallbackStructuredCompositionProbe:phase",
    "nativeFact": "A retained synchronous MainActor callback returns after its inline mutation while independently scheduled work composes async let children, a shared detached task with two waiters, task-group children, and cancellation-handler observation.",
    "notes": "Already-GREEN characterization motivated by TaskObservatory. Exact output must be started,async:5|shared:10:10|cancelled. The sums and cancellation result are deterministic; no sibling order, physical thread, or unrelated scheduler order is asserted."
  },
  {
    "id": "host-callback-overlap",
    "fixture": "Fixtures/host-callback-overlap.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "parityInterpreterHostCallbackOverlap()",
    "interpreterProjection": "host-callback-overlap-global-string:parityHostCallbackOverlapProbe:events:worker-started",
    "nativeFact": "A MainActor task created by one synchronous callback may remain suspended while a later callback enters, observes and mutates the same actor-isolated state, and resumes it; the later callback completes before that task continues on MainActor.",
    "notes": "The harness waits for the first callback's task to publish worker-started before invoking the same retained callback again. Continuation resume is synchronous but MainActor serialization forces second-return before worker-resumed, so the exact three snapshots are causal and assert no unrelated scheduler or physical-thread order. Apple Swift 6.3.3 strict-concurrency compilation and the interpreter produced the same result in twenty bounded runs before and after mutable program registries plus the selected HostRegistry moved into session-owned RuntimeProgramState; every four-worker native shard reported SHA-256 4b1da57b3c315b718431c1f2b0fd875d7b3de0d2e66f35b46a79762815ea2652."
  },
  {
    "id": "host-static-shadow-after-suspension",
    "fixture": "Fixtures/host-static-shadow-after-suspension.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parityHostStaticShadowAfterSuspension()",
    "interpreterProjection": "string",
    "nativeFact": "A same-module Date.now extension remains the selected static member when an implicit .now value crosses a host-call boundary after suspension.",
    "notes": "The exact output is 1784228400. Task.yield establishes a suspension but no relative ready-task order is asserted; the focused ownership regression separately invokes an escaped origin callback after the interpreter has prepared a newer program."
  },
  {
    "id": "source-provenance-after-suspension",
    "fixture": "Fixtures/source-provenance-after-suspension.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await paritySourceProvenanceAfterSuspension()",
    "interpreterProjection": "string",
    "nativeFact": "An escaping async closure keeps the lexical source identity of its body across suspension; #line is the authored line in that body rather than a later invocation context.",
    "notes": "The exact output is 5. The fixture asserts only immutable source provenance across one suspension; it makes no scheduler-order, physical-thread, or parallelism claim."
  },
  {
    "id": "task-read-missing-await-diagnostic",
    "fixture": "Fixtures/task-read-missing-await-diagnostic.swift",
    "mode": "diagnostic",
    "assertion": "diagnostic",
    "repetitions": 1,
    "timeoutSeconds": 15,
    "diagnosticContains": ["expression is 'async' but is not marked with 'await'", "property access is 'async'"],
    "diagnosticLine": 4,
    "nativeFact": "Task.value and Task.result are async property accesses and source that omits await is rejected even inside an async function.",
    "notes": "The fixture diagnoses both properties with the same stable missing-await category. Compiler-backed interpreter preflight remains M7; M2's runtime dispatcher must still never return a placeholder."
  },
  {
    "id": "task-cancellation-after-completion",
    "fixture": "Fixtures/task-cancellation-after-completion.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 1,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskCancellationAfterCompletionProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Calling cancel() after a task has completed marks its handle cancelled without changing the stored successful value.",
    "notes": "The first awaited value establishes terminal completion before cancel() in program order. Exact output is active,cancelled,value,value; no ready-task scheduler order is asserted."
  },
  {
    "id": "async-let-missing-await-diagnostic",
    "fixture": "Fixtures/async-let-missing-await-diagnostic.swift",
    "mode": "diagnostic",
    "assertion": "diagnostic",
    "repetitions": 1,
    "timeoutSeconds": 15,
    "diagnosticContains": ["expression is 'async' but is not marked with 'await'", "reference to async let"],
    "diagnosticLine": 7,
    "nativeFact": "Reading an async-let binding is an async operation and is rejected when its use is not marked with await.",
    "notes": "Compiler-backed preflight remains M7. M4 still rejects the direct runtime read instead of exposing its internal task carrier, and lexical cleanup cancels and joins the child."
  },
  {
    "id": "actor-isolation-diagnostic",
    "fixture": "Fixtures/actor-isolation-diagnostic.swift",
    "mode": "diagnostic",
    "assertion": "diagnostic",
    "repetitions": 1,
    "timeoutSeconds": 15,
    "diagnosticContains": ["actor-isolated property", "nonisolated context"],
    "diagnosticLine": 6,
    "nativeFact": "A synchronous nonisolated function cannot read actor-isolated mutable state without an actor hop.",
    "notes": "Milestone 0 records the compiler fact; interpreter preflight comparison belongs to Milestone 7."
  },
  {
    "id": "actor-computed-setter-diagnostic",
    "fixture": "Fixtures/actor-computed-setter-diagnostic.swift",
    "mode": "diagnostic",
    "assertion": "diagnostic",
    "repetitions": 1,
    "timeoutSeconds": 15,
    "diagnosticContains": ["actor-isolated property", "can not be mutated from a nonisolated context"],
    "diagnosticLine": 9,
    "nativeFact": "An actor-isolated computed property cannot be mutated from outside the actor, and writing await before the assignment does not create a legal setter hop.",
    "notes": "The compiler also reports that the await contains no async operation and recommends an isolated method. Runtime setter execution is therefore confined to an already-owned actor segment."
  },
  {
    "id": "actor-subscript-setter-diagnostic",
    "fixture": "Fixtures/actor-subscript-setter-diagnostic.swift",
    "mode": "diagnostic",
    "assertion": "diagnostic",
    "repetitions": 1,
    "timeoutSeconds": 15,
    "diagnosticContains": ["actor-isolated subscript", "can not be mutated from a nonisolated context"],
    "diagnosticLine": 9,
    "nativeFact": "An actor-isolated subscript cannot be mutated from outside the actor, and writing await before the assignment does not create a legal setter hop.",
    "notes": "The compiler also warns that the await contains no async operation. Runtime subscript-setter execution is therefore confined to an already-owned actor segment."
  },
  {
    "id": "throwing-property-missing-try-diagnostic",
    "fixture": "Fixtures/throwing-property-missing-try-diagnostic.swift",
    "mode": "diagnostic",
    "assertion": "diagnostic",
    "repetitions": 1,
    "timeoutSeconds": 15,
    "diagnosticContains": ["property access can throw", "not marked with 'try'"],
    "diagnosticLine": 8,
    "nativeFact": "A throwing property getter preserves its effect at the access site, and an unhandled read without try is rejected.",
    "notes": "The minimal form is derived from swiftlang test/Concurrency/actor_call_implicitly_async.swift at the pinned Swift 6.3.3 release commit. M7 additionally verifies the same effect after serialization through a synthetic host declaration module, while a legal typed HostProperty executes success and error paths."
  },
  {
    "id": "task-group-named-add",
    "fixture": "Fixtures/task-group-named-add.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "try await taskGroupNamedAddProbe()",
    "interpreterProjection": "string",
    "nativeFact": "All eight addTask(name:priority:operation:) and addTaskUnlessCancelled(name:priority:operation:) group overloads assign the supplied name to the accepted child across ordinary, throwing, discarding, and throwing-discarding groups.",
    "notes": "Every group is active, each conditional add must be accepted, scope exit joins all children, and the recorder sorts the eight values before comparison. Exact output is discarding-add|discarding-unless|ordinary-add|ordinary-unless|throwing-add|throwing-discarding-add|throwing-discarding-unless|throwing-unless; no child scheduling order is asserted."
  },
  {
    "id": "task-name",
    "fixture": "Fixtures/task-name.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskNameProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Task.name is the immutable optional name supplied at creation for ordinary, detached, and task-group child tasks; omitted and explicit nil names observe nil rather than inheriting their creator's name, and an empty string remains distinct from nil.",
    "notes": "Every read occurs inside the task whose name is observed. Awaited handles establish result delivery; group values are sorted, so no ready-task order is asserted. Exact output is nil|ordinary|optional|nil|parent/nil|detached|empty|group,nil."
  },
  {
    "id": "top-level-async",
    "fixture": "Fixtures/top-level-async.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await topLevelAsyncProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Both deprecated top-level async(priority:operation:) overloads return ordinary unstructured Task handles: an explicit priority is visible when the child starts, a child may finish after its creating function returns, successful value reads preserve the value, and throwing value reads rethrow the source error.",
    "notes": "The successful child records its priority before any handle wait can donate priority, then remains behind a MainActor release gate after its creator returns. Awaited handles establish result delivery. Exact output is value:nil|17|boom; no relative start order between the two children is asserted."
  },
  {
    "id": "top-level-detached-aliases",
    "fixture": "Fixtures/top-level-detached-aliases.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await topLevelDetachedAliasesProbe()",
    "interpreterProjection": "string",
    "nativeFact": "The nonthrowing and throwing overloads of both deprecated top-level asyncDetached(priority:operation:) and detach(priority:operation:) create detached Task handles: they start with unnamed task metadata and cleared task-local storage, preserve explicit priorities before value-await donation, deliver successful values, and rethrow their exact source failures.",
    "notes": "Each alias is created in a synchronous nonisolated helper. Both successful operations record entry before the parent opens their shared MainActor gate, so their utility/background priorities are captured before either handle wait can donate priority and both complete after their creating functions return. Awaited handles establish result delivery without asserting relative start order. Exact output is asyncDetached:nil:17:default|detach:nil:9:default|asyncDetached-boom|detach-boom."
  },
  {
    "id": "task-group-executor-preference-nil-add",
    "fixture": "Fixtures/task-group-executor-preference-nil-add.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "try await taskGroupExecutorPreferenceNilAddProbe()",
    "interpreterProjection": "string",
    "nativeFact": "All eight addTask overloads carrying executorPreference across ordinary, throwing, discarding, and throwing-discarding groups accept an explicit nil preference, execute the child operation, preserve supplied or absent Task.name metadata, and join the children before their scopes return.",
    "notes": "Strict Swift 6 SIL selects each exact named and unnamed executorPreference overload once. Every group body waits until both operations record their names before returning, then scope exit joins the children. The recorder sorts all eight observations, so no child scheduling order is asserted. Exact output is discarding-named:discarding-name|discarding-unnamed:nil|ordinary-named:ordinary-name|ordinary-unnamed:nil|throwing-discarding-named:throwing-discarding-name|throwing-discarding-unnamed:nil|throwing-named:throwing-name|throwing-unnamed:nil."
  },
  {
    "id": "task-group-executor-preference-nil-add-unless-cancelled",
    "fixture": "Fixtures/task-group-executor-preference-nil-add-unless-cancelled.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "try await taskGroupExecutorPreferenceNilAddUnlessCancelledProbe()",
    "interpreterProjection": "string",
    "nativeFact": "All eight addTaskUnlessCancelled overloads carrying executorPreference across ordinary, throwing, discarding, and throwing-discarding groups accept explicit nil preferences while active, run named and unnamed children with the supplied Task.name metadata, and return false without creating children after group cancellation.",
    "notes": "Strict Swift 6 SIL selects each exact named and unnamed executorPreference overload twice: once while active and once after cancelAll(). Every group waits until its two accepted children record before cancellation, scope exit joins them, and the final recorder output is sorted, so no child scheduling order is asserted. Exact output is discarding-decisions:true:true:false:false|discarding-named:discarding-name|discarding-unnamed:nil|ordinary-decisions:true:true:false:false|ordinary-named:ordinary-name|ordinary-unnamed:nil|throwing-decisions:true:true:false:false|throwing-discarding-decisions:true:true:false:false|throwing-discarding-named:throwing-discarding-name|throwing-discarding-unnamed:nil|throwing-named:throwing-name|throwing-unnamed:nil."
  },
  {
    "id": "task-group-immediate-add",
    "fixture": "Fixtures/task-group-immediate-add.swift",
    "mode": "runtime",
    "assertion": "partial-order",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "try await taskGroupImmediateAddProbe()",
    "interpreterProjection": "string",
    "requiredEvents": [
      "ordinary-add-start:ordinary-add",
      "ordinary-add-after",
      "ordinary-add-finish",
      "ordinary-unless-start:ordinary-unless",
      "ordinary-unless-after:true",
      "ordinary-unless-finish",
      "ordinary-unless-rejected:false",
      "ordinary-cancelled-prefix:true",
      "ordinary-cancelled-check:caught",
      "ordinary-cancelled-after",
      "ordinary-cancelled-resumed:true",
      "throwing-add-start:throwing-add",
      "throwing-add-after",
      "throwing-add-finish",
      "throwing-unless-start:throwing-unless",
      "throwing-unless-after:true",
      "throwing-unless-finish",
      "throwing-unless-rejected:false",
      "discarding-add-start:discarding-add",
      "discarding-add-after",
      "discarding-add-finish",
      "discarding-unless-start:discarding-unless",
      "discarding-unless-after:true",
      "discarding-unless-finish",
      "discarding-unless-rejected:false",
      "throwing-discarding-add-start:throwing-discarding-add",
      "throwing-discarding-add-after",
      "throwing-discarding-add-finish",
      "throwing-discarding-unless-start:throwing-discarding-unless",
      "throwing-discarding-unless-after:true",
      "throwing-discarding-unless-finish",
      "throwing-discarding-unless-rejected:false"
    ],
    "precedes": [
      ["ordinary-add-start:ordinary-add", "ordinary-add-after"],
      ["ordinary-add-after", "ordinary-add-finish"],
      ["ordinary-unless-start:ordinary-unless", "ordinary-unless-after:true"],
      ["ordinary-unless-after:true", "ordinary-unless-finish"],
      ["ordinary-unless-after:true", "ordinary-unless-rejected:false"],
      ["ordinary-cancelled-prefix:true", "ordinary-cancelled-check:caught"],
      ["ordinary-cancelled-check:caught", "ordinary-cancelled-after"],
      ["ordinary-cancelled-after", "ordinary-cancelled-resumed:true"],
      ["throwing-add-start:throwing-add", "throwing-add-after"],
      ["throwing-add-after", "throwing-add-finish"],
      ["throwing-unless-start:throwing-unless", "throwing-unless-after:true"],
      ["throwing-unless-after:true", "throwing-unless-finish"],
      ["throwing-unless-after:true", "throwing-unless-rejected:false"],
      ["discarding-add-start:discarding-add", "discarding-add-after"],
      ["discarding-add-after", "discarding-add-finish"],
      ["discarding-unless-start:discarding-unless", "discarding-unless-after:true"],
      ["discarding-unless-after:true", "discarding-unless-finish"],
      ["discarding-unless-after:true", "discarding-unless-rejected:false"],
      ["throwing-discarding-add-start:throwing-discarding-add", "throwing-discarding-add-after"],
      ["throwing-discarding-add-after", "throwing-discarding-add-finish"],
      ["throwing-discarding-unless-start:throwing-discarding-unless", "throwing-discarding-unless-after:true"],
      ["throwing-discarding-unless-after:true", "throwing-discarding-unless-finish"],
      ["throwing-discarding-unless-after:true", "throwing-discarding-unless-rejected:false"]
    ],
    "nativeFact": "Every addImmediateTask and active addImmediateTaskUnlessCancelled operation begins synchronously on its inherited actor before the adding call returns; after a forced suspension the caller continues and opens the child gate. Active conditional calls return true, post-cancel conditional calls return false without running their operation, while an unconditional child added after cancelAll observes Task.isCancelled and Task.checkCancellation in its synchronous prefix and after resumption. Task.name is installed before the prefix, and scope exit joins every accepted child.",
    "notes": "Apple Swift 6.3.3 strict SIL selects all eight macOS 26 declarations, including both active and post-cancellation construction paths. Required-event multiplicity makes a rejected closure observable, while finish order between independent accepted children is deliberately unspecified. The pinned swiftlang startImmediately tests independently describe the synchronous prefix and preferred-executor resume contract, but their task-executor runtime block is disabled upstream; this same-source fixture supplies the active executable oracle for all four group kinds with an explicit nil preference."
  },
  {
    "id": "task-immediate",
    "fixture": "Fixtures/task-immediate.swift",
    "mode": "runtime",
    "assertion": "partial-order",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskImmediateProbe()",
    "interpreterProjection": "string",
    "requiredEvents": [
      "ordinary-start:ordinary:parent:17",
      "ordinary-after",
      "ordinary-finish",
      "detached-start:detached:default:9",
      "detached-after",
      "detached-finish",
      "ordinary-throwing-start:ordinary-throwing:parent:25",
      "ordinary-throwing-after",
      "ordinary-throwing-finish",
      "ordinary-throwing-error",
      "detached-throwing-start:detached-throwing:default:21",
      "detached-throwing-after",
      "detached-throwing-finish",
      "detached-throwing-error",
      "values:11:33"
    ],
    "precedes": [
      ["ordinary-start:ordinary:parent:17", "ordinary-after"],
      ["ordinary-after", "ordinary-finish"],
      ["detached-start:detached:default:9", "detached-after"],
      ["detached-after", "detached-finish"],
      ["ordinary-throwing-start:ordinary-throwing:parent:25", "ordinary-throwing-after"],
      ["ordinary-throwing-after", "ordinary-throwing-finish"],
      ["ordinary-throwing-finish", "ordinary-throwing-error"],
      ["detached-throwing-start:detached-throwing:default:21", "detached-throwing-after"],
      ["detached-throwing-after", "detached-throwing-finish"],
      ["detached-throwing-finish", "detached-throwing-error"],
      ["ordinary-finish", "values:11:33"],
      ["detached-finish", "values:11:33"]
    ],
      "nativeFact": "All four Task.immediate and Task.immediateDetached overload families synchronously enter a same-actor operation before construction returns, stop at a real suspension, install name and explicit priority before that prefix, and preserve values and exact source failures through handle waits. Task.immediate copies task-local context while Task.immediateDetached clears it; detached context inheritance is independent from the shared immediate-start policy.",
    "notes": "Apple Swift 6.3.3 strict compilation and SIL select each of the four macOS 26 declarations once. The MainActor gates establish start < after < finish without asserting finish order between independent tasks. Explicit nil executor preferences delimit the supported subset; arbitrary operation actors and nonnil TaskExecutor preferences remain recorded divergences. Twenty native fresh runs produced two allowed sibling-finish distributions."
  },
  {
    "id": "with-unsafe-current-task",
    "fixture": "Fixtures/with-unsafe-current-task.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await unsafeCurrentTaskProbe()",
    "interpreterProjection": "string",
    "nativeFact": "The synchronous and asynchronous withUnsafeCurrentTask overloads pass the current task identity inline. The capability reports effective and base priority, cancel() immediately updates both capability and Task cancellation views, identity survives suspension, and both overloads rethrow the operation's exact source error.",
    "notes": "Apple Swift 6.3.3 strict compilation selects both active macOS declarations. Exact output is deterministic because the probe compares effective priority with Task.currentPriority, equal-identity hashes only with each other, and observes only explicit base-priority raw values; it does not claim scheduling order or physical threads. This is the deterministic same-source adapter for pinned swiftlang test/Concurrency/Runtime/async_task_withUnsafeCurrentTask.swift, whose pointer-regex oracle covers only the synchronous overload."
  },
  {
    "id": "task-priority-escalation-handler",
    "fixture": "Fixtures/task-priority-escalation-handler.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskPriorityEscalationHandlerProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A priority increase that occurs while an operation is in scope invokes every active nested priority-escalation handler with the exact old and new effective priorities. Registration does not replay an earlier increase, while a later in-scope increase is delivered. The operation observes the new priority when it resumes. The operation's exact source error and CancellationError propagate through the scope, and handlers are removed before a later escalation after success, failure, or cancellation exit.",
    "notes": "Apple Swift 6.3.3 strict compilation selects both public macOS 26 declarations; the underscored source-callable wrapper is exercised with explicit nil isolation. MainActor handshakes force each high-priority waiter to register only after the low-priority operation or post-scope continuation is ready. Handler events are sorted because relative order between simultaneously active nested callbacks is deliberately unspecified; physical threads and unrelated task order are not asserted."
  },
  {
    "id": "with-task-executor-preference-nil",
    "fixture": "Fixtures/with-task-executor-preference-nil.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await taskExecutorPreferenceNilProbe()",
    "interpreterProjection": "string",
    "nativeFact": "With no ambient custom TaskExecutor, withTaskExecutorPreference(nil, isolation: nil) invokes each supplied bare unqualified global async operation function, whose declaration carries a plain explicit nonisolated modifier and no executor preference, in the same task. The success operation observes its detached task's name, native high-priority raw value, task-local binding, and cancellation state across a forced yield; identity, name, priority, task local, and cancellation remain stable after scope exit. Cancelling the current task inside the cancellation operation is visible to the caller after return, while successful values and the exact typed source error propagate.",
    "notes": "Apple Swift 6.3.3 compiled the same source in Swift 6 strict-concurrency mode with warnings as errors, and twenty bounded native runs produced one exact observation. The observation remains exact after synchronous and suspending argument collection migrated to immutable call-site metadata and its captured bare-unqualified direct-reference provenance. Only the explicit-nil task-executor and isolation subset without an ambient custom preference and with the supplied bare unqualified global async operation functions, whose declarations carry a plain explicit nonisolated modifier and no executor preference, is asserted. Native nil inherits an ambient custom preference rather than clearing it. @concurrent operations, qualified or parenthesized references, custom executor scheduling and inheritance, nonisolated(nonsending), closure-expression operations, arbitrary actor isolation, resume executors, physical threads, and unrelated task order remain outside this case."
  },
  {
    "id": "extract-isolation-nonisolated",
    "fixture": "Fixtures/extract-isolation-nonisolated.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "extractIsolationNonisolatedProbe()",
    "interpreterProjection": "string",
    "nativeFact": "For both supplied bare unqualified direct global async function values with plain explicit nonisolated declarations, one also @concurrent, extractIsolation returns nil. It only inspects each function value: neither supplied operation is invoked.",
    "notes": "Apple Swift 6.3.3 strict-concurrency compilation selects the active deprecated variadic-generic declaration. Twenty bounded native runs produce one exact observation. The fixture deliberately places uses in deprecated contexts; the shared NativeMain call site may still emit the expected deprecation warning. MainActor and arbitrary actor identities, unannotated functions under configurable default isolation, qualified or parenthesized references, local aliases, function-value conversions, member references, closure expressions, synchronous functions, and the replacement closure.isolation property remain outside this bare-unqualified-direct-global-async-declaration subset. No scheduling, suspension, executor-hop, or physical-thread behavior is asserted because extractIsolation is synchronous metadata inspection."
  },
  {
    "id": "protocol-async-sequence-iteration",
    "fixture": "Fixtures/protocol-async-sequence-iteration.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await protocolAsyncSequenceIterationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Protocol for-await iteration obtains a user-defined AsyncSequence iterator and repeatedly awaits its mutating next() operation until nil. Each nonnil element is delivered exactly once; a real Task.yield suspension inside next() does not duplicate, skip, or terminate iteration.",
    "notes": "Apple Swift 6.3.3 strict-concurrency compilation succeeded, and twenty bounded native runs produced exact output 3:6. Count and a commutative sum avoid any scheduler-order claim; physical threads, unrelated ready-task order, throwing iteration, cancellation, early exit, and host-backed sequences are outside this first M6 success slice."
  },
  {
    "id": "protocol-async-sequence-throwing",
    "fixture": "Fixtures/protocol-async-sequence-throwing.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await protocolAsyncSequenceThrowingProbe()",
    "interpreterProjection": "string",
    "nativeFact": "When a mutating AsyncIterator.next() throws a typed source error after two successful suspending results, for try await terminates without delivering an element for the throwing call and the enclosing case-specific catch handles that exact error.",
    "notes": "Apple Swift 6.3.3 strict-concurrency compilation succeeded, and twenty bounded native runs produced exact output 2:3:caught. Count and sum record only the two successful elements; the case-specific catch proves source-error identity without expanding this sequence test into associated-value catch-pattern support. Physical threads, unrelated scheduling, cancellation, early exit, and host-backed sequences are not asserted."
  },
  {
    "id": "protocol-async-sequence-cancellation",
    "fixture": "Fixtures/protocol-async-sequence-cancellation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await protocolAsyncSequenceCancellationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Cancelling a task while a user AsyncIterator.next() is suspended does not automatically terminate for-await iteration. Cancellation remains cooperative: the iterator resumes, observes its task's request, may return an element that the loop consumes, and the task may complete successfully while remaining marked cancelled.",
    "notes": "A MainActor gate forces cancel() after next() enters and before it resumes. Apple Swift 6.3.3 strict-concurrency compilation succeeded, and twenty bounded native runs produced exact output 1:7:true:true. The iterator chooses value 7 only when it observes cancellation, then returns nil on its next call. No physical-thread or unrelated ready-task order is asserted; cancellable streams are a separate M6 question."
  },
  {
    "id": "protocol-async-sequence-early-exit",
    "fixture": "Fixtures/protocol-async-sequence-early-exit.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await protocolAsyncSequenceEarlyExitProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Breaking from protocol for-await iteration stops requesting elements immediately. The current iteration's defer runs before control reaches the statement after the loop, just as it does for earlier normally completed iterations.",
    "notes": "The iterator has no terminal nil path, so exact completion proves that break prevents a third next call. Apple Swift 6.3.3 strict-concurrency compilation succeeded, and twenty bounded native runs produced exact output 12:2:next-1,defer-1,next-2,defer-2,after. Task.yield forces real suspension in every next call; no physical-thread or unrelated ready-task order is asserted."
  },
  {
    "id": "protocol-async-sequence-return-continue",
    "fixture": "Fixtures/protocol-async-sequence-return-continue.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await protocolAsyncSequenceReturnContinueProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Continue from a protocol for-await body runs that iteration's defer before requesting the next element. Returning from the consumer runs the current body defer and the enclosing function defer, stops before another next request, and resumes the caller afterward.",
    "notes": "The iterator has no terminal nil path and every next call suspends with Task.yield. Apple Swift 6.3.3 strict-concurrency compilation succeeded, and twenty bounded native runs produced exact output returned-3:3:next-1,continue-1,defer-1,next-2,body-2,defer-2,next-3,return-3,defer-3,consumer-defer,after. No physical-thread or unrelated ready-task order is asserted."
  },
  {
    "id": "protocol-async-sequence-default-witnesses",
    "fixture": "Fixtures/protocol-async-sequence-default-witnesses.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await protocolAsyncSequenceDefaultWitnessesProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Protocol-extension default witnesses can satisfy both a refined AsyncSequence's makeAsyncIterator() requirement and a refined AsyncIteratorProtocol's mutating async next() requirement. For-await dispatches those defaults and preserves iterator value state across suspension until nil.",
    "notes": "The concrete sequence and iterator declare only their associated-type witnesses and stored iterator state; neither redeclares the two callable requirements. Apple Swift 6.3.3 strict-concurrency compilation succeeded, and twenty bounded native runs produced exact output 3:6. Every successful default next() suspends with Task.yield; no physical-thread or unrelated ready-task order is asserted."
  },
  {
    "id": "host-bridged-async-sequence",
    "fixture": "Fixtures/host-bridged-async-sequence.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await hostBridgedAsyncSequenceProbe()",
    "interpreterProjection": "string",
    "nativeFact": "For-await over an SDK-owned opaque AsyncSequence calls its host-gateway makeAsyncIterator() once and repeatedly awaits the host iterator's next() until nil. Three suspending values are delivered exactly once, and the terminal fourth next() ends iteration.",
    "notes": "The shared native support models an SDK sequence while the interpreter receives only typed HostRegistry contracts for the opaque sequence and iterator carriers. Apple Swift 6.3.3 strict-concurrency compilation succeeded with warnings as errors, and twenty bounded native runs produced exact output 3:12. Focused interpreter evidence additionally requires all four next() calls to own tracked host operations and leaves the host-operation and task/scope/group registries empty. No fixture ID or carrier name is recognized by production code."
  },
  {
    "id": "async-stream-suspended-consumer",
    "fixture": "Fixtures/async-stream-suspended-consumer.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncStreamSuspendedConsumerProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An unbounded AsyncStream builder returns immediately, its producer task yields two values across real suspensions, and a for-await consumer that starts before the first yield resumes once per value before finish terminates iteration at nil.",
    "notes": "Apple Swift 6.3.3 strict-concurrency compilation succeeded with warnings as errors, and twenty bounded native/interpreter runs produced exact output 2:6 with canonical native-observation SHA-256 5c90df842175db3c82f1151ac4cdbb285bc67a7e6f4136a0f0db96ea56d63b49. The MainActor producer begins with Task.yield so the consumer reaches an empty stream and must enter the runtime-owned waitingForStream suspension before delivery. Focused evidence requires one stream record to be created and every stream/task/scope/group/host-operation/scheduler registry to drain. The semantic subset is adapted from the unchanged pinned swiftlang async_stream.swift oracle (SHA-256 940c49ec9cfe4a0292f13757e0a56be5a601618fd5ecaae6d489ce9952569447); that full upstream file still needs an adapter because it imports StdlibUnittest and covers unsupported surface. No unrelated task order or physical-thread behavior is asserted."
  },
  {
    "id": "async-stream-cancelled-consumer",
    "fixture": "Fixtures/async-stream-cancelled-consumer.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncStreamCancelledConsumerProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Cancelling a task suspended in an empty AsyncStream.Iterator.next() synchronously invokes onTermination with .cancelled before resuming next() with nil. The consumer remains marked cancelled while completing normally.",
    "notes": "The MainActor child sets enteredNext immediately before awaiting next(); run-to-suspension means the parent cannot observe that flag until the iterator is actually parked. Apple Swift 6.3.3 strict-concurrency compilation succeeded with warnings as errors, and twenty bounded native/interpreter runs produced exact output true:true:cancelled:cancelled with canonical native-observation SHA-256 56a7e6749ac968b86afc193b51e0a60609748b599aff47a818c4c729d5c17616. The result asserts the causal callback-before-nil edge, cancellation observation, and final callback value without relying on unrelated scheduler order or a physical thread. Focused evidence requires the stream wait, cancellation registration, stream record, source tasks, and scheduler to drain."
  },
  {
    "id": "async-stream-finish-termination",
    "fixture": "Fixtures/async-stream-finish-termination.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncStreamFinishTerminationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "AsyncStream.Continuation.finish() marks the stream terminal, synchronously invokes onTermination(.finished) before returning, preserves already-buffered values, and makes repeated finish calls inert. A yield after finish returns .terminated and cannot create another element.",
    "notes": "The callback writes finished and source code appends after-finish only after finish() returns, making their order observable without scheduler assumptions. Apple Swift 6.3.3 strict-concurrency compilation succeeded with warnings as errors, and twenty bounded native/interpreter runs produced exact output 3:true:true:finished,after-finish:terminated with canonical native-observation SHA-256 e5c5b4559a7c0e21a670c4febbf08d3bff2709e4d9580c49d0b93b5b69404f2b. Two terminal next() calls prove stable nil, and the post-terminal yield result is rendered directly as terminated. This was characterization of the existing general stream mechanism; no production change was required."
  },
  {
    "id": "async-stream-multiple-consumers",
    "fixture": "Fixtures/async-stream-multiple-consumers.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncStreamMultipleConsumersProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Two independent AsyncStream iterators may wait concurrently on the same empty storage. A single continuation.finish() resumes every parked consumer with nil, and both tasks complete normally.",
    "notes": "Each MainActor child increments enteredCount immediately before awaiting next(). The parent cannot observe count 2 until both children have yielded the executor inside next(), then calls finish. Apple Swift 6.3.3 strict-concurrency compilation succeeded with warnings as errors, and twenty bounded native/interpreter runs produced exact output true:true:2 with canonical native-observation SHA-256 4b4ba4ee7f6ceb999b5122aea7f8ca85d6e4269f859552649e79676f384f2090. The result is commutative and makes no claim about which consumer resumes first. Focused evidence requires at least two runtime-owned stream suspensions and complete cleanup. This was characterization of the existing general stream mechanism; no production change was required."
  },
  {
    "id": "async-stream-copied-iterators",
    "fixture": "Fixtures/async-stream-copied-iterators.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncStreamCopiedIteratorsProbe()",
    "interpreterProjection": "string",
    "nativeFact": "AsyncStream.Iterator is a value type whose copies have independent mutating-next access while sharing the same stream storage. Two copies may park concurrently, and two yields resume them with one distinct element each.",
    "notes": "Each task creates a local mutable iterator from the same seed value, then signals immediately before next(isolation: #isolation). The explicit isolation argument is required by strict Swift 6 sendability checking and keeps both calls on MainActor. The parent waits until both are parked, yields 4 and 6, and sorts the two results; no consumer assignment or resume order is asserted. Apple Swift 6.3.3 compiled with warnings as errors, and twenty native/interpreter runs produced exact output 4:6:2 with canonical native-observation SHA-256 594fcb32181db21eab266d2a8ba6ccdbb8183487d482a5db80bee4e6990e9062. Focused evidence requires two real stream suspensions and complete cleanup."
  },
  {
    "id": "async-stream-scope-termination",
    "fixture": "Fixtures/async-stream-scope-termination.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncStreamScopeTerminationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "When an unfinished AsyncStream and its continuation lose their last references at function scope exit, storage deinitialization synchronously invokes onTermination(.cancelled) before the caller continues.",
    "notes": "The nested function discards the only sequence value and does not let its builder continuation escape. The outer function reads the callback-owned state immediately after the nested call, making scope-release ordering observable without scheduler assumptions. Apple Swift 6.3.3 compiled in Swift 6 complete-strict mode with warnings as errors, and twenty bounded native/interpreter runs produced exact output cancelled with canonical native-observation SHA-256 a78cb0e23e5bae8b901c6a25188a7d0233033b9177f49a4996a46316f8083d52. The captured interpreter RED was none: storage ARC closed the runtime record but omitted the source callback. Focused evidence requires one created stream, zero consumer suspensions, and complete stream/task/scope/group/host-operation/scheduler cleanup."
  },
  {
    "id": "async-stream-escaped-continuation-lifetime",
    "fixture": "Fixtures/async-stream-escaped-continuation-lifetime.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncStreamEscapedContinuationLifetimeProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An escaped AsyncStream producer continuation does not own the unfinished stream's lifetime. Releasing the final sequence/iterator owner synchronously invokes onTermination(.cancelled); a later yield through the still-live continuation returns .terminated, and releasing that handle does not invoke the callback again.",
    "notes": "The nested function discards its only sequence value while storing the builder continuation in an outer state object. The caller records termination immediately after sequence scope exit, calls yield through the escaped handle, releases that handle, and records termination again. These edges require no tasks, sleeps, or scheduler-order assumptions. Apple Swift 6.3.3 compiled in Swift 6 complete-strict mode with warnings as errors, and twenty bounded native/interpreter runs produced exact output cancelled:terminated:cancelled with canonical native-observation SHA-256 7ca384f8a0176ff57fa4e3c0055a1ff7783123568a4e7552a39be27fae5a3e20. The captured interpreter RED was none:enqueued:cancelled because its continuation carrier incorrectly retained storage. Focused evidence requires one created stream, zero consumer suspensions, and complete stream/task/scope/group/host-operation/scheduler cleanup."
  },
  {
    "id": "async-stream-buffering-newest",
    "fixture": "Fixtures/async-stream-buffering-newest.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncStreamBufferingNewestProbe()",
    "interpreterProjection": "string",
    "nativeFact": "AsyncStream bufferingNewest(2) retains at most the two newest unconsumed values. The first two yields report decreasing remaining capacity, each later yield reports the oldest value it displaced, and iteration receives only 3 then 4 before terminal nil.",
    "notes": "All four yields occur synchronously in the builder before iteration, so buffer state and eviction order are deterministic. YieldResult is rendered directly to preserve its remaining-capacity and dropped-element payloads; no task order or physical executor behavior is asserted. Apple Swift 6.3.3 compiled in Swift 6 complete-strict mode with warnings as errors, and twenty bounded native/interpreter runs produced exact output enqueued(remaining: 1)|enqueued(remaining: 0)|dropped(1)|dropped(2)=>3,4,true with canonical native-observation SHA-256 cc694ba7c7410fafe79768c883e7b3072fc461257e94b6b5b1da4bfa050533f8. The captured interpreter RED was the fail-closed diagnostic that only .unbounded buffering was supported. Focused evidence requires one created stream, zero consumer suspensions, and complete stream/task/scope/group/host-operation/scheduler cleanup."
  },
  {
    "id": "async-stream-buffering-oldest",
    "fixture": "Fixtures/async-stream-buffering-oldest.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncStreamBufferingOldestProbe()",
    "interpreterProjection": "string",
    "nativeFact": "AsyncStream bufferingOldest(2) retains at most the first two unconsumed values. The first two yields report decreasing remaining capacity, each later yield reports the new value it rejected, and iteration receives only 1 then 2 before terminal nil.",
    "notes": "All four yields occur synchronously in the builder before iteration, so buffer state and rejection order are deterministic. YieldResult is rendered directly to preserve its remaining-capacity and dropped-element payloads; no task order or physical executor behavior is asserted. Apple Swift 6.3.3 compiled in Swift 6 complete-strict mode with warnings as errors, and twenty bounded native/interpreter runs produced exact output enqueued(remaining: 1)|enqueued(remaining: 0)|dropped(3)|dropped(4)=>1,2,true with canonical native-observation SHA-256 ce750f2131b0fb4526ee96775de38a23ab5c0b853f4a751b1f11993b223052f0. The captured interpreter RED was the fail-closed diagnostic that .bufferingOldest was unsupported. Focused evidence requires one created stream, zero consumer suspensions, and complete stream/task/scope/group/host-operation/scheduler cleanup."
  },
  {
    "id": "async-stream-zero-capacity",
    "fixture": "Fixtures/async-stream-zero-capacity.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncStreamZeroCapacityProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Both bufferingNewest(0) and bufferingOldest(0) retain no unconsumed value. Each yield reports the newly supplied element as dropped, finish is terminal, and the first iterator read returns nil.",
    "notes": "Each zero-capacity stream is produced and drained sequentially on MainActor. The only yield occurs synchronously before iteration, so rejection and terminal state are deterministic and no task order or physical executor behavior is asserted. Apple Swift 6.3.3 compiled in Swift 6 complete-strict mode with warnings as errors, and twenty bounded native/interpreter runs produced exact output newest:dropped(1):true|oldest:dropped(2):true with canonical native-observation SHA-256 835466db10fcdaa4c20da208c0dbea51584198f6d47292fdf4096c8310702079. The captured interpreter RED was its explicit fail-closed diagnostic for .bufferingNewest(0). Focused evidence requires two created streams, zero consumer suspensions, and complete stream/task/scope/group/host-operation/scheduler cleanup. Negative capacities remain explicitly unsupported."
  },
  {
    "id": "async-throwing-stream-suspended-failure",
    "fixture": "Fixtures/async-throwing-stream-suspended-failure.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncThrowingStreamSuspendedFailureProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An empty AsyncThrowingStream consumer suspends, receives a yielded value, and then observes the exact source error supplied to finish(throwing:) after draining that value.",
    "notes": "The MainActor producer starts with Task.yield before delivering one value and a typed source error. The exact count, sum, and case-specific catch avoid unrelated task-order or physical-executor claims. Apple Swift 6.3.3 compiled in Swift 6 complete-strict mode with warnings as errors, and twenty bounded native/interpreter runs produced exact output 1:2:caught with canonical native-observation SHA-256 b35c84ff9dee12339f6f150c9f5074154bc4ee6c478e988f675c468f1f291d56. The captured interpreter RED was the missing AsyncThrowingStream constructor diagnostic. Focused evidence requires one created stream, at least one consumer suspension, and complete stream/task/scope/group/host-operation/scheduler cleanup. Normal finish, termination callbacks, bounded buffering, and throwing-stream lifetime edges remain fail-closed or open."
  },
  {
    "id": "async-throwing-stream-finish-termination",
    "fixture": "Fixtures/async-throwing-stream-finish-termination.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncThrowingStreamFinishTerminationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "AsyncThrowingStream normal finish synchronously invokes one onTermination(.finished(nil)) callback before returning, preserves a prior buffered value, makes repeated terminal reads nil, and makes a later yield return terminated.",
    "notes": "The builder performs every producer operation synchronously and records an event immediately after finish returns. Three sequential iterator reads then expose the buffered value and stable terminal state without task-order or physical-executor claims. Apple Swift 6.3.3 compiled in Swift 6 complete-strict mode with warnings as errors, and twenty bounded native/interpreter runs produced exact output 3:true:true:finished(nil),after-finish:terminated with canonical native-observation SHA-256 c208018808dc13933512c4d0274986274461222cfafc3806ebfcfd65d880f944. The captured interpreter RED was assignment rejection for AsyncThrowingStream.Continuation.onTermination. Focused evidence requires one created stream, zero consumer suspensions, and complete stream/task/scope/group/host-operation/scheduler cleanup. Failure/cancellation callback payloads and implicit lifetime termination remain fail-closed or open."
  },
  {
    "id": "async-throwing-stream-failure-termination",
    "fixture": "Fixtures/async-throwing-stream-failure-termination.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncThrowingStreamFailureTerminationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "AsyncThrowingStream finish(throwing:) synchronously invokes one onTermination(.finished(error)) callback containing the supplied source error before returning, preserves a prior buffered value, rethrows that same error after the value, and terminates later yields.",
    "notes": "The callback renders the associated termination error only to prove that it contains the stopped case, avoiding module-qualified description differences. Every producer operation is synchronous; sequential iterator reads make value-before-error ordering deterministic without task-order or physical-executor claims. Apple Swift 6.3.3 compiled in Swift 6 complete-strict mode with warnings as errors, and twenty bounded native/interpreter runs produced exact output 5:caught:finished-error,after-finish:terminated with canonical native-observation SHA-256 f1ab4a6a9520f687349a779caf3d418c77cd925a5b9011cf4d98167001453935. The captured interpreter RED was its explicit fail-closed diagnostic for a failure termination callback. Focused evidence requires one created stream, zero consumer suspensions, and complete stream/task/scope/group/host-operation/scheduler cleanup. Cancellation callbacks and implicit lifetime termination remain fail-closed or open."
  },
  {
    "id": "async-throwing-stream-cancelled-consumer",
    "fixture": "Fixtures/async-throwing-stream-cancelled-consumer.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncThrowingStreamCancelledConsumerProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Cancelling a task parked in empty AsyncThrowingStream.Iterator.next() synchronously invokes onTermination(.cancelled) before next returns nil; the task may return normally while remaining marked cancelled.",
    "notes": "The MainActor child marks entry immediately before awaiting next. Run-to-suspension prevents the parent from observing that marker until the iterator can park, so cancel occurs at the controlled edge without unrelated task-order or physical-executor claims. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and returned true:true:cancelled:cancelled in twenty bounded runs. The interpreter RED timed out because its explicit throwing-stream cancellation guard discarded the callback and left the parked waiter unresolved. The shared cancellation path now delivers the flavor-specific .cancelled value before resuming next with nil. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 8248bec5de22875ebe62a306cd31b2da279ff76a8ae4e49dbe34dbb67c77ea93; focused runtime evidence observes one suspension and complete ownership cleanup."
  },
  {
    "id": "async-throwing-stream-buffering-newest",
    "fixture": "Fixtures/async-throwing-stream-buffering-newest.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncThrowingStreamBufferingNewestProbe()",
    "interpreterProjection": "string",
    "nativeFact": "AsyncThrowingStream bufferingNewest(2) retains the newest two unconsumed values, reports remaining capacity after successful insertion, returns each displaced oldest value once full, and drains only the retained values before terminal nil.",
    "notes": "Every producer operation precedes iteration in one MainActor task. Sequential next calls make the yield results, eviction semantics, retained-value order, and terminal nil deterministic without task-order or physical-executor claims. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced exact output enqueued(remaining: 1)|enqueued(remaining: 0)|dropped(1)|dropped(2)=>3,4,true in twenty bounded runs. The interpreter RED was the explicit blanket diagnostic rejecting every bounded AsyncThrowingStream policy. The shared storage kernel now accepts only the proven positive-capacity bufferingNewest subset; bufferingOldest and zero capacity stay fail-closed. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 94d8669f69f6d2b253081259b081438fe1602acc67a8d2158bf00165c7923f7b; focused evidence records one stream, no consumer suspension, and complete ownership cleanup."
  },
  {
    "id": "async-throwing-stream-buffering-oldest",
    "fixture": "Fixtures/async-throwing-stream-buffering-oldest.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncThrowingStreamBufferingOldestProbe()",
    "interpreterProjection": "string",
    "nativeFact": "AsyncThrowingStream bufferingOldest(2) retains the first two unconsumed values, reports remaining capacity after successful insertion, returns each newly rejected value once full, and drains only the retained values before terminal nil.",
    "notes": "Every producer operation precedes iteration in one MainActor task. Sequential next calls make the yield results, rejection semantics, retained-value order, and terminal nil deterministic without task-order or physical-executor claims. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced exact output enqueued(remaining: 1)|enqueued(remaining: 0)|dropped(3)|dropped(4)=>1,2,true in twenty bounded runs. The interpreter RED was the explicit policy capability diagnostic. The shared storage kernel now accepts both proven positive-capacity policies while zero capacity stays fail-closed. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 6272b53c8b144fdc2dcd1a634e3d61f188df3a54ed9f5297cb29e1690bd0db68; focused evidence records one stream, no consumer suspension, and complete ownership cleanup."
  },
  {
    "id": "async-throwing-stream-zero-capacity",
    "fixture": "Fixtures/async-throwing-stream-zero-capacity.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncThrowingStreamZeroCapacityProbe()",
    "interpreterProjection": "string",
    "nativeFact": "AsyncThrowingStream bufferingNewest(0) and bufferingOldest(0) retain no unconsumed value, return the supplied value as dropped, and read terminal nil after finish.",
    "notes": "Each stream performs one synchronous yield and finish before one sequential next call. The exact yield result and empty terminal state are program-ordered; no scheduler or physical-executor behavior is asserted. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced exact output newest:dropped(1):true|oldest:dropped(2):true in twenty bounded runs. The interpreter RED was the remaining zero-capacity capability guard. Removing the temporary throwing-policy guard lets both flavors share every nonnegative policy while the common parser still rejects negative and unknown forms. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 72fec1663febaddef370e266d91916d3a79842c423b27d3dea3ed16093cadd0b; focused evidence records two streams, no consumer suspension, and complete ownership cleanup."
  },
  {
    "id": "async-throwing-stream-copied-iterators",
    "fixture": "Fixtures/async-throwing-stream-copied-iterators.swift",
    "mode": "runtime",
    "assertion": "runtime-trap",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncThrowingStreamCopiedIteratorsProbe()",
    "interpreterProjection": "string",
    "nativeTrapContains": ["attempt to await next() on more than one task"],
    "interpreterTrapContains": ["attempt to await AsyncThrowingStream.Iterator.next() concurrently"],
    "nativeFact": "AsyncThrowingStream permits only one pending next call per stream: starting next on a value-copied iterator while another copy is parked traps instead of creating a second consumer.",
    "notes": "Task.immediate runs the first copied iterator's synchronous prefix until its real next suspension before returning the handle. The parent then starts next on a second value copy while the first is causally pending. The expected trap is process-isolated; no waiter order or physical-executor behavior is asserted."
  },
  {
    "id": "async-throwing-stream-scope-termination",
    "fixture": "Fixtures/async-throwing-stream-scope-termination.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncThrowingStreamScopeTerminationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "When an unfinished AsyncThrowingStream and its builder continuation lose their last references at nested-function exit, storage deinitialization synchronously invokes onTermination(.cancelled) before the caller continues.",
    "notes": "The nested function discards the only sequence value and does not let its builder continuation escape. The caller reads callback-owned state immediately, so lifetime ordering is exact without scheduler, executor, or thread claims. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced cancelled in twenty bounded native runs. The interpreter RED was its explicit AsyncThrowingStream implicit-termination unsupported diagnostic. Removing that temporary flavor guard lets the shared storage-deinit path synchronously pass the throwing `.cancelled` value, preserve callback failures for the next safe point, and close the record. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 165b1dc6bc0549403e963d0309882cc678f0ef9ff759b898171790b9d9641cc1; focused evidence records one stream, no consumer suspension, and complete ownership cleanup."
  },
  {
    "id": "async-throwing-stream-escaped-continuation-lifetime",
    "fixture": "Fixtures/async-throwing-stream-escaped-continuation-lifetime.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await asyncThrowingStreamEscapedContinuationLifetimeProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An escaped AsyncThrowingStream producer continuation does not retain unfinished stream storage: final sequence release synchronously cancels the stream, later yield is terminated, and releasing the handle is inert.",
    "notes": "All three observations are program-ordered in one MainActor task. No task scheduling, executor, or thread behavior is asserted. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced cancelled:terminated:cancelled in twenty bounded native runs. This was an already-GREEN characterization: the shared producer carrier was already weak for both stream flavors after scope-exit support landed, so no production change was required. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 37ca395c9da690fb5efa6fba8dfec52ec9ee6ddce65ce54e759749b1a8932bb2; focused evidence records one stream, no consumer suspension, and complete ownership cleanup."
  },
  {
    "id": "unsafe-continuation-fail-closed",
    "fixture": "Fixtures/unsafe-continuation-fail-closed.swift",
    "mode": "runtime",
    "assertion": "interpreter-diagnostic",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await unsafeContinuationFailClosedProbe()",
    "interpreterProjection": "string",
    "interpreterDiagnosticContains": [
      "withUnsafeContinuation",
      "unsafe continuation ownership is unsupported",
      "use a checked continuation"
    ],
    "nativeFact": "Swift accepts withUnsafeContinuation with explicit MainActor isolation; its synchronous body resumes once with an exact value and the awaiting call returns that value.",
    "notes": "The body resumes immediately in program order, so exact native output value:33 makes no scheduler-order, physical-thread, or parallelism claim. This native-positive case is intentionally an interpreter-diagnostic boundary rather than behavioral parity: the uncited unsafe family is outside the demand depth cap and must fail closed before invoking its body or allocating continuation ownership."
  },
  {
    "id": "unsafe-throwing-continuation-fail-closed",
    "fixture": "Fixtures/unsafe-throwing-continuation-fail-closed.swift",
    "mode": "runtime",
    "assertion": "interpreter-diagnostic",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "try await unsafeThrowingContinuationFailClosedProbe()",
    "interpreterProjection": "string",
    "interpreterDiagnosticContains": [
      "withUnsafeThrowingContinuation",
      "unsafe continuation ownership is unsupported",
      "use a checked continuation"
    ],
    "nativeFact": "Swift accepts withUnsafeThrowingContinuation with explicit MainActor isolation; its synchronous body resumes once with an exact value and the awaiting call returns that value.",
    "notes": "The body resumes immediately in program order, so exact native output value:44 makes no scheduler-order, physical-thread, or parallelism claim. This native-positive case is intentionally an interpreter-diagnostic boundary rather than behavioral parity: the uncited unsafe family is outside the demand depth cap and must fail closed before invoking its body or allocating continuation ownership."
  },
  {
    "id": "checked-continuation-value-resume",
    "fixture": "Fixtures/checked-continuation-value-resume.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedContinuationValueResumeProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A one-shot checked continuation returns the exact value supplied by a detached producer after that producer yields, with explicit nil isolation from a nonisolated caller.",
    "notes": "The exact result is causal and makes no scheduling-order, physical-thread, or parallelism claim. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced 41 in twenty bounded native runs. The interpreter RED was unresolved identifier 'withCheckedContinuation'. Generated dispatch now routes the proven explicit-isolation-nil value subset through a task-owned bounded continuation record; the detached producer exercises resume from another source task. Focused evidence observes waitingForContinuation, required-executor restoration, one record, infrastructure-abort cleanup, and empty continuation/task/scope/group/stream/host-operation/scheduler registries. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 cdc0e05905b13ba3a3ef5a18fbefc271aedd047a639102d2b4ef965cc4f8c1a7."
  },
  {
    "id": "checked-continuation-double-resume",
    "fixture": "Fixtures/checked-continuation-double-resume.swift",
    "mode": "runtime",
    "assertion": "runtime-trap",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedContinuationDoubleResumeProbe()",
    "interpreterProjection": "string",
    "nativeTrapContains": [
      "SWIFT TASK CONTINUATION MISUSE",
      "tried to resume its continuation more than once"
    ],
    "interpreterTrapContains": [
      "checked continuation",
      "resumed more than once"
    ],
    "nativeFact": "A checked nonthrowing continuation accepts its first returning resume and traps the process synchronously when the same token is resumed a second time; the second value is never returned to source.",
    "notes": "Both resume calls are program-ordered in the continuation body, so the trap requires no scheduler, executor, physical-thread, or parallelism claim. This is the self-contained same-source adapter for the unchanged pinned swiftlang checked_continuation.swift oracle, whose StdlibUnittest test independently expects a crash for the same returning-then-returning misuse. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors; native and interpreted execution trapped in all twenty bounded isolated runs. This was an already-GREEN characterization: the source carrier already rejected the second resume as a fatal invariant, so no production change was required for this form. Focused evidence records one created record, no suspension, and complete ownership cleanup. Canonical native-observation SHA-256: 14011640abe8a0f135e2a1f33df41bcb1fd9839ed9365f7d30e03fb19e1ce64e."
  },
  {
    "id": "checked-continuation-abandonment",
    "fixture": "Fixtures/checked-continuation-abandonment.swift",
    "mode": "runtime",
    "assertion": "runtime-warning",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedContinuationAbandonmentProbe()",
    "interpreterProjection": "string",
    "interpreterCompletionPolicy": "cancel-remaining-tasks",
    "nativeWarningContains": [
      "SWIFT TASK CONTINUATION MISUSE",
      "checkedContinuationAbandonmentProbe() leaked its continuation without resuming it",
      "remain suspended forever"
    ],
    "interpreterWarningContains": [
      "SWIFT TASK CONTINUATION MISUSE",
      "checkedContinuationAbandonmentProbe() leaked its continuation without resuming it",
      "remain suspended forever"
    ],
    "nativeFact": "Dropping the last checked-continuation token without resuming logs a runtime misuse warning, does not trap, and leaves the task waiting on that continuation suspended indefinitely.",
    "notes": "Task.immediate causally executes the complete continuation body before returning its handle, so caller-returned plus the warning requires no sleep or scheduler-order assumption. Apple Swift 6.3.3 compiled the source in complete strict-concurrency mode with warnings as errors and produced exit 0, stdout caller-returned, and the same three warning fragments in twenty bounded runs. The native wrapper then exits its process; the interpreter wrapper applies cancelRemainingTasks only after the same source observation so the intentionally suspended task and continuation registry can be torn down. That host-boundary cleanup is not a source continuation result and does not alter the warning invariant. No physical-thread or parallelism claim is made."
  },
  {
    "id": "checked-throwing-continuation-abandonment",
    "fixture": "Fixtures/checked-throwing-continuation-abandonment.swift",
    "mode": "runtime",
    "assertion": "runtime-warning",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedThrowingContinuationAbandonmentProbe()",
    "interpreterProjection": "string",
    "interpreterCompletionPolicy": "cancel-remaining-tasks",
    "nativeWarningContains": [
      "SWIFT TASK CONTINUATION MISUSE",
      "checkedThrowingContinuationAbandonmentProbe() leaked its continuation without resuming it",
      "remain suspended forever"
    ],
    "interpreterWarningContains": [
      "SWIFT TASK CONTINUATION MISUSE",
      "checkedThrowingContinuationAbandonmentProbe() leaked its continuation without resuming it",
      "remain suspended forever"
    ],
    "nativeFact": "Dropping the last checked-throwing-continuation token without resuming logs a runtime misuse warning, does not enter source catch or trap, and leaves the task waiting on that continuation suspended indefinitely.",
    "notes": "Task.immediate causally executes the complete throwing-continuation body before returning its handle. Apple Swift 6.3.3 compiled the source in complete strict-concurrency mode with warnings as errors and produced exit 0, stdout caller-returned, and the same three warning fragments in twenty bounded runs; source catch is unreachable because abandonment is a warning and not an error result. This form shares the checked token canary and the nonthrowing fixture's captured missing-warning RED. The native wrapper exits its process, while the interpreter wrapper applies cancelRemainingTasks only after the source observation to drain the deliberately suspended runtime owner. No scheduler-order, physical-thread, or parallelism claim is made."
  },
  {
    "id": "checked-continuation-escaped-token-lifetime",
    "fixture": "Fixtures/checked-continuation-escaped-token-lifetime.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "try await checkedContinuationEscapedTokenLifetimeProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A checked continuation may escape its body and resume its suspended owner later. After that owner completes, retaining the already-resumed token does not retain the owner's task-local object; releasing the token is inert for both checked forms.",
    "notes": "Task.immediate causally reaches each unresumed continuation suspension before the caller inspects the escaped token and weak task-local sentinel. Explicit resume followed by await owner.value establishes task completion before the second weak-lifetime observation; the token remains stored until after that observation. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced true:47:true:true|true:53:true:true with empty stderr in twenty bounded runs. The interpreter was already GREEN before any production change: its continuation token already holds the runtime weakly and retains only the diagnostic sink. Focused host evidence keeps both resumed tokens alive after the source return, proves the continuation/task/session graph empty and both Interpreter and CooperativeConcurrencyRuntime released, then releases the tokens without a warning. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 7685db10bf77cdad7182f1297a12b536b1dfda617f8e182b78fe3f1892426a97. No sleep, ready-task order, physical-thread, or parallelism claim is made."
  },
  {
    "id": "checked-continuation-mainactor-resume",
    "fixture": "Fixtures/checked-continuation-mainactor-resume.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedContinuationMainActorResumeProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An explicit MainActor isolation argument hops a @concurrent nonisolated caller to MainActor for the checked-continuation body, then restores that caller's cooperative executor after a causally delayed detached producer supplies the value.",
    "notes": "The task-value gate forces the detached producer to suspend before a separate controller opens it. Exact output worker|main|worker proves caller entry, the contextually isolated continuation body, and caller-executor restoration after resume; no ready-task order, producer physical thread, or parallel execution is asserted. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced the exact output in twenty native runs."
  },
  {
    "id": "checked-throwing-continuation-value-error",
    "fixture": "Fixtures/checked-throwing-continuation-value-error.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedThrowingContinuationValueErrorProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Two sequential checked throwing continuations project the exact value supplied through resume(returning:) and the original source error supplied through resume(throwing:) into its matching catch.",
    "notes": "Each continuation has its own detached producer, which yields before resuming. Program order between the two awaits fixes value before error, but no producer scheduling order, physical thread, or parallel execution is asserted. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced value:23|error:failed in twenty native runs."
  },
  {
    "id": "checked-throwing-continuation-double-resume",
    "fixture": "Fixtures/checked-throwing-continuation-double-resume.swift",
    "mode": "runtime",
    "assertion": "runtime-trap",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedThrowingContinuationDoubleResumeProbe()",
    "interpreterProjection": "string",
    "nativeTrapContains": [
      "SWIFT TASK CONTINUATION MISUSE",
      "tried to resume its continuation more than once"
    ],
    "interpreterTrapContains": [
      "checked continuation",
      "resumed more than once"
    ],
    "nativeFact": "A checked throwing continuation accepts its first returning resume and traps the process synchronously when the same token is then resumed by throwing; the second terminal outcome cannot be caught as a source error.",
    "notes": "Both resume calls are program-ordered in the continuation body, so the trap requires no scheduler, executor, physical-thread, or parallelism claim. This is the self-contained same-source adapter for the unchanged pinned swiftlang checked_continuation.swift oracle, whose StdlibUnittest test independently expects a crash for the same returning-then-throwing misuse. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and trapped in twenty bounded runs. The interpreter RED was a successful child exit: async do/catch converted the fatal second-resume invariant into a source-catchable error. The common suspending do/catch boundary now rethrows every fatal RuntimeError, matching the synchronous evaluator and try? boundary; no continuation-name special case was added. Native/interpreter runtime-trap parity is 20/20 with canonical native-observation SHA-256 d9182563ed7f04da65516cc3b3cd55b5936278384e7cf2a8dc502e0eef4ce6ef. Focused evidence covers both checked forms, one record and zero suspensions per immediate misuse, and complete ownership cleanup."
  },
  {
    "id": "checked-throwing-continuation-mainactor-error",
    "fixture": "Fixtures/checked-throwing-continuation-mainactor-error.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedThrowingContinuationMainActorErrorProbe()",
    "interpreterProjection": "string",
    "nativeFact": "From a @concurrent nonisolated caller, MainActor isolation runs a checked-throwing continuation body on MainActor and a delayed source-error resume restores the caller executor before its matching catch.",
    "notes": "The task-value gate forces the detached producer to suspend before a controller opens it. Exact output worker|main|worker proves caller entry, contextual MainActor body execution, and cooperative caller restoration before source-error catch without asserting producer scheduling order, physical thread, or parallel execution. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced the exact output in twenty bounded native runs. This was an already-GREEN characterization: the shared contextual-executor invocation and failed continuation transition already composed correctly, so no production change was required. Focused evidence observes the cooperative required executor while the owner is parked, exact catch-lane restoration, one canonical suspension, and complete cleanup. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 cf35e0f2fff2d29dd27a7b7b026bc52ecd705ce15e72919ac8fc24ae7d3b17fe."
  },
  {
    "id": "checked-throwing-continuation-result-resume",
    "fixture": "Fixtures/checked-throwing-continuation-result-resume.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedThrowingContinuationResultResumeProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A checked throwing continuation maps a concrete Result success to its exact value and a concrete Result failure to its original source error through resume(with:).",
    "notes": "The two continuation calls are sequential, and each detached producer yields before resume(with:), so value precedes error causally without asserting producer scheduling order, physical thread, or parallel execution. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced value:29|error:failed in twenty bounded native runs. The interpreter RED timed out on repetition one because the detached producer rejected the unrouted with: label and left the owner parked. The common Result carrier adapter now delegates success and failure to the existing returning and throwing terminal transitions. Focused evidence records two canonical suspensions and complete cleanup, compiler preflight rejects a non-Result argument, and native/interpreter parity is 20/20 with canonical native-observation SHA-256 6400bcdc43bd6c03163fe44074b7e214559b9b36dbabb3a06017c268cb703c4e."
  },
  {
    "id": "checked-continuation-result-spellings",
    "fixture": "Fixtures/checked-continuation-result-spellings.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedContinuationResultSpellingsProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A nonthrowing checked continuation maps Result<Int, Never>.success to its exact value, while a checked throwing continuation maps existential Result<Int, any Error> success and failure to the exact value and original source error through resume(with:).",
    "notes": "The three continuation calls are sequential, and each detached producer yields before resume(with:), so the output is causally ordered without asserting producer scheduling order, physical thread, or parallel execution. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced never:31|any-success:37|any-error:failed in twenty bounded native runs. This was an already-GREEN characterization: the common Result carrier already delegated nonthrowing Result<Int, Never> success and existential Result<Int, any Error> success/failure to the established terminal transitions, so no production change was required. Focused evidence records three canonical suspensions, exact value/error projection, complete cleanup, and successful compiler preflight for both spellings. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 5983e5accbf237aacb90f6930d587fc78001990db22efdb2f89d538c7b4a22d2."
  },
  {
    "id": "checked-continuation-omitted-isolation",
    "fixture": "Fixtures/checked-continuation-omitted-isolation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedContinuationOmittedIsolationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "Omitted checked-continuation isolation evaluates the #isolation default in the caller's lexical context: nil preserves a @concurrent nonisolated caller lane, while MainActor.shared keeps a MainActor caller, body, and resumed catch on MainActor.",
    "notes": "The nonthrowing and throwing calls are sequential, and each detached producer yields before resume, so exact lane triples are causal without asserting producer scheduling order, physical thread, or parallel execution. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced nonisolated=worker:worker:worker|main=main:main:main in twenty bounded native runs. The interpreter RED rejected the first call because withCheckedContinuation required an explicit isolation argument. Omitted isolation now materializes #isolation once through the shared caller-lexical-isolation kernel before the existing nil/MainActor validation; it therefore preserves nonisolated nil and MainActor defaults without silently converting an arbitrary source actor to nil. Focused evidence covers two canonical suspensions, exact lane restoration, complete cleanup, and fail-closed omitted arbitrary-actor isolation before record creation. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 62b25d7d6a1744bcea392ca81dca119864b5725b5f893c232ee9fbccbe24ed7e."
  },
  {
    "id": "checked-continuation-source-actor-isolation",
    "fixture": "Fixtures/checked-continuation-source-actor-isolation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedContinuationSourceActorIsolationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An explicitly selected source actor owns the synchronous checked-continuation body and restores a nonisolated caller after resume; a caller-defaulted source actor retains body ownership, releases its mailbox while parked so another actor message runs, and reacquires it before continuation.",
    "notes": "The explicit producer yields before resume. In the caller-defaulted case, a detached controller can open the producer gate only after its actor method reenters and increments isolated state, so exact output explicit=worker:owned:worker|default=owned:owned:owned:1 proves release and reacquisition without asserting unrelated ready-task order, physical threads, or parallel execution. Dynamic native ownership uses Actor.assertIsolated through a parity helper rather than lexical #isolation. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced the exact output in twenty bounded runs. The interpreter RED rejected explicit source-actor isolation before creating a continuation because the entry adapter accepted only nil or MainActor. The shared suspending invocation kernel now accepts a contextual executor, acquires the selected source actor for the synchronous body, restores a different caller executor after the body, and lets the canonical continuation suspension release and later reacquire an already-owned source actor. Omitted isolation reaches the same path through caller-lexical #isolation. Focused evidence records two continuations, two canonical suspensions, causal actor reentry, exact caller/body/resume ownership, and empty actor/task/continuation/scope/group/stream/host-operation/scheduler registries. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 b919a36e3510d689feccceed310076dfe2168910cbc94a97b224c6658d585935."
  },
  {
    "id": "checked-throwing-continuation-source-actor-isolation",
    "fixture": "Fixtures/checked-throwing-continuation-source-actor-isolation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedThrowingContinuationSourceActorIsolationProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An explicitly selected source actor owns the synchronous checked-throwing-continuation body and restores a nonisolated caller before matching the resumed source error; a caller-defaulted source actor releases its mailbox while parked, permits another actor message, and reacquires it before source-error catch handling.",
    "notes": "The explicit producer yields before resuming with a source error selected from dynamic actor ownership. In the caller-defaulted case, a detached controller can open the producer gate only after its actor method reenters and increments isolated state, so exact output explicit=worker:owned:worker|default=owned:owned:failed:owned:1 proves source-error projection, mailbox release, and reacquisition without asserting unrelated ready-task order, physical threads, or parallel execution. Dynamic native ownership uses Actor.assertIsolated through a parity helper rather than lexical #isolation. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode and produced the exact output in twenty bounded native runs. This was an already-GREEN characterization: the existing contextual-executor invocation and failed continuation transition already composed for explicit and caller-defaulted source actors, so no production change was required. Focused evidence records two canonical suspensions, causal actor reentry, exact source-error catch restoration, and empty actor/task/continuation/scope/group/stream/host-operation/scheduler registries. Native/interpreter parity is 20/20 with canonical native-observation SHA-256 82eb9f8c66f3251029411784959e1746ce0e1565620a36da6e9fa781fe82c383."
  },
  {
    "id": "checked-continuation-void-resume",
    "fixture": "Fixtures/checked-continuation-void-resume.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await checkedContinuationVoidResumeProbe()",
    "interpreterProjection": "string",
    "nativeFact": "A checked continuation whose success value is Void resumes through the zero-argument resume() convenience and returns control to its awaiting owner.",
    "notes": "The detached producer yields before calling resume(), so the assertion covers delayed Void completion and owner resumption without asserting ready-task order, a physical thread, or parallel execution. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced void-resumed in twenty native runs."
  },
  {
    "id": "parallel-literal-detached",
    "fixture": "Fixtures/parallel-literal-detached.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelLiteralDetachedProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 2,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "Two Task.detached operations that return Sendable literals publish those exact values through their task handles after both values are awaited.",
    "notes": "The two handles are awaited in source order, but neither worker start nor completion order is asserted. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced exact atlas:42 in twenty bounded runs. The interpreter compile-RED had no validated parallel execution mode or physical-source execution receipt. In opt-in mode, each admitted literal-only operation is lowered on MainActor to an immutable snapshot kernel, executed by the shared bounded physical driver, and materialized back on MainActor; the closure, environment, RuntimeValue, heap, host bridge, and evaluator never cross the worker boundary. More complex closures retain the unchanged cooperative path."
  },
  {
    "id": "parallel-captured-string-count",
    "fixture": "Fixtures/parallel-captured-string-count.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelCapturedStringCountProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 2,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "A Task.detached operation may read an immutable captured String and publish its exact grapheme count through the task handle; the result does not depend on worker start or completion order.",
    "notes": "CotEditor demand-cites this exact spelling in Sources/Scanners/EditorCounter.swift. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced exact 5:9 in twenty bounded runs; every five-repetition native shard reported SHA-256 056cf461832cc0054882f24a52cbebddce0d96374179c21cc2f13e22eea43f3e. The interpreter behavioral value was already correct through cooperative fallback, but the captured RED proved explicit parallel mode recorded zero physical source executions instead of two. The common lowering now retains source let/var mutability on each Box, admits only a locally owned immutable String binding, copies it into RuntimeWorkerCapability on MainActor, and executes typed String.count IR on the bounded worker. Mutable bindings, globals, authored closure signatures, and every other expression remain cooperative; no Box, Environment, syntax, heap, host value, or evaluator crosses the worker boundary."
  },
  {
    "id": "parallel-shadowed-string-count",
    "fixture": "Fixtures/parallel-shadowed-string-count.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelShadowedStringCountOutput()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "A same-module computed property declared in an extension of String shadows the standard-library count property, including inside a Task.detached operation, so the task publishes the source property's exact Int value.",
    "notes": "CotEditor demand-cites detached String.count at Sources/Scanners/EditorCounter.swift:130. This gap-closure oracle proves that runtime String shape plus member spelling is not a declaration identity. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced exact 41 in twenty bounded runs; every five-repetition native shard reported SHA-256 e195c78d2738596cc170bb3277a29bf2f181174109dcad6afec0ab1a95b0033a. Before the fix, both cooperative and explicit-parallel interpretation incorrectly selected the standard-library grapheme-count property and returned 5; explicit parallel mode additionally recorded one physical execution. Core String source extensions now dispatch before imported native members. Physical lowering asks the originating RuntimeProgramState for a typed property-target identity and admits only the exact standard-library String.count target; source declarations and missing state fail closed. A retained two-run regression proves origin-state ownership while the positive stdlib probe retains its physical receipt. No worker identity or unrelated scheduler order is asserted."
  },
  {
    "id": "parallel-captured-string-array-count",
    "fixture": "Fixtures/parallel-captured-string-array-count.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelCapturedStringArrayCountProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 2,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "A Task.detached operation may read an immutable captured array of Substrings, map each value to its Swift grapheme count, reduce those counts by addition, and publish the exact sum through the task handle; the result does not depend on worker start or completion order.",
    "notes": "CotEditor demand-cites the exact selectedStrings.map(\\.count).reduce(0, +) spelling at Sources/Scanners/EditorCounter.swift:176, where selectedStrings is [Substring]. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced exact 6:10 in twenty bounded runs; every five-repetition native shard reported SHA-256 ddbe36d8251c2e1a3d3d558a0e0796e114031f85a98cde26c8536d13562ab0ed. The interpreter behavioral value was already correct through cooperative fallback, but the captured RED proved explicit parallel mode recorded zero physical source executions instead of two. MainActor lowering admits only a locally owned immutable array whose recursively copied elements are String snapshots and only the exact map(\\.count).reduce(0, +) spelling. The worker executes typed String-count-sum IR; mutable bindings, globals, alternate seeds, authored signatures, and every other expression remain cooperative. No Box, Environment, syntax, RuntimeValue, heap, host value, or evaluator crosses the worker boundary."
  },
  {
    "id": "parallel-detached-yield",
    "fixture": "Fixtures/parallel-detached-yield.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedYieldProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 2,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "A background-priority Task.detached body may suspend at Task.yield and its awaited Void value completes; the result does not identify a worker thread or constrain unrelated ready-task ordering.",
    "notes": "swift-composable-architecture demand-cites this exact spelling in Sources/ComposableArchitecture/TestStore.swift:1856. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and returned exact yielded:2 in twenty bounded runs; every five-repetition native shard reported SHA-256 a8da6accf02cddfd0779ab299c9ccaf8f75d2092aa1704824745f6c197af7e8c. The interpreter value was already correct through cooperative fallback, but the captured RED proved explicit parallel mode recorded zero physical source executions instead of two. MainActor lowering admits only a signature-free, argument-free, single-expression detached body whose expression is the exact zero-argument await Task.yield() call. It emits a typed task-yield kernel with an empty checked capability; the real detached worker suspends at Swift Task.yield and publishes a Void snapshot. Authored signatures, multiple statements, alternate calls, and every captured value remain cooperative. Twenty paired cooperative/parallel runs preserve exact yielded:2, empty task registries, and zero/two receipts. The two calls are sequential; no worker thread identity or unrelated ready-task order is asserted."
  },
  {
    "id": "parallel-shadowed-task-yield",
    "fixture": "Fixtures/parallel-shadowed-task-yield.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelShadowedTaskYieldProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "A local value named Task shadows the standard-library Task type inside its lexical scope, so `await Task.yield()` invokes that value's source method and returns its exact String result even when the enclosing operation was launched by Task.detached.",
    "notes": "This gap-closure oracle protects every spelling-based physical kernel from confusing a source call with a standard-library intrinsic. The local detached-operation factory is formed outside the shadowing scope, so the same source still launches a real native Task.detached operation. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and returned exact source in twenty bounded runs; every five-repetition native shard reported SHA-256 5f56add95e0689069a663ad8f90d7ec54eb7305fc06e321ef8ca348d55017328. No worker identity or unrelated scheduler order is asserted. Before the fix, cooperative interpretation returned source while explicit parallel mode admitted the inner call by raw AST spelling, returned Void, and recorded one physical execution. After the fix, parallel interpretation also returns source and records zero physical submissions or executions."
  },
  {
    "id": "parallel-detached-conditional-sleep",
    "fixture": "Fixtures/parallel-detached-conditional-sleep.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedConditionalSleepParityProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 2,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "A Task.detached body may capture an immutable Bool parameter, select between two finite Duration literals, and suspend in throwing Task.sleep(for:) before publishing Void. Cancelling a second task before its body enters makes the sleep throw CancellationError and leaves its handle marked cancelled; elapsed time, worker identity, and unrelated scheduler order are not guaranteed.",
    "notes": "Signal-iOS demand-cites `try await Task.sleep(for: slowLinking ? .seconds(3) : .milliseconds(500))` in BackupRestoreProgressModal.swift:541, where slowLinking is an immutable Bool parameter. The same-source fixture uses zero/one-millisecond success durations plus a causally pre-cancelled 30-second branch so the assertion is bounded and does not treat timing as semantics. Apple Swift 6.3.3 compiled it in Swift 6 complete-strict mode with warnings as errors and returned exact slow:fast|cancelled:true in twenty bounded runs; every focused five-run shard reported SHA-256 d78d19bf880bb60f640e843e2e59091db7555910efa54f0ed50b4b5e8d62ac95. The captured receipt RED already returned the correct value through cooperative fallback but explicit parallel mode recorded zero successful physical source executions instead of two. MainActor now admits only plain `try await Task.sleep(for:)` with a directly owned immutable Bool condition and nonnegative integer-literal `.seconds`/`.milliseconds` branches, then copies the Bool and emits typed conditional-Duration plus sleep IR. Other units, overloads, mutable/global conditions, authored signatures, and richer bodies remain cooperative. A Sendable actor relay preserves pre-cancelled body entry and forwards cancellation to the actual source worker, so the throwing sleep produces CancellationError. Twenty paired mode runs preserve the exact value, empty registries, zero/two successful executions, and zero/three submissions. The TSan board passed twenty native overlap iterations and all thirty-three driver/source-kernel tests on four workers in 23 seconds without a race or interceptor diagnostic; no elapsed time, worker identity, or scheduler order is asserted."
  },
  {
    "id": "detached-source-member-call-target",
    "fixture": "Fixtures/detached-source-member-call-target.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await detachedSourceMemberCallTargetProbe()",
    "interpreterProjection": "string",
    "nativeFact": "An explicit self member call inside Task.detached selects the captured MainActor-isolated instance method, suspends through its async body, and publishes that method's exact value rather than a same-named global function.",
    "notes": "FoodTruck demand-cites the exact Task.detached { await self.updatesLoop() } shape in StoreMessagesManager.swift:33-35. The exact output is target:member; worker identity and unrelated scheduler order are not asserted. This already-GREEN behavior first anchored immutable callee-shape metadata and now anchors an origin-bound Sendable descriptor for the uniquely shape-resolved own reference method. Same-shape overloads remain unresolved, the source method stays on the confined evaluator with zero physical receipts, and no Instance, RuntimeProgramState, closure, environment, or evaluator crosses a worker boundary."
  },
  {
    "id": "detached-explicit-mainactor-closure",
    "fixture": "Fixtures/detached-explicit-mainactor-closure.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await detachedExplicitMainActorClosureProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "An explicit @MainActor detached-operation closure is MainActor-isolated before and after suspension, while a plain Task.detached operation formed in the same MainActor function remains nonisolated across the same suspension.",
    "notes": "Planet demand-cites `Task.detached { @MainActor in ... }` at Planet/Entities/PlanetStore.swift:246, and the pinned external corpus contains 57 matching operation shapes across Planet, Provenance, and apple-browsers. Apple Swift 6.3.3's complete-strict region checker emits an internal checker diagnostic for that exact spelling; adding explicit `@Sendable`, which is already required semantically by Task.detached and has no executor effect, is the minimal native-oracle workaround. The strict fixture then returned exact same|same#none|none in twenty bounded runs; every focused five-run native-observation shard retained canonical SHA-256 032c610c796c0f579b3af0c81e114c283f50d2ce0e576bb16089ef843ef1da7a. The deterministic interpreter RED returned same|same#same|same: explicit isolation happened to look correct because every anonymous closure inherited its lexical MainActor, but the plain detached operation incorrectly kept that actor. Closure formation now records explicit MainActor or retains user-global-actor candidates for the existing lazy resolver independently from lexical inheritance. Ordinary detached task entry suppresses inherited anonymous-closure isolation while preserving an explicit closure executor; Task, Task.immediate, and Task.immediateDetached retain their separate inheritance rules. The authored signature remains cooperative with zero physical receipts. No worker identity or unrelated scheduler order is asserted."
  },
  {
    "id": "detached-sendable-closure-isolation",
    "fixture": "Fixtures/detached-sendable-closure-isolation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await detachedSendableClosureIsolationProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "A closure-signature @Sendable attribute does not select an executor: Task still inherits its MainActor formation context, while Task.detached remains nonisolated before and after suspension.",
    "notes": "The pinned external corpus demand-cites the exact `Task.detached { @Sendable in ... }` spelling in swift-composable-architecture's CurrentValueRelayTests.swift:38 and the priority-bearing form in CotEditor's FileNode.swift:410. Apple Swift 6.3.3 compiled the same source in Swift 6 complete-strict mode with warnings as errors and returned exact same|same#none|none in twenty bounded runs; every focused five-run native-observation shard retained canonical SHA-256 c1c7528bed014c8a1065d919d0b0813aad3cb8170d5d495fd0968c2812edeaa4. The interpreter was already GREEN after the preceding closure-executor fix, so this is behavioral characterization with no production change. Runtime-inert `@Sendable` leaves API-selected executor policy intact: Task inherits the MainActor lexical context, while Task.detached suppresses it. Both authored signatures remain cooperative with zero physical receipts. No Sendable checking, worker identity, or unrelated scheduling order is asserted."
  },
  {
    "id": "detached-mainactor-weak-self",
    "fixture": "Fixtures/detached-mainactor-weak-self.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await detachedMainActorWeakSelfProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "An explicit MainActor detached-operation closure remains MainActor-isolated across suspension while its weak self capture remains non-owning; a receiver released during a causal suspension is absent on resume.",
    "notes": "Provenance demand-cites 11 exact `Task.detached { @MainActor [weak self] in ... }` operation shapes, including DeltaSkinInputHandler.swift:3064 and two priority-bearing forms. Apple Swift 6.3.3's complete-strict region checker emits an internal checker diagnostic for that exact spelling; the fixture adds executor-neutral `@Sendable`, then compiles with warnings as errors. Twenty bounded native runs returned exact same|same:alive#same|same:released; the standalone twenty-line output had SHA-256 8f8952506fd713e4c83ea12ceeb60b04bdca0851bde0d3068fb4e1bac0966d38, and every focused five-run native-observation shard retained canonical SHA-256 768eb94127084dde469655732899dc1af00bd57c0f04a710c4edbc8e939c46d6. An actor gate causally proves the detached operation has entered and suspended before the parent clears its final strong receiver. The earlier behavioral characterization was already GREEN; this physical gap closure captured a receipt-only RED of zero submissions/executions instead of two. Closure formation now admits only exact `[weak self]`, ordered `@MainActor` with the optional native-oracle `@Sendable` workaround, and no parameter/effect/return or additional capture surface. The existing empty wrapper and confined continuation token retain the complete closure and genuine weak box on MainActor; only entry/task identity crosses the worker boundary. The exact corpus spelling without `@Sendable` records a physical receipt, while additional captures, reversed attributes, and a source-shadowed MainActor stay cooperative. Focused parity passed 20/20 on four workers in one second, the complete source-kernel suite passed 49/49, and the canonical prebuilt iteration passed those 49 implementation tests plus all 46 methodology/gate checks and all 20 parity repetitions in two seconds. The exact-tip physical board passed 92/92 tests in three suites in one second; a fresh TSan build passed native overlap 20/20 plus all 92 tests in 47 seconds without diagnostics. No worker identity or unrelated scheduling order is asserted."
  },
  {
    "id": "detached-mainactor-weak-capture",
    "fixture": "Fixtures/detached-mainactor-weak-capture.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await detachedMainActorNamedWeakCaptureProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "An explicit MainActor detached-operation closure with one named weak capture remains actor-isolated across suspension without retaining that capture; a referent released during a causal suspension is absent on resume.",
    "notes": "KeyboardCowboy demand-cites the exact `Task.detached { @MainActor [weak notifications] in ... }` spelling in WorkflowRunner.swift:50-52. Apple Swift 6.3.3's complete-strict region checker emits an internal checker diagnostic for that exact spelling, so the same-source fixture adds executor-neutral `@Sendable` and compiles with warnings as errors; a separate interpreter probe retains the exact corpus spelling. Twenty bounded native runs returned exact same|same:alive#same|same:released; the standalone twenty-line output had SHA-256 8f8952506fd713e4c83ea12ceeb60b04bdca0851bde0d3068fb4e1bac0966d38. An actor gate causally proves body suspension before the parent clears the final strong reference. Cooperative and parallel interpreter modes already returned the exact value, but the deterministic receipt RED recorded zero physical submissions/executions instead of two. The existing explicit-MainActor weak-self admission is generalized by capture semantics rather than identifier spelling: one exact weak capture may use the empty wrapper and confined continuation token, while the complete closure and genuine weak box remain confined. Capture aliases/initializers, unowned ownership, additional captures, reversed attributes, and source-shadowed MainActor stay cooperative. Every five-run focused shard retained SHA-256 7491365c8c4a8fa386bd87ee3d2ac9ab4af932eced41a0597eea8945526ef8fd. Focused parity passed 20/20 on four workers in one second, the complete source-kernel suite passed 50/50, and the canonical prebuilt iteration passed those 50 implementation tests plus all 46 methodology/gate checks and all 20 parity repetitions in two seconds. The exact-tip physical board passed 93/93 tests in three suites in one second; a fresh TSan build passed native overlap 20/20 plus all 93 tests in 51 seconds without diagnostics. No worker identity or unrelated scheduling order is asserted."
  },
  {
    "id": "detached-mainactor-mixed-captures",
    "fixture": "Fixtures/detached-mainactor-mixed-captures.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await detachedMainActorMixedCaptureProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "An explicit MainActor detached operation with one strong and two weak captures preserves actor isolation and the authored ownership edges across suspension: the strong referent remains while causally released weak referents disappear.",
    "notes": "apple-browsers demand-cites the exact `Task.detached { @MainActor [responders, weak webView, weak webViewDeinitObserver] in ... }` spelling in DistributedNavigationDelegate.swift:133. Apple Swift 6.3.3's complete-strict region checker emits an internal checker diagnostic for the exact spelling, so the same-source fixture adds executor-neutral `@Sendable` and compiles with warnings as errors; a separate interpreter probe retains the unmodified corpus spelling. Twenty bounded native runs returned exact same|same:responders:alive:alive#same|same:responders:released:released; the standalone twenty-line output had SHA-256 1a85049a3ab02941d13c7bf7b3bef253460ee7d5727b66a7f9ffff78c229eb1c. An actor gate causally proves suspension before the parent releases all three references: the strong capture still reads responders while both weak captures are nil. Cooperative and parallel interpreter modes already returned the exact value, but the deterministic receipt RED recorded zero physical submissions/executions instead of two. Closure formation now publishes one typed explicit-MainActor continuation-signature capability for the demand-backed captureless, single-weak, and exact strong/weak/weak shapes instead of accumulating corpus-specific booleans. Only entry/task identity crosses the empty wrapper; the complete closure and capture boxes remain confined. Reordered ownership, shorter lists, aliases/initializers, other attributes, and source-shadowed MainActor stay cooperative. Every five-run focused shard retained SHA-256 332be7a83579da5bd920d679296ff7a338a14538922b2fc103b3ff84a50d2f3c. Focused parity passed 20/20 on four workers in one second, the complete source-kernel suite passed 52/52, and the canonical prebuilt iteration passed those 52 implementation tests plus all 46 methodology/gate checks and all 20 parity repetitions in two seconds. The exact-tip physical board passed 95/95 tests in three suites in one second; a fresh TSan build passed native overlap 20/20 plus all 95 tests in 56 seconds without diagnostics. No worker identity or unrelated scheduling order is asserted."
  },
{
  "id": "detached-contextual-task-static-member",
  "fixture": "Fixtures/detached-contextual-task-static-member.swift",
  "mode": "runtime",
  "assertion": "exact",
  "repetitions": 20,
  "timeoutSeconds": 5,
  "interpreterEntry": "await detachedContextualTaskStaticMemberProbe()",
  "interpreterProjection": "string",
  "interpreterExecutionMode": "parallel",
  "interpreterMaximumParallelism": 1,
  "expectedPhysicalSourceKernelExecutions": 0,
  "nativeFact": "A contextual .detached call in a Task-returning function resolves to Task.detached, starts its operation, and preserves detached nonisolation before and after suspension.",
  "notes": "Swiftfin demand-cites the exact task = .detached(priority: .userInitiated) operation spelling at DefaultsObservable.swift:41. Strict Apple Swift 6.3.3 compiled the contextual static-member fixture with complete concurrency checking and warnings as errors; twenty bounded native runs returned exact none|none with SHA-256 a659177e21f1577dd1f402a481e88a0cdc39546ca94bc7cbe6a56c48c39c43de. Before the fix, a minimal deterministic RED returned a non-string contextual marker instead of launched, while the causal Swiftfin-shaped probe could wait forever because the operation never started. Expected-type annotation resolution now asks the annotated imported nominal for its real static member and invokes it through the existing generated member gateway. Source enum/struct statics and host-type extensions retain precedence; unknown imported statics retain the typed-marker fallback. Both cooperative and explicit parallel modes return none|none, while the multi-expression body remains cooperative with zero physical receipts. Every five-run focused shard retained SHA-256 0e8e7cc7779ed16ceef55632d5372244b64e74f7c12bae42e3c23cd8f055a0bb. Focused parity passed 20/20 on four workers in one second, the source-kernel suite passed 54/54, and the canonical prebuilt iteration passed those 54 implementation tests plus all 46 methodology/gate checks and all 20 parity repetitions in two seconds. The exact-tip physical board passed 97/97 tests in three suites in one second; a fresh TSan build passed native overlap 20/20 plus all 97 tests in 27 seconds without diagnostics. No scheduler order or worker identity is asserted."
},
  {
    "id": "parallel-detached-mainactor-source-call",
    "fixture": "Fixtures/parallel-detached-mainactor-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedMainActorSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "A detached task whose complete body awaits an argument-free MainActor-isolated method on captured self hops to MainActor, preserves mutations of that isolated receiver, and observes a cancellation request made before the hop without suppressing method entry.",
    "notes": "FoodTruck demand-cites `Task.detached { await self.updatesLoop() }` at StoreMessagesManager.swift:33-35. Both source methods in this bounded probe return Void, yield once on MainActor, and publish only causally controlled state: the first increments an isolated counter, while the second is cancelled before the caller yields MainActor and therefore records Task.isCancelled as true. Apple Swift 6 complete-strict compilation with warnings as errors and the interpreter returned exact 1:true in twenty bounded runs; every five-run native shard reported SHA-256 5a14c888baeb47a0a83163bf1680a3fdcc9a508e7b5aa5bf4191aad7390e1d3a. The receipt RED already returned that value through cooperative fallback but explicit parallel mode recorded zero physical source executions instead of two. Physical lowering admits only a signature-free one-expression direct-self call whose runtime-selected own source-class method is uniquely shape-resolved, async, nonthrowing, MainActor-isolated, origin-plan matched, and Void- or String-returning. A typed Sendable command carries only entry/task IDs, exact target descriptor, and result kind; the physical wrapper calls a purpose-built MainActor relay that recovers the confined closure from RuntimeTaskRecord and reinstalls its EvaluationTaskContext. The wrapper releases its worker permit at confined-executor entry, so FoodTruck's long-lived updates loop cannot starve later finite kernels. Instance, closure, Environment, RuntimeProgramState, heap, and evaluator never enter the worker capability. Actor, inherited/nonisolated, non-self, throwing, mutable/expression/opaque argument forms, ambiguous, foreign-origin, and richer-result calls remain cooperative. The expanded TSan board passed native overlap 20/20 and all 48 driver/kernel/source-call tests in 66 seconds without a race or interceptor diagnostic."
  },
  {
    "id": "parallel-detached-concurrent-source-call",
    "fixture": "Fixtures/parallel-detached-concurrent-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedConcurrentSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "A detached task can call an @concurrent nonisolated async source method on captured self with labeled scalar arguments; that method has nil lexical isolation, may hop to MainActor for receiver mutation, and returns its exact value.",
    "notes": "TaskObservatory demand-cites three direct-self @concurrent nonisolated calls with labeled Int/Int64 arguments in TaskObservatoryStore.start(). The bounded probe passes two integer literals, yields on the default executor, observes nil #isolation through a defaulted isolated parameter, hops to MainActor to record state, and returns exact 7:11|18:none. Apple Swift 6 complete-strict compilation with warnings as errors and the interpreter returned that value in twenty bounded runs; every five-run native shard reported SHA-256 34d58dd88621c8d3b3208bf3323e40b36c7d84323ee01c44574ee52ebcef4315. The receipt RED already returned that value through cooperative fallback but explicit parallel mode recorded zero physical source executions instead of one. Admission is limited to integer literals or directly owned immutable Int/Int64 captures matched to nondefaulted, nonvariadic, nonisolated Int/Int64 parameters; labels and checked binding IDs travel in the Sendable command while CallArguments, source boxes, the receiver closure, and evaluator remain confined. Mutable captures, expression arguments, captured Boolean, String, and other unsupported scalar arguments, explicit ordinary nonisolated methods, actors, throwing calls, ambiguous targets, and richer results remain cooperative."
  },
  {
    "id": "parallel-detached-mainactor-bool-source-call",
    "fixture": "Fixtures/parallel-detached-mainactor-bool-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedMainActorBooleanSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "Two sequentially awaited detached tasks may pass true and false literals to a MainActor-isolated async source method, preserve that actor's exact mutation order, and return both exact results.",
    "notes": "iTorrent demand-cites the exact one-line `Task.detached(priority: .utility) { await self.invalidateBackgroundState(true/false) }` spellings at WebServerService.swift:133 and 138. The bounded probe awaits the true and false calls sequentially, yields inside the selected MainActor method, records isolated state, and returns exact on:off|TF without asserting worker identity or unrelated scheduler order. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned that value in twenty bounded runs; every five-run native shard reported canonical SHA-256 ec0dfbfcbd3bbeab6dd3c5728b5da0face87e81f2b3be963f6d9d1acfa617bf6. The receipt RED already returned the exact value through cooperative fallback but recorded zero physical submissions/executions instead of two. The common source-call command now carries an explicit integer-or-Boolean value kind, the relay verifies it against each copied snapshot, and admission verifies the corresponding Int/Int64/Bool parameter type before confined re-entry. Boolean support is depth-capped to literals; captured Bool, expression, String, mutable, ambiguous, actor, throwing, and richer-result forms remain cooperative."
  },
  {
    "id": "parallel-detached-actor-source-call",
    "fixture": "Fixtures/parallel-detached-actor-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedActorSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "An actor initializer may launch a detached task that awaits a synchronous Void method on the captured actor; a later actor method that yields while waiting releases the mailbox, the detached call enters that mutually exclusive executor, and its exact mutation becomes visible on resumption.",
    "notes": "Session-iOS demand-cites `Task.detached(priority: .utility) { await self.loadExtensionLogsAndResumeLogging() }` in SessionUtilitiesKit/General/Logging.swift:638, directly from the Logger actor initializer; the selected method is synchronous, actor-isolated, argument-free, and Void-returning, and the handle is not retained. The bounded same-source probe preserves that launch shape. Its result method causally waits with Task.yield until the detached wrapper re-enters the actor and returns exact actor|R. Apple Swift 6.3.3 compiled it in complete-strict Swift 6 mode with warnings as errors and returned that exact value in twenty bounded runs; every native five-run shard reported canonical SHA-256 ee46339758b9bf4f9030cfb7cc9db448e6b2c961004386a9c262fe21f5ce57cc. The receipt RED already returned the correct value through cooperative fallback but recorded zero physical submissions/executions instead of one. Admission is limited to a uniquely resolved, argument-free, synchronous, nonthrowing, Void-returning own method isolated to the exact receiver actor and using the default actor executor. The worker carries no actor, instance, closure, environment, heap, or evaluator: the confined relay restores the logical task context and ordinary suspending invocation acquires the exact actor mailbox. Async actor methods outside the separate single-Int Void and explicit single-defaulted-Bool Void subsets, synchronous argument-bearing actor calls, String-returning actor methods, nonisolated methods, custom actor executors, ambiguity, throwing calls, and richer results remain cooperative."
  },
  {
    "id": "parallel-detached-async-actor-int-source-call",
    "fixture": "Fixtures/parallel-detached-async-actor-int-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedAsyncActorIntegerSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "A detached task may copy an immutable Int into a labeled argument of an async method isolated to its captured actor; that method retains mutually exclusive actor ownership before suspension, releases it at Task.yield, reacquires it before continuation, and publishes the exact ordered mutations.",
    "notes": "Planet demand-cites `Task.detached(priority: .utility) { await self.handleDaemonTermination(terminationStatus: ret) }` in IPFSDaemon.swift:371-373, where ret is an immutable Int callback parameter and the selected actor method is async, nonthrowing, and Void-returning. The bounded same-source probe captures status 17, records start:17 before Task.yield, then records done:17 only after actor-mailbox reacquisition. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact start:17|done:17 in twenty bounded runs; every native five-run shard reported canonical SHA-256 b2ba89617abb88d104c2131843423923d4bbf7b369d08f05192dd8985b01325a. The receipt RED already returned the exact value through cooperative fallback but recorded zero physical submissions/executions instead of one. The actor route reuses the checked integer snapshot and label command, confined EvaluationTaskContext relay, and canonical suspending actor invocation; admission is capped to exactly one Int/Int64 argument for an async Void actor method. Boolean calls outside the separate explicit single-defaulted-Bool subset, String, multiple, other defaulted, mutable, expression, throwing, custom-executor, and richer-result actor calls remain cooperative."
  },
  {
    "id": "parallel-detached-async-defaulted-actor-bool-source-call",
    "fixture": "Fixtures/parallel-detached-async-defaulted-actor-bool-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedAsyncDefaultedActorBooleanSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "A detached task may explicitly pass a Boolean literal to a defaulted Bool parameter of an async method isolated to its captured actor; that method owns the actor before suspension, releases it at Task.yield, reacquires it before continuation, and publishes the exact ordered mutations.",
    "notes": "Planet demand-cites Task.detached(priority: .utility) { await self.setupIPFS(andLaunch: true) } in IPFSDaemon.swift:335-337; setupIPFS(andLaunch:) is an async, nonthrowing, Void-returning actor method whose Bool parameter defaults to false but is explicitly supplied at this call site. The bounded same-source probe records start:true before Task.yield and done:true only after actor-mailbox reacquisition. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact start:true|done:true in twenty bounded runs; every native five-run shard reported canonical SHA-256 1b0355375aa6a812ce51840fb7f3c38c1c21320283d5e013d0cd27e5c30e1abe. The receipt RED already returned the exact value through cooperative fallback but recorded zero physical submissions/executions instead of one. Admission is signature-shaped: one explicitly supplied Boolean literal may target one defaulted Bool parameter of an async Void method on the exact default actor. Omitted defaults, captured Bool, defaulted integer parameters, nondefaulted Boolean parameters, String, multiple, mutable, expression, throwing, custom-executor, and richer-result actor calls remain cooperative."
  },
  {
    "id": "parallel-detached-custom-global-actor-source-call",
    "fixture": "Fixtures/parallel-detached-custom-global-actor-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedCustomGlobalActorSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "An unretained detached task may invoke an argument-free async Void method on a source class through a user-defined global actor; the method has that exact canonical actor isolation before suspension and after reacquiring it following Task.yield.",
    "notes": "Provenance demand-cites Task.detached(priority: .utility) { await self.createSystemDirectories() } at BIOSDirectoryWatcher.swift:79-80; the selected source-class method is argument-free, async, nonthrowing, Void-returning, and annotated with the actor-declared FileSystemActor at lines 109-110. The bounded same-source probe makes its result method wait on the same global actor, requiring mailbox release for the unretained wrapper, and returns exact same|same from #isolation identity checks before and after Task.yield. Apple Swift 6.3.3 complete-strict compilation with warnings as errors and interpreted execution returned that value in twenty bounded runs. Every native five-run shard reported canonical SHA-256 fcb1cc9c933c78c04ad6becc131ea2f7d8ca50a23b090f5336dbcb64c0be6261. The receipt RED already returned the exact value through cooperative fallback but recorded zero physical submissions/executions instead of one. Admission performs only declaration lookup for one uniquely matching source @globalActor actor with the default executor; it does not read static shared or initialize source state. Ordinary confined invocation resolves the canonical shared actor and owns its mailbox. Argument-bearing or String-returning custom-global-actor methods, struct/enum-backed global actors, custom executors, throwing calls, and richer results remain cooperative."
  },
  {
    "id": "parallel-detached-inherited-source-call",
    "fixture": "Fixtures/parallel-detached-inherited-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedInheritedSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "A plain async Void method on an @unchecked Sendable source class inherits the detached caller's nil actor isolation, retains nil isolation across Task.yield, and may complete an unretained detached wrapper without an actor hop.",
    "notes": "iTorrent demand-cites the one-expression Task.detached(priority: .utility) { await self.refreshWebServerState() } and refreshWebDavServerState() spellings in WebServerService.swift:120 and 128; WebServerService is @unchecked Sendable and both selected own source-class methods are plain async, nonthrowing, argument-free, and Void-returning at lines 198 and 222. The bounded same-source probe launches an unretained wrapper and reports #isolation before and after Task.yield without asserting a thread or scheduler order. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact none|none in twenty bounded runs; every native five-run shard reported canonical SHA-256 dc022b9fd32ef23613a6bf01ee0af601d88cf1f8fce887c8924f7047de1bd4b4. The receipt RED already returned that exact value through cooperative fallback but recorded zero physical submissions/executions instead of one. Admission is capped to an argument-free async nonthrowing Void own source-class method with inherited caller isolation; synchronous, argument-bearing, String-returning, explicitly nonisolated, throwing, ambiguous, and richer-result forms remain cooperative."
  },
  {
    "id": "parallel-detached-inherited-string-source-call",
    "fixture": "Fixtures/parallel-detached-inherited-string-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedInheritedStringSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "A detached wrapper may pass one labeled immutable String capture to an inherited-isolation async nonthrowing Void source method; the copied value is exact and the method retains nil actor isolation across suspension.",
    "notes": "Planet demand-cites `Task.detached(priority: .background) { await self.sendNotificationForNewCID(cid: cid) }` at MyPlanetModel.swift:2948-2950; the optional-binding `cid` is immutable and the selected own method takes one labeled String, is async/nonthrowing/Void, and has inherited isolation at line 3850. The strict same-source probe uses an @unchecked Sendable receiver, returns exact bafy-planet:none|none in twenty bounded native/interpreter runs, and every focused five-run shard retains canonical SHA-256 2e15021f8c242902f4ed71d5b4dd1a16a4cc66ec7ca6135550bd1b58782e99a0. The deterministic receipt RED already returned that value through cooperative fallback but recorded zero physical submissions/executions instead of one. The general checked source-call argument schema now carries a string kind, recursively copies only a directly owned immutable String capture, validates the selected String parameter and worker snapshot, and materializes the argument only after confined re-entry. Admission is capped to one inherited-isolation async nonthrowing Void method; String literals, mutable captures, MainActor/@concurrent/actor routes, multiple arguments, throwing methods, and richer results remain cooperative."
  },
  {
    "id": "parallel-detached-inherited-try-optional-source-call",
    "fixture": "Fixtures/parallel-detached-inherited-try-optional-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedInheritedTryOptionalSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "An argument-free inherited-isolation async throwing Void source method called as try? from a detached wrapper keeps nil actor isolation across suspension; success becomes Optional.some(Void), while an ordinary source throw is contained as Optional.none.",
    "notes": "Meshtastic-Apple demand-cites the exact nested `Task.detached(priority: .utility) { try? await self.refreshDevicesAPIData() }` spelling at MeshtasticAPI.swift:136-138; MeshtasticAPI is an @unchecked Sendable source class and the selected own method at line 199 is inherited-isolation, async, throwing, argument-free, and Void-returning. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact success:none|none#some|failure:none|none#nil in twenty bounded native/interpreter runs; every native five-run shard retained canonical SHA-256 bea8145a5762b64cfca390d6d8247e3f3c6d695e8d6a1a7d22bc294251068b9e, and the standalone twenty-line native output had SHA-256 99d9d9b6aa87c21d6bf64a993ab7eb29a2969a115f8210328f4073dc8e9b64d9. The deterministic receipt RED already returned the exact value through cooperative fallback but recorded zero physical submissions/executions instead of two. A typed suppress-to-optional command disposition catches ordinary errors during MainActor-confined re-entry, so InterpretedThrow.value never crosses the worker boundary and only Optional<Void> snapshots return. Fatal RuntimeError and InterpreterSessionAbort remain unsuppressed. Admission is capped to the exact direct-self argument-free inherited-isolation async throwing Void route; plain try, try!, arguments, richer results, MainActor, @concurrent, and weak-self variants remain cooperative."
  },
  {
    "id": "parallel-detached-try-optional-sleep-prefix",
    "fixture": "Fixtures/parallel-detached-try-optional-sleep-prefix.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedTryOptionalSleepPrefixProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "A detached task that contains `try? await Task.sleep(for:)` followed by another statement suppresses sleep cancellation and executes that continuation in the same cancelled task; the continuation observes `Task.isCancelled == true`, and the handle completes successfully while retaining its cancellation bit.",
    "notes": "FreeChat demand-cites `Task.detached(priority: .userInitiated) { try? await Task.sleep(for: .seconds(1)); await submit(input) }` at ConversationView.swift:228-231. The bounded same-source fixture executes a zero-duration success branch and immediately cancels a 30-second branch, so cancellation rather than elapsed time opens the second continuation. Apple Swift 6.3.3 compiled it in complete-strict Swift 6 mode with warnings as errors and returned exact completed:false,cancelled:true|false:true in twenty bounded native/interpreter runs; every five-run native shard retained SHA-256 0a5b2906a7b66290e031388b4917c98fc54cbd9670544a3d5a05930982e24523, and the standalone twenty-line observation had SHA-256 21dcdc5c10474172ee5c69f6c6fb8fbdbcb4f2a72619d013e3cd30273145d14e. The deterministic receipt RED already returned the exact value through cooperative fallback but recorded zero physical submissions/executions instead of two. Admission is capped to an exact signature-free two-item body whose first item is a registered core-Task `try?` sleep over a nonnegative integer-literal seconds/milliseconds duration and whose second item is one awaited expression. The worker executes only the sleep. RuntimeTaskRecord retains the suffix and its complete RuntimeValue/Error outcome behind a Sendable entry/task token; cancellation reaches the actual worker sleep, and the bounded permit is released before confined re-entry under the originating EvaluationTaskContext. Plain try, try!, alternate overloads/units, three-item/reversed bodies, and authored signatures remain cooperative. Four focused regressions cover parity, nested-work liveness, negative admission, and fatal-trap containment; the exact-tip physical board passed 78 tests in three suites in one second, and the rebuilt scoped TSan board passed native overlap 20/20 plus all 78 tests in 60 seconds without a race or interceptor diagnostic."
  },
  {
    "id": "parallel-detached-try-optional-nanoseconds-sleep-prefix",
    "fixture": "Fixtures/parallel-detached-try-optional-nanoseconds-sleep-prefix.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedTryOptionalNanosecondsSleepPrefixProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "A detached task whose `try? await Task.sleep(nanoseconds:)` is followed by `MainActor.run` suppresses sleep cancellation and executes the MainActor continuation in the same cancelled task; the continuation observes the cancellation bit and the handle completes successfully.",
    "notes": "Provenance demand-cites `Task.detached(priority: .utility) { [weak self] in try? await Task.sleep(nanoseconds: 100_000_000); await MainActor.run { self?.setupPrimarySubscriptions() } }` at ImportProgressViewModel.swift:132-136. The bounded same-source fixture retains that weak-capture, literal-nanoseconds, and MainActor-run shape with zero-duration success and immediately cancelled 30-second branches. Strict Apple Swift 6.3.3 returned exact `completed:false,cancelled:true|false:true` in twenty bounded runs; the standalone observation digest was 21dcdc5c10474172ee5c69f6c6fb8fbdbcb4f2a72619d013e3cd30273145d14e and every five-run focused shard retained 432229c2edb0bd47e62066c636a5d17499311877f8ea718aa1af5c2ddd3f0263. Before the fix, parallel mode recorded zero physical receipts and both interpreter modes returned `|false:true` because imported MainActor.run degraded to an inert member marker. The active _Concurrency.swiftinterface now generates an exact MainActor.run intrinsic; same-named source types retain source precedence, explicit resultType plus generated-but-unrouted MainActor members fail closed, and only a nonnegative nanoseconds integer literal in the cited two-item weak-self closure is physically admitted. The exact-tip physical board passed 82/82 in three suites; a fresh TSan build passed native overlap 20/20 plus all 82 tests in 32 seconds without a race or interceptor diagnostic."
  },
  {
    "id": "parallel-detached-signature-free-try-optional-nanoseconds-sleep-prefix",
    "fixture": "Fixtures/parallel-detached-signature-free-try-optional-nanoseconds-sleep-prefix.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedSignatureFreeTryOptionalNanosecondsSleepPrefixProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "A signature-free detached task whose literal `try? await Task.sleep(nanoseconds:)` is followed by `MainActor.run` suppresses sleep cancellation and executes that continuation in the same cancelled task; the continuation observes the cancellation bit and the handle completes successfully.",
    "notes": "Planet demand-cites the exact signature-free `Task.detached(priority: .utility) { try? await Task.sleep(nanoseconds: 200_000_000); await MainActor.run { self.isOperating = false } }` spelling at PlanetAPIController.swift:70-75 and 101-106. The bounded same-source fixture retains the implicit strong receiver, literal nanoseconds, MainActor suffix, zero-duration success, and immediately cancelled 30-second branch. Strict Apple Swift 6.3.3 compiled it with complete concurrency checking and warnings as errors and returned exact `completed:false,cancelled:true|false:true` in twenty runs; the standalone twenty-line output had SHA-256 21dcdc5c10474172ee5c69f6c6fb8fbdbcb4f2a72619d013e3cd30273145d14e, and every five-run focused shard retained 128279d479fdb8fdc315653b76f48229b0d39d7a7b39b551b6c12b11056708cb. Before the fix, cooperative and parallel interpreter modes already returned the exact value, but parallel mode recorded zero physical submissions/executions instead of two. The existing confined continuation command now admits only a nonnegative literal nanoseconds argument for the signature-free two-item route; only the sleep crosses the worker boundary, while the receiver, suffix closure, MainActor invocation, and complete RuntimeValue/Error outcome remain confined. Captured/computed nanoseconds and source-shadowed Task.sleep remain cooperative. Focused parity passed 20/20, the physical board passed 86/86, and a fresh TSan build passed native overlap 20/20 plus all 86 tests without diagnostics."
  },
  {
    "id": "parallel-detached-mainactor-run-continuation",
    "fixture": "Fixtures/parallel-detached-mainactor-run-continuation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedMainActorRunContinuationProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "A signature-free detached task whose complete body is imported `MainActor.run(body:)` enters that body even when already cancelled; the body observes the same task's cancellation bit, and the handle completes successfully while retaining that bit.",
    "notes": "Planet demand-cites the exact signature-free `Task.detached(priority: .utility) { await MainActor.run { self.isOperating = true } }` spelling at PlanetAPIController.swift:64-68 and 95-99. The bounded same-source fixture forms both tasks on MainActor, so cancelling the second handle before yielding causally precedes its MainActor body without relying on scheduler order. Strict Apple Swift 6.3.3 compiled it with complete concurrency checking and warnings as errors and returned exact `completed:false,cancelled:true|false:true` in twenty runs; the standalone twenty-line output had SHA-256 21dcdc5c10474172ee5c69f6c6fb8fbdbcb4f2a72619d013e3cd30273145d14e, and every five-run focused shard retained 1e980421bc83665ffaf6eaeac708ecf26e9e12520d4fa90e200ded68e5802c8f. Before the fix, cooperative and parallel interpreter modes already returned the exact value, but parallel mode recorded zero physical submissions/executions instead of two. A real detached wrapper now carries only an empty checked worker capability and entry/task continuation token, releases its permit at MainActor handoff, and invokes the complete original source closure through confined re-entry. The receiver, closure, MainActor body, and RuntimeValue/Error outcome never cross the worker boundary. Admission requires the generated imported MainActor.run identity; a same-named source type stays cooperative. Focused parity passed 20/20 on four workers in one second, and the complete parallel source-kernel suite passed 45/45. The canonical prebuilt iteration passed 51 implementation tests in two suites, all 46 methodology/gate checks, and all 20 parity repetitions in nine seconds. The exact-tip physical board passed 88/88 tests in three suites in one second; a fresh TSan build passed native overlap 20/20 plus all 88 tests in 27 seconds without a race or interceptor diagnostic."
  },
  {
    "id": "parallel-detached-explicit-mainactor-continuation",
    "fixture": "Fixtures/parallel-detached-explicit-mainactor-continuation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedExplicitMainActorContinuationProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "An immediately cancelled detached task with the exact `@MainActor in` operation signature still enters its actor-isolated body, which observes the same task's cancellation bit; the handle completes successfully while retaining that bit.",
    "notes": "Planet demand-cites `Task.detached(priority: .utility) { @MainActor in PlanetPublishedServiceStore.shared.reloadPublishedFolders() }` at IPFSState.swift:144-146. The bounded same-source fixture forms both tasks on MainActor, so cancelling the second handle before awaiting it causally precedes body entry without relying on scheduler order. Strict Apple Swift 6.3.3 compiled the exact spelling with complete concurrency checking and warnings as errors and returned `completed:false,cancelled:true|false:true` in twenty runs; the standalone twenty-line output had SHA-256 21dcdc5c10474172ee5c69f6c6fb8fbdbcb4f2a72619d013e3cd30273145d14e, and every five-run focused shard retained df51c444c43d210be0c338ca979a1bcaf2f38902db17119d5435a4ae6f61059a. Before the fix, cooperative and parallel interpreter modes already returned the exact value, but parallel mode recorded zero physical submissions/executions instead of two. The existing empty physical wrapper and confined continuation/outcome token now admit only one imported `@MainActor` attribute with no capture-list, parameter, effect, return, or additional-attribute surface and exactly one body expression. The complete closure and RuntimeValue/Error outcome remain confined. An additional `@Sendable` attribute and a source-shadowed MainActor stay cooperative with zero receipts. Focused parity passed 20/20 on four workers in one second, and the complete parallel source-kernel suite passed 47/47. The canonical prebuilt iteration passed 53 implementation tests in two suites, all 46 methodology/gate checks, and all 20 parity repetitions in 15 seconds. The exact-tip physical board passed 90/90 tests in three suites in one second; a fresh TSan build passed native overlap 20/20 plus all 90 tests in 55 seconds without a race or interceptor diagnostic."
  },
  {
    "id": "parallel-detached-weak-try-optional-sleep-prefix",
    "fixture": "Fixtures/parallel-detached-weak-try-optional-sleep-prefix.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedWeakTryOptionalSleepPrefixProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 2,
    "nativeFact": "A capture-only weak-self detached task that suppresses cancellation from literal-duration `Task.sleep(for:)` continues in the same cancelled task, passes its captured immutable String to the actor-isolated suffix, and completes successfully while its handle retains the cancellation bit.",
    "notes": "Session-iOS demand-cites the exact `Task.detached(priority: .medium) { [weak self] in try? await Task.sleep(for: .seconds(3)); await self?.sendFailureNotifications(groupId) }` spelling in GroupPromoteMemberJob.swift:203-206 and GroupInviteMemberJob.swift:207-210. The bounded same-source fixture retains the weak-self actor receiver, directly captured immutable String, literal Duration, two-item body, and immediately cancelled 30-second branch. Strict Apple Swift 6.3.3 compiled it with complete concurrency checking and warnings as errors and returned exact `group-a:false,group-b:true|false:true` in twenty runs; the standalone twenty-line output had SHA-256 cb53afa4960d6525d08403b93ea3b837eebeca584367848c60be88d7adfdd712, and every five-run focused shard retained cfd9ce47bbf230ed12dc5f7afa3a8f7734ca8f6b58f8153ca1e7f62263acb396. Before the fix, cooperative and parallel interpreter modes already returned the exact value, but parallel mode recorded zero physical submissions/executions instead of two. The existing confined continuation command now admits literal seconds/milliseconds Duration for the exact capture-only weak-self signature; only the sleep crosses the worker boundary, while the actor, weak box, captured String, suffix closure, and complete RuntimeValue/Error outcome remain confined. Captured/computed durations and additional capture-list entries remain cooperative. Focused parity passed 20/20, the physical board passed 84/84, and a fresh TSan build passed native overlap 20/20 plus all 84 tests without diagnostics."
  },
  {
    "id": "parallel-detached-weak-concurrent-string-source-call",
    "fixture": "Fixtures/parallel-detached-weak-concurrent-string-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedWeakConcurrentStringSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "A live weak optional-self detached operation may pass one labeled immutable String capture to an @concurrent async nonthrowing Void source-class method; the exact value arrives while the method retains nil actor isolation across suspension.",
    "notes": "Amperfy demand-cites `Task.detached(priority: .high) { [weak self] in await self?.loadImageAndCacheIt(imagePath: imagePathToDisplay) }` at LibraryEntityImage.swift:171-173; the optional-binding String is immutable and the selected @concurrent source-class method takes one labeled String, is async/nonthrowing/Void, and begins at line 177. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact cover-cache:none|none#some in twenty bounded runs; every native five-run shard retained canonical SHA-256 048303aaf53c0ed1e6f9788bb5cf8564a63ad7b333f05addf7971bc5c510e699. The deterministic interpreter RED already returned that value through cooperative fallback but recorded zero physical submissions/executions instead of one. Weak admission is a separate route proof: the checked worker carries only the copied String snapshot, while confined re-entry reloads the genuinely weak receiver, re-resolves the exact target with the materialized argument, and temporarily strengthens only a live receiver for invocation. An occupied-permit regression proves the String-bearing command does not retain a receiver released before re-entry. String literals, mutable captures, inherited/MainActor/actor routes, multiple arguments, throwing methods, and richer results remain cooperative."
  },
  {
    "id": "parallel-detached-weak-inherited-string-literal-source-call",
    "fixture": "Fixtures/parallel-detached-weak-inherited-string-literal-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedWeakInheritedStringLiteralSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "A live weak optional-self detached operation may pass one labeled String literal to an inherited-isolation async nonthrowing Void source-class method; the literal arrives exactly and the method retains nil actor isolation across suspension.",
    "notes": "Provenance demand-cites `Task.detached { [weak self] in await self?.restartProcessingIfQueueHasPendingWork(context: \"timeout\") }` at GameImporter.swift:1297-1299; the selected own source-class method begins at line 1092 and is inherited-isolation, async, nonthrowing, Void-returning, with one labeled String parameter. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact timeout:none|none#some in twenty bounded native/interpreter runs; every native five-run shard retained canonical SHA-256 fc29406e305e33efd900981045bd11728b7edc7f352ec5dd5f3dac67a99ee469. The deterministic interpreter RED already returned that value through cooperative fallback but recorded zero physical submissions/executions instead of one. Typed argument provenance distinguishes the side-effect-free String literal from an immutable capture without relying on a synthetic binding name: only this weak inherited literal route is added, while the Planet direct inherited route remains capture-only and the Amperfy weak @concurrent route remains capture-only. Mutable or captured String arguments on this route, String literals on those routes, MainActor/actor targets, multiple arguments, throwing methods, and richer results remain cooperative."
  },
  {
    "id": "parallel-detached-weak-inherited-captured-string-source-call",
    "fixture": "Fixtures/parallel-detached-weak-inherited-captured-string-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedWeakInheritedCapturedStringSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "A live weak optional-self detached operation may pass one labeled immutable String capture to an inherited-isolation async nonthrowing Void source-class method; the copied value arrives exactly and the method retains nil actor isolation across suspension.",
    "notes": "Session-iOS repeats `Task.detached(priority: .userInitiated) { [weak self] in await self?.updateMentions(for: newText) }` at ConversationVC+Interaction.swift:1016-1018 and AttachmentApprovalViewController.swift:860-862; each selected own source-class method is inherited-isolation, async, nonthrowing, Void-returning, and accepts one labeled String. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact session-message:none|none#some in twenty bounded native/interpreter runs; every native five-run shard retained canonical SHA-256 d12895520e72e0f0c35194394fa4cc6fe01f5cf0bba7f133fc04e8fe06994403. The deterministic interpreter RED already returned that value through cooperative fallback but recorded zero physical submissions/executions instead of one. The weak inherited route now accepts its existing typed capturedImmutable String origin as well as the separately proved literal origin; confined re-entry still reloads the genuine weak receiver and materializes only the checked snapshot. Mutable or expression Strings, MainActor/actor methods, multiple arguments, throwing methods, and richer results remain cooperative."
  },
  {
    "id": "parallel-detached-weak-inherited-string-array-source-call",
    "fixture": "Fixtures/parallel-detached-weak-inherited-string-array-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedWeakInheritedStringArraySourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "A live weak optional-self detached operation may pass one unlabeled immutable [String] capture to an inherited-isolation async nonthrowing Void source-class method; every copied element arrives exactly and the method retains nil actor isolation across suspension.",
    "notes": "KeyboardCowboy demand-cites `Task.detached { [weak self] in await self?.reload(additionalPaths) }` at ApplicationStore.swift:79-81; `additionalPaths` is an immutable [String] and the selected own method at line 90 is inherited-isolation, async, nonthrowing, Void-returning, with one unlabeled [String] parameter. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact Applications,WebApps:none|none#some in twenty bounded native/interpreter runs; every native five-run shard retained canonical SHA-256 1782ec2cb9384815bd4effeae9daa6f2521dc4064c23871eea026619d5184104. The deterministic interpreter RED already returned that value through cooperative fallback but recorded zero physical submissions/executions instead of one. A typed stringArray command kind verifies both the declared [String] element type and every recursively copied worker snapshot element; admission is limited to one directly owned immutable capture on the weak inherited route. Direct inherited, weak @concurrent, and weak MainActor [String] calls remain cooperative with zero receipts; mutable, literal/expression, other-element, multiple-argument, throwing, and richer-result forms remain outside the route."
  },
  {
    "id": "parallel-detached-strong-self-capture-source-call",
    "fixture": "Fixtures/parallel-detached-strong-self-capture-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedStrongSelfCaptureSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "An ordinary capture-only [self] in signature strongly captures the source-class receiver without introducing actor isolation; its plain async Void method inherits the detached caller's nil actor isolation before and after Task.yield.",
    "notes": "Provenance demand-cites the exact Task.detached(priority: .userInitiated) { [self] in await self.registerDefaults() } spelling in DiscSerialExtractorRegistry.swift:105-107; the registry is Sendable and registerDefaults() is an inherited-isolation, async, nonthrowing, argument-free, Void-returning own source-class method at lines 90-100. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact strong:none|none in twenty bounded runs; every native five-run shard retained canonical SHA-256 14c92e632cbf820172e940cbe85fb910e691b2b5a8e7ccd49b1a5f976660df9e. The deterministic interpreter RED returned the same value and drained its runtime state, but recorded zero physical submissions/executions instead of one. Admission recognizes only one ordinary strong self capture with no alias, ownership modifier, attributes, parameters, effects, return clause, additional capture, or trailing comma, and may use it only for a direct-self source-call wrapper. Unowned, alias, multi-capture, explicit-signature, and non-call forms stay cooperative; weak/optional-self async dispatch and richer closure forms remain unclaimed outside this slice."
  },
  {
    "id": "parallel-detached-weak-self-source-call",
    "fixture": "Fixtures/parallel-detached-weak-self-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelDetachedWeakSelfSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 1,
    "nativeFact": "A capture-only [weak self] detached wrapper reads the weak receiver when its body executes, temporarily retains a live receiver for one argument-free async Void optional call, and preserves the detached caller's nil actor isolation across Task.yield.",
    "notes": "Provenance demand-cites the exact Task.detached(priority: .background) { [weak self] in await self?.processQueue() } spelling in CloudKitDownloadQueue.swift:188-190; processQueue() is an inherited-isolation, async, nonthrowing, argument-free, Void-returning own source-class method. Apple Swift 6.3.3 complete-strict compilation with warnings as errors and interpreted execution returned exact weak:none|none in twenty bounded runs; every native five-run shard reported canonical SHA-256 59b08bc91e9e8533552cc5edbaeedf8d0af325e761b6131da38169d353f0b121. The deterministic receipt RED already returned that value through cooperative fallback but recorded zero physical submissions/executions instead of one. Admission is limited to exactly one ordinary weak self capture, one optional-self call, and this inherited argument-free Void method family. The worker command contains no receiver, weak box, closure, environment, or evaluator; confined registration retains only the source closure's genuine weak box and resolves the receiver after MainActor re-entry. A causal occupied-permit regression releases the only strong receiver before re-entry and receives Optional.none through one physical wrapper, proving registration does not strengthen the weak capture. Nil-at-admission, weak argument-bearing calls, String-returning calls, aliases, additional captures, explicit signatures, unowned captures, and all richer forms remain cooperative."
  },
  {
    "id": "weak-self-optional-async-source-call",
    "fixture": "Fixtures/weak-self-optional-async-source-call.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await weakSelfOptionalAsyncSourceCallProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "An alive receiver reached through await weakSelf?.method() executes its async method and suspends normally, while a nil optional receiver skips both argument evaluation and invocation and returns nil; optional chaining adds only one Optional result level.",
    "notes": "Repeated corpus demand includes Session's Task.detached { [weak self] in await self?.loadAndCacheAlbumData(...) } calls at MediaGalleryViewModel.swift:477-484 and Provenance's no-argument processQueue wrapper at CloudKitDownloadQueue.swift:188-190. The same-source oracle retains one weak receiver through an awaited detached handle, observes nil actor isolation around Task.yield, then invokes a nil optional with a fatal argument trap that must not be evaluated. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact alive:none|none|nil in twenty bounded runs; every native five-run shard retained canonical SHA-256 76d6367b231b0e0530517d36cac3c518d1d5eb029e181e53e7ac83722d93b2a7. The interpreter RED entered the selected async method through synchronous optional-member invocation and failed when Task.yield required the suspension-aware runtime. Async explicit-member evaluation now evaluates the optional base exactly once, returns nil before collecting arguments when absent, or rewrites a present source class/actor reference into the ordinary suspension-aware call path and flattens its result. Optional value types keep their prior path until mutating write-back is separately proven. This String-returning optional call and all argument-bearing weak calls remain cooperative with zero physical receipts; only the separately proved capture-only inherited argument-free Void route may launch physically. Optional async closure invocation and general physical weak-call families remain outside this slice."
  },
  {
    "id": "optional-async-closure-invocation",
    "fixture": "Fixtures/optional-async-closure-invocation.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await optionalAsyncClosureInvocationProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "An optional async closure reached through await callback?() executes and suspends normally when present; nil skips argument evaluation and invocation, returns nil, and optional chaining adds only one Optional result level.",
    "notes": "Corpus demand includes Aidoku's await loadMore?() at HomeListView.swift:145, iTorrent's await refreshTask?() at BaseCollectionViewController.swift:31, and Provenance's await iterationComplete?() at iCloudContainerSyncer.swift:187; each declaration is an optional async closure. The same-source oracle invokes a present @Sendable closure with one integer, observes nil actor isolation around Task.yield, then invokes a nil optional with a fatal argument trap that must not be evaluated. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact live:7:none|none|nil in twenty bounded runs; every native five-run shard retained canonical SHA-256 aeba7d1f8923f7f34fb729387f5cf709d80eefe1cf283d8faff2811c04e40706. The interpreter RED evaluated the present Optional closure through synchronous invocation and failed when Task.yield required the suspension-aware runtime. Suspension-aware call evaluation now recognizes optional chaining on the callee before argument collection: nil returns immediately, while a present callable is unwrapped, invoked through the ordinary async path, and flattened to one Optional result level. The source closure and captures remain cooperative with zero physical receipts; optional value-type async chains, weak-receiver destruction races, and physical weak-reference transfer remain outside this slice."
  },
  {
    "id": "weak-receiver-release-across-suspension",
    "fixture": "Fixtures/weak-receiver-release-across-suspension.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await weakReceiverReleaseAcrossSuspensionProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "Once a detached [weak receiver] task has causally entered and suspended before reading that capture, releasing the final strong owner deallocates the receiver; resumption observes nil and skips the optional call.",
    "notes": "Session demand-cites this lifetime shape in GroupPromoteMemberJob.swift:203-209 and GroupInviteMemberJob.swift:207-209, where a Task.detached [weak self] operation sleeps before invoking self?.sendFailureNotifications(...). The same-source oracle replaces elapsed time with a reentrant actor gate: the detached task causally reports entry and suspends, the parent clears the only strong receiver, and only then does the gate reopen before the optional read. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact released in twenty bounded runs; every native five-run shard retained canonical SHA-256 c5e92e2453b9fcfd589dc5d3b917f8708b27239decb0a58ca175b11b85c27b6e. The unchanged interpreter was already GREEN with the same output, zero physical receipts, and empty task/actor registries. RuntimeTaskRecord may strongly own the source closure, but that closure's weak capture box does not retain the source instance. This characterization remains the cooperative multi-statement lifetime guarantee; the separately proved one-expression inherited argument-free Void route also defers its weak read until confined re-entry, while general physical weak-call families remain forbidden."
  },
  {
    "id": "optional-value-mutating-async-writeback",
    "fixture": "Fixtures/optional-value-mutating-async-writeback.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await optionalValueMutatingAsyncWritebackProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "An async mutating method reached through optional chaining borrows a present source value across suspension and writes its final value back through both direct Optional storage and an enclosing source-value property; nil remains unchanged and returns nil.",
    "notes": "The official swiftlang/swift test/IRGen/run-coroutine_accessors.swift at swift-6.3.3-RELEASE commit 064859e41d68596f486c5d724401cb370f260409, SHA-256 2fdae2aa9cd0153da1db13b5e227c6fe5a74112eda85b91795fad9554a80cc95, lines 259-270 and 368-386 requires await b.value?.mutate() to preserve nil and copy the mutated present payload back as hihi. This same-source distillation adds a real Task.yield plus direct and nested Optional lvalues. Apple Swift 6.3.3 complete-strict compilation with warnings as errors and interpreted execution returned exact direct-entered-resumed:direct-entered-resumed|nested-entered-resumed:nested-entered-resumed|nil:nil in twenty bounded runs; every native five-run shard retained canonical SHA-256 7b98f96ff0ce968ecede27d9907e5ae498748983dd442b768d9b47f5d171a234. The deterministic interpreter RED invoked the present mutating method through the synchronous optional-member path and failed at Task.yield with async host function 'yield' requires runAsync and await. Suspension-aware optional-member dispatch now borrows a writable Optional source-value payload through its existing LValue transaction, invokes the mutating method on an independent working copy, and commits the final receiver through the same direct or enclosing stored-property path before lifting the result once. Nil returns before arguments or invocation. The case remains cooperative with zero physical receipts; throwing/cancellation exits and coroutine/computed/subscript accessor families remain separately unclaimed."
  },
  {
    "id": "optional-value-mutating-async-throwing-writeback",
    "fixture": "Fixtures/optional-value-mutating-async-throwing-writeback.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await optionalValueMutatingAsyncThrowingWritebackProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "A present Optional source value borrowed for an async mutating call writes mutations performed before a throwing exit back to its storage; this unwind rule also applies when the thrown exit is the current task's CancellationError.",
    "notes": "The official swiftlang/swift test/IRGen/run-coroutine_accessors.swift at swift-6.3.3-RELEASE commit 064859e41d68596f486c5d724401cb370f260409, SHA-256 2fdae2aa9cd0153da1db13b5e227c6fe5a74112eda85b91795fad9554a80cc95, lines 259-270 and 368-386 proves optional async mutation uses coroutine write-back. The same release's test/Interpreter/coroutine_accessors_old_abi_nounwind.swift, SHA-256 68f7dec3be698ae0010c9cf35b25bbc8c53e910e31d2cfd14a67145bb9fc2ce9, lines 9-41 proves the modify cleanup runs on a throwing unwind. This same-source distillation uses direct Optional storage for the typed source error and an enclosing source-value stored property for Task.checkCancellation after deterministic self-cancellation; both payloads mutate before and after Task.yield. Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact threw:throw-entered-resumed|cancelled:cancel-entered-resumed in twenty bounded runs; every native five-run shard retained canonical SHA-256 a7e9b9c675182511ce7f0f538699d283c2b57d960f1a0c2b574c29e4e270f295. The deterministic interpreter RED caught both expected errors but returned threw:throw|cancelled:cancel, proving that exceptional exit discarded the working receiver. The ordinary suspending mutating-method helper now accepts a confined write-back-on-exit transaction, resolves final self after normal or exceptional body completion, commits through the Optional path's existing LValue, and only then returns or rethrows the original failure. The case remains cooperative with zero physical receipts; coroutine/computed/subscript accessor families remain separately unclaimed."
  },
  {
    "id": "optional-computed-value-mutating-async-writeback",
    "fixture": "Fixtures/optional-computed-value-mutating-async-writeback.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await optionalComputedValueMutatingAsyncWritebackProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "A writable synchronous computed Optional property on a source struct is read once for an async mutating optional-chain call and written once with the final payload after either normal return or a throwing unwind.",
    "notes": "The official swiftlang/swift test/IRGen/run-coroutine_accessors.swift at swift-6.3.3-RELEASE commit 064859e41d68596f486c5d724401cb370f260409, SHA-256 2fdae2aa9cd0153da1db13b5e227c6fe5a74112eda85b91795fad9554a80cc95, lines 200-220 and 368-386 proves computed Optional access is a coroutine copy-in/copy-out owner around an async mutation. The same release's test/Interpreter/coroutine_accessors_old_abi_nounwind.swift, SHA-256 68f7dec3be698ae0010c9cf35b25bbc8c53e910e31d2cfd14a67145bb9fc2ce9, lines 9-41 proves its cleanup runs on unwind. This same-source distillation uses the ordinary get/set spelling and records getter, suspended method, setter, and final stored payload for normal and typed-throw exits. Apple Swift 6.3.3 complete-strict compilation with warnings as errors and interpreted execution returned exact get|enter|exit:seed-entered-resumed|set:seed-entered-resumed|seed-entered-resumed#get|enter|exit:seed-entered-resumed|set:seed-entered-resumed|seed-entered-resumed in twenty bounded runs; every native five-run shard retained canonical SHA-256 a02611976cbcd7de5b34b6f5bb05bcfddd74953d47d67d9db2666eb907bc88ab. The deterministic interpreter RED returned get|enter|seed#get|enter|seed: it read once and began each method, but synchronous optional dispatch lost the suspension-aware continuation and never invoked either setter. The confined Optional LValue transaction now admits a directly named source-value property only when metadata proves a synchronous nonthrowing getter, a setter, and an Optional annotation. It reads once, invokes the ordinary suspending mutating copy, reconstructs the original typed Optional wrapper, and writes through the same computed lvalue on return or unwind. The case remains cooperative with zero physical receipts; one companion interpreter-diagnostic case pins the named fail-closed boundary for every unadmitted owner."
  },
  {
    "id": "optional-computed-reference-mutating-async-fail-closed",
    "fixture": "Fixtures/optional-computed-reference-mutating-async-fail-closed.swift",
    "mode": "runtime",
    "assertion": "interpreter-diagnostic",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await optionalComputedReferenceMutatingAsyncFailClosedProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "interpreterDiagnosticContains": [
      "async optional value mutation",
      "computed reference owner",
      "unsupported"
    ],
    "nativeFact": "A class-owned writable computed Optional property copies a value payload back after its async mutating method suspends and returns.",
    "notes": "Apple Swift 6.3.3 complete-strict compilation with warnings as errors returned exact seed-entered-resumed in twenty bounded runs; every native five-run shard retained canonical SHA-256 ce729f52a3592f1c93a7140c8bc10addb7e62d41ae42f06a3651ce98efcba89a. Before the boundary landed, the interpreter entered the mutating method synchronously and failed late at Task.yield with an unrelated requires-runAsync diagnostic. It now intentionally fails closed before argument evaluation or payload mutation because suspension-safe computed reference-owner write-back is outside the demand depth cap. The named async optional value mutation/storage-path diagnostic is the citation path for reopening native coroutine, richer computed-owner, nested-owner, and subscript transactions; silent partial execution is forbidden."
  },
  {
    "id": "parallel-captured-string-distance",
    "fixture": "Fixtures/parallel-captured-string-distance.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelCapturedStringDistanceParityProbe()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 2,
    "expectedPhysicalSourceKernelExecutions": 3,
    "nativeFact": "A Task.detached body may capture an immutable String and a String.Index derived from that same value, compute distance from startIndex, and publish the exact grapheme-step count through its awaited handle. Cancelling a non-checking task does not suppress that value, although its handle remains marked cancelled; no worker order is implied.",
    "notes": "CotEditor demand-cites this standard-library expression at Sources/Scanners/EditorCounter.swift:191. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and returned exact 2:5|2:true in twenty bounded runs; every five-repetition native shard reported SHA-256 da866620191667df04f2901b8f5ca2478e97f730df4787c6d7c5b18260518f28. The fixture includes extended grapheme clusters, awaits two ordinary handles, and immediately cancels a third before awaiting its still-successful value. The value path was already behaviorally correct through cooperative fallback, but the captured receipt RED observed zero physical executions instead of two; after physical lowering, a second RED exposed infrastructure cancellation escaping into the non-checking source body as CancellationError. MainActor admission now requires the exact distance(from: string.startIndex, to: location) spelling over directly owned immutable String and String.Index captures derived by source code, copies only those typed snapshots, and emits typed startIndex/distance IR. A source-kernel execution policy isolates the finite native job from logical source cancellation, while the ordinary driver API still forwards infrastructure cancellation. Mutable bindings, globals, alternate from expressions, authored signatures, and all other shapes remain cooperative. Twenty paired cooperative/parallel runs preserve exact 2:5|2:true, empty registries, and zero/three receipts; no worker order, thread identity, or general Task.isCancelled kernel is claimed."
  },
  {
    "id": "parallel-shadowed-string-distance",
    "fixture": "Fixtures/parallel-shadowed-string-distance.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelShadowedStringDistanceOutput()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "A same-module method declared in an extension of String with the exact distance(from:to:) signature shadows the standard-library method, including inside a Task.detached operation, so the task publishes the source method's exact Int value.",
    "notes": "CotEditor demand-cites detached String.distance(from:to:) at Sources/Scanners/EditorCounter.swift:191. This gap-closure oracle proves that receiver shape, member name, and argument labels are not a declaration identity. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced exact 77 in twenty bounded runs; every five-repetition native shard reported SHA-256 0e8f6fc06b0a6860d392095f867a5b94dc032ed589e0a43d0ce9b30924258f53. Before the fix, cooperative interpretation selected the source method and returned 77, while explicit parallel mode executed the standard-library distance kernel, returned 2, and recorded one physical submission/execution. Physical lowering now asks the originating RuntimeProgramState for a typed standard-library method proof. Until session-owned overload resolution can publish an exact declaration, any same-base source extension overload fails closed; labels alone cannot prove identity in the presence of defaults, generics, and parameter types. A retained two-run regression proves origin-state ownership, while the positive stdlib distance probe retains its physical receipt. No worker identity or unrelated scheduler order is asserted."
  },
  {
    "id": "parallel-shadowed-array-map",
    "fixture": "Fixtures/parallel-shadowed-array-map.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelShadowedArrayMapOutput()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "A more-specific same-module Array.map overload for Element == Substring is selected by map(\\.count) inside Task.detached, so the surrounding reduction publishes the source method's exact result.",
    "notes": "CotEditor demand-cites selectedStrings.map(\\.count).reduce(0, +) at Sources/Scanners/EditorCounter.swift:176. This gap-closure oracle isolates declaration selection for map; reduce, the element count key path, and unrelated overload families remain separate target proofs. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced exact 41 in twenty bounded runs; every five-repetition native shard reported SHA-256 06012cab1f7bd007fb1ad3ff15d7822d89b450f3ea30814bf7d8f27b8d2687bb. Before the fix, both cooperative and explicit-parallel interpretation ignored the more-specific source overload and returned the standard-library result 3; explicit parallel mode additionally recorded one physical submission/execution. Core Array source extensions now dispatch before imported native members. The count-reduction lowerer also requires a typed standard-library Array.map proof from the originating RuntimeProgramState; any same-base source overload fails closed. A retained two-run regression proves origin-state ownership while the positive stdlib map probe retains its physical receipt. No worker identity or scheduler order is asserted."
  },
  {
    "id": "parallel-shadowed-array-reduce",
    "fixture": "Fixtures/parallel-shadowed-array-reduce.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelShadowedArrayReduceOutput()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "A more-specific same-module Array.reduce overload for Element == Int is selected after map(\\.count) inside Task.detached, so the task publishes the source method's exact result.",
    "notes": "CotEditor demand-cites selectedStrings.map(\\.count).reduce(0, +) at Sources/Scanners/EditorCounter.swift:176. This gap-closure oracle isolates declaration selection for reduce; the element count key path and unrelated overload families remain separate target proofs. Apple Swift 6.3.3 compiled the fixture in Swift 6 complete-strict mode with warnings as errors and produced exact 73 in twenty bounded runs; every five-repetition native shard reported SHA-256 510c4b8fd1343251f41b2cfbed0f4b84e7517a95b4603e64a96e98284898f67e. Before the fix, cooperative interpretation selected the source overload and returned 73, while explicit parallel mode executed the standard-library reduction, returned 3, and recorded one physical submission/execution. The count-reduction lowerer now requires a typed standard-library Array.reduce proof from the originating RuntimeProgramState in addition to its Array.map proof; any same-base source overload fails closed. A retained two-run regression proves origin-state ownership while the positive stdlib reduction retains its physical receipt. No worker identity or scheduler order is asserted."
  },
  {
    "id": "parallel-shadowed-substring-count",
    "fixture": "Fixtures/parallel-shadowed-substring-count.swift",
    "mode": "runtime",
    "assertion": "exact",
    "repetitions": 20,
    "timeoutSeconds": 5,
    "interpreterEntry": "await parallelShadowedSubstringCountOutput()",
    "interpreterProjection": "string",
    "interpreterExecutionMode": "parallel",
    "interpreterMaximumParallelism": 1,
    "expectedPhysicalSourceKernelExecutions": 0,
    "nativeFact": "A same-module Substring.count computed property is selected by a context-inferred \\.count key path over an explicitly typed [Substring] inside Task.detached, while the identical key path over [String] still selects String.count; the exact combined result is 178:3.",
    "notes": "CotEditor demand-cites selectedStrings.map(\\.count).reduce(0, +) at Sources/Scanners/EditorCounter.swift:176. This oracle isolates the key-path element property after the Array.map and Array.reduce target proofs. The explicit [Substring] versus [String] annotations are semantic evidence for each key path's inferred root; their shared runtime String storage is not a declaration identity. Apple Swift 6.3.3 compiled the same-source fixture in complete strict Swift 6 mode with warnings as errors and returned exact 178:3 in twenty bounded runs; all four five-run native shards reported SHA-256 6c177a2ff65fa47eaf04034c87bd3446d3047a1267fb98e302d5634fb93cb2a5. Before the fix, both interpreter modes returned 3 for the Substring branch and explicit parallel mode recorded one physical submission/execution. Array key-path application now carries the source binding's static element type into root-property dispatch, so Substring and String remain distinct even though RuntimeValue stores both as String snapshots. Physical admission separately requires an explicit Substring array annotation and a typed standard-library Substring.count target identity from the closure's originating RuntimeProgramState; a source extension fails closed. The retained two-run regression returns exact 178 and 3 with zero receipts, while the positive stdlib Substring reduction keeps its physical path. No worker identity or scheduler order is asserted."
  }
]
